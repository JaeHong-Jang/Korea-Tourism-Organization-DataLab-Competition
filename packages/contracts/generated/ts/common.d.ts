/* 자동 생성 — packages/contracts/schemas에서 npm run contracts:gen 으로 만든다. 직접 고치지 않는다 */

/**
 * 여러 계약이 함께 쓰는 형식. 날짜는 KST ISO 8601, 인원·금액은 정수, 값 없음은 null. id는 종류 접두사를 가진다
 */
export interface Common {
  [k: string]: unknown;
}
