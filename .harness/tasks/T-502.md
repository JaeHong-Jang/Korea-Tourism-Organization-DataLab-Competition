# T-502: 행사 CRUD + 예보 스냅샷(불변)
- 레인/워커: L5 / gpt-6-sol        - 선행: T-501 완료
- 목표(한 문장): 내 행사 저장·조회·삭제와, 한 번 저장하면 바뀌지 않는 예보 스냅샷 API를 계약대로 만든다.
- 읽을 것: packages/contracts/openapi/records.yaml(`/v1/events`·`/v1/events/{id}`·`/v1/events/{id}/snapshots`), schemas/event·forecast-report(스냅샷 = 발행된 예보서 전체, 불변), docs/plan/11 M5-F1·M5-F2
- 허용 경로: `services/records/src/{Events,Snapshots}/`(Controller·Repository·Service 파일 분리), `services/records/src/Routes/`, `services/records/migrations/`(새 번호 파일만), `services/records/tests/`
- 인터페이스: 요청·응답 모두 ContractValidator로 검증, 잘못된 요청은 400(JSON 오류 본문), 스냅샷 UPDATE·DELETE 시도는 409
- 수용 기준(실행 명령): `cd services/records && composer test && composer stan` 통과(CRUD, 계약 위반 400, 스냅샷 불변 409, 픽스처 forecast 저장·조회 왕복이 같음)
- 범위 밖: 초안·원장(T-503·T-504)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5
