# T-502: 행사 CRUD + 예보 스냅샷(불변)
- 레인/워커: L5 / gpt-6-sol        - 선행: T-501 완료
- 목표(한 문장): 내 행사 저장·조회·삭제와, 한 번 저장하면 바뀌지 않는 예보 스냅샷 API를 계약대로 만든다.
- 읽을 것: packages/contracts/openapi/records.yaml(`/v1/events`·`/v1/events/{id}`·`/v1/events/{id}/snapshots`), schemas/event·forecast-report(스냅샷 = 발행된 예보서 전체, 불변), docs/plan/11 M5-F1·M5-F2
- 허용 경로: `services/records/src/{Events,Snapshots}/`(Controller·Repository·Service 파일 분리), `services/records/src/Routes/`, `services/records/migrations/`(새 번호 파일만), `services/records/tests/`
- 인터페이스: 요청·응답 모두 ContractValidator로 검증, 잘못된 요청은 400(JSON 오류 본문), 스냅샷 UPDATE·DELETE 시도는 409
- 수용 기준(실행 명령): `cd services/records && composer test && composer stan` 통과(CRUD, 계약 위반 400, 스냅샷 불변 409, 픽스처 forecast 저장·조회 왕복이 같음)
- 범위 밖: 초안·원장(T-503·T-504)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5

## 오케스트레이터 처리 (PARTIAL 해소, 2026-09-24)
- 허용 경로를 좁게 잡은 것은 명세 실수다. 오케스트레이터가 `src/App.php`에 `RecordsRoutes::register($app, $db)`를 직접 연결했다(`App::create(?PDO $db = null)` — 테스트는 메모리 DB를 넘긴다, `RecordsApiTest`의 중복 등록 제거).
- 실제 서버 확인(php -S, 임시 데이터 루트): POST /v1/events 200 · GET 목록 · 잘못된 행사 400 · 스냅샷 POST 200 · 같은 스냅샷 재전송 409 · records.sqlite가 CROWDCAST_DATA_ROOT 아래 생성.

## 게이트 피드백 1회차 (게이트 3 교차 리뷰 FAIL — gpt-6-astra) — 워크트리에 1차 결과와 오케스트레이터의 App.php 연결이 있다
1. [High] `src/Snapshots/Service.php:29` — 스냅샷 안의 id가 서로 맞는지 검사하지 않는다(최상위 `forecastId`만 바꾸면 같은 예보가 다른 id로 다시 200 저장, 다른 `card.eventId`도 통과). → 계약 `packages/contracts/rules/integrity.mjs`의 `reportProblems` 머리 규칙과 같은 판정을 PHP로: `forecast.id = forecastId = card.id`, `forecast.eventId = event.id = card.eventId = 경로의 {id}`, 모든 `claims[].forecastId = forecastId`·`claims[].sessionId = sessionId`. 어긋나면 400. 저장된 스냅샷을 돌려줄 때도 같은 검사를 통과해야 한다(응답 검증).
2. [Med] `src/Snapshots/Service.php:60` — 행사를 삭제(tombstone)하면 활성 행사만 찾는 `events->find()` 때문에 기존 스냅샷 GET이 404가 된다. → 스냅샷 조회는 삭제 표시된 행사도 존재로 본다(발행 이력은 남아야 한다). 삭제 후 조회 회귀 테스트.
3. [Med] `src/Routes/JsonResponse.php:22` — `expectedByHost.value`를 `1e400`으로 넣으면 POST는 200인데 목록 재인코딩에서 `INF` 때문에 GET 전체가 500. → 저장 전에 모든 수치가 유한한지(재인코딩 가능한지) 확인해 400. 행사·스냅샷 두 저장 경로에 회귀 테스트.

## 게이트 피드백 2회차 (게이트 3 재리뷰 FAIL — gpt-6-astra) — 마지막 재디스패치
1회차 3건(id 불일치 9종 400, tombstone 뒤 조회, 비유한 수치 400)은 리뷰어가 재현해 해결을 확인했다. 남은 High 2건은 **계약 규칙을 PHP로 옮겨** 막는다(직접 새 규칙을 만들지 말 것).
1. [High] 숫자 카드 = 예보의 투영 — `packages/contracts/rules/card-projection.mjs`의 `projectCard`·`cardDiff`를 `src/Snapshots/CardProjection.php`로 그대로 옮긴다(키 순서 무관 비교 = `canonical`). `card`가 투영과 다르면 400.
2. [High] 예보서 참조 무결성 — `packages/contracts/rules/integrity.mjs`의 `refProblems(doc, "forecast-report", master)`(범위 없음)를 `src/Snapshots/ReportIntegrity.php`로 옮긴다: 모든 참조 칸(REF_KEYS)을 문서 안에서(규칙·조항·데이터셋·모델 실행은 `packages/contracts/jsonld/master-ids.json` + 등록된 모델 카드 id `mr-v0-1-0` 등) 풀고, 같은 id 다른 내용 충돌, 자리표시자 대상 칸 null, `reportProblems`(머리 일치·근거 묶음 합집합·유사/평시·문장 세션·예보·검사 revision). 문제가 하나라도 있으면 400(요청)·500(저장본 응답 검증 실패).
   - **판정 일치 테스트**: `packages/contracts/fixtures-integrity/forecast-report/*.json` 전부를 PHP로 판정해 `valid-`는 통과·`invalid-`는 거부인지(계약 JS와 같은 결과) — 한 파일이라도 다르면 실패.
3. [Med] `src/Snapshots/Repository.php:21` — 발행 시각을 문자열로 정렬해 시간대가 다르면 순서가 뒤집힌다 → 원문은 그대로 두고 정렬은 UTC로 환산한 값(예: 저장할 때 `published_at_utc` 열을 새 마이그레이션으로 추가하거나 정렬 시 환산).

## 오케스트레이터 직접 수정 (재디스패치 2회 소진)
- `ReportIntegrity`가 "등록된 모델 실행"을 계약의 **테스트 픽스처 폴더**(`fixtures/model-card/valid-*.json`)에서 읽고 있었다(피드백 2회차 문구가 유도한 실수). 모델 실행은 계속 새로 등록되므로 이렇게 두면 새 모델 등록 뒤 과거 스냅샷 응답 검증이 깨진다 → records는 `mr-` 형식만 확인하고 **등록 여부는 발행 시점에 근거 그래프(knowledge `/publish`, S08)가 본다**. 규칙·조항·데이터셋·가정은 계약 `master-ids.json`으로 계속 검사.
- 확인: test 17·147, stan 0, 무결성 픽스처 판정 불변(400 11 · 200 1 · 409 1), 새 모델 id `mr-v0-2-0` 스냅샷 저장·조회 200.
