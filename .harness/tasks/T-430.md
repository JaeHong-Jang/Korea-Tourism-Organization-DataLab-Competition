# T-430: 예보팀 펫 13종 + 상태 5종 + PetAvatar + 견본
- 레인/워커: L4c / gpt-6-sol        - 선행: T-401(웹 셸) 완료
- 목표(한 문장): 예보팀 팀장 1 + 팀원 12명을 한 세계관의 펫 캐릭터(SVG)로 만들고, 상태 5종을 움직임으로 보여 주는 `PetAvatar` 컴포넌트와 견본 페이지를 만든다.
- 읽을 것: docs/plan/04 §7(펫 모양·팀 표시·표정·형식)·§3-4(팀 색)·§13, docs/plan/10 §2(팀원 이름·역할), packages/contracts/schemas/agent-status.schema.json(agentId·team·state), packages/contracts/design-tokens.css(`--team-*`)
- 허용 경로: `apps/web/src/components/pets/` (펫마다 파일 하나: `lead.tsx` … `briefer.tsx`, `pet-avatar.tsx`, `pet-body.tsx`(공통 몸통), `pets-gallery.tsx`, `pets.css`, `index.ts`), `apps/web/src/components/pets/__tests__/`
- 인터페이스:
  - `<PetAvatar agentId state size label? />` — `agentId`·`state`는 계약 enum 그대로, `size` 32·48·96, 이름표(한국어 이름: 지휘·받아쓰기·동네지기·기록관·예보관·출처확인·숫자대조·법규담당·깐깐이·해설가·카드장인·계획서·브리핑)를 항상 함께
  - 모양: 구름처럼 둥근 몸통 + 어두운 얼굴 창 + 픽셀 눈 두 개 + 가슴 역할 기호(04 §7 표). 몸통 색 = 팀 토큰(`--team-analysis`·`--team-verification`·`--team-report`, 팀장 `--team-lead` + 지휘봉). 참고 이미지를 그대로 따라 그리지 않는다(자체 디자인)
  - 상태: idle(가끔 깜빡임) · working(눈 좌우, 몸 살짝 통통) · done(눈웃음) · waiting/blocked(눈 − −, 손 들기) · error(× ×). CSS 키프레임, `prefers-reduced-motion`이면 정지
  - 견본: `pets-gallery.tsx`를 `/dev/pets`에서 보여 준다(라우트 등록은 L4a 셸이 이미 `/dev/*`를 제공 — 없으면 리포트에 요청)
- 수용 기준(실행 명령):
  - `npm -w apps/web test` 통과(13종 × 5상태 렌더, 이름표·aria-label 존재, 알 수 없는 agentId는 오류)
  - Playwright로 `/dev/pets`를 낮·밤 1366×768로 찍어 `reports/figures/screens/T-430-pets-<day|night>.png`(한글 이름표가 보여야 함)
  - 색 하드코딩 0(토큰만), `npm -w apps/web run lint` 통과
- 범위 밖: 작업판 배치(T-405), 3D 카메오(T-435)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5 (펫 하나 = 파일 하나)
