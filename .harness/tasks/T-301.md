# T-301: 게이트웨이(TypeScript·Hono) 골격 + 서비스 클라이언트 + 상태 확인
- 레인/워커: L3 / gpt-6-astra        - 선행: R-01 통과
- 목표(한 문장): 웹의 유일한 API 창구인 Hono 게이트웨이 골격을 만들고, 세 백엔드 클라이언트(응답 계약 검증 포함)와 앱·Ollama 상태를 따로 보여 주는 `/api/health`를 둔다.
- 읽을 것: docs/plan/05 §2·§4·§8·§11, docs/plan/10 §0·§4, packages/contracts/openapi/gateway.yaml, packages/contracts/scripts/check.mjs(Ajv 2020 등록 방식), AGENTS.md §3·§5
- 허용 경로: `services/gateway/` 전체(`package.json`, `tsconfig.json`, `biome.json`, `src/`, `tests/`)
- 인터페이스:
  - `package.json` 이름 `@crowdcast/gateway`, 스크립트 `dev`(tsx watch, 포트 8787), `test`(vitest run), `build`(tsc --noEmit), `lint`(biome). 의존: hono, @hono/node-server, ajv, ajv-formats, `@crowdcast/contracts`(workspace)
  - 레포 루트에서 `npm -w services/gateway run dev`로 뜬다(dev.mjs 등록 명령)
  - `GET /api/health` → `{ services: { forecast|knowledge|records: { ok, latencyMs } }, ollama: { ok, host } }` — 앱 서비스와 Ollama를 **따로** 확인, 각 2초 제한, 하나가 꺼져도 200으로 상태만 알린다
  - `src/clients/{forecast,knowledge,records}-client.ts`: 시간 제한 fetch + 응답을 계약 스키마로 검증(Ajv 2020, `ajv/dist/2020`)
  - `src/config.ts`: 포트·서비스 주소·`OLLAMA_HOST`(dev.mjs가 넘겨줌)
  - 폴더: `src/routes/`(라우트 하나당 파일), `src/clients/`, `src/contract/`(Ajv 등록), `src/team/`·`src/llm/`은 다음 task(빈 폴더 만들지 않기)
- 수용 기준(실행 명령):
  - `npm -w services/gateway test` 통과: health가 서비스별·Ollama 상태를 분리(fetch 가짜로), 클라이언트가 invalid 픽스처 응답을 거부
  - `npm -w services/gateway run build` 통과(타입 오류 0), `npm -w services/gateway run lint` 통과
  - `node scripts/dev.mjs --check`에 `✓ gateway`
- 범위 밖: 예보팀 런타임·Ollama 호출(T-302·T-303), SSE, 다른 레인 파일
- 외부 호출: npm 설치를 위한 네트워크만
- 코드 규칙: AGENTS.md §5 (파일 kebab-case, 문단마다 한 줄 주석)
