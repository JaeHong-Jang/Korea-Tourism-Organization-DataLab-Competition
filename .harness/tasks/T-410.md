# T-410: S8 운영 화면 + S1 주간 타임라인 브러시 — 파이프라인 실행 목록·단계·게이트·해시, C 평가 결과, 데이터·모델 최신성
- 레인/워커: L4b / gpt-6-sol        - 선행: T-104(파이프라인 실행 기록)·T-308(C 평가)·T-404 완료. forecast `/v1/runs`·`/v1/ops/freshness`는 **T-106(L1)이 병행 구현** — 이 task는 계약·픽스처로 만든다.
- 목표(한 문장): 자리만 있는 `/ops`를 실제 운영 화면으로 만들고(M8-F1~F3), S1 주간 타임라인에 **브러시로 기간을 골라 필터**하는 기능(M1-F4-b P1)을 더한다.
- 읽을 것: docs/plan/11 M8·M1-F4(**정본**)·화면 구분 원칙, docs/plan/04 §6(차트 규칙)·§3·§13, packages/contracts/{schemas/{pipeline-run,ops-status}.schema.json, fixtures/{pipeline-run,ops-status}/, openapi/gateway.yaml(`/api/ops/runs`·`/api/ops/status`)}, reports/evals/(T-308 결과 모양), apps/web/src/{pages/ops-page.tsx, features/kpi-timeline/weekly-timeline.tsx, lib/{api-client,selection-store}.ts, features/validation/(표·카드 스타일 재사용)}, AGENTS.md §3·§5
- 허용 경로: `apps/web/src/pages/ops-page.tsx`, `apps/web/src/features/ops/`(새), `apps/web/src/features/kpi-timeline/`, `apps/web/src/lib/`(ops API 읽기 함수·필터 기간 상태 — 기존 필터 스토어에 기간이 있으면 그것을 쓴다), `apps/web/src/styles/`(ops·타임라인 부분), `tests/e2e/`(새 `s8.spec.ts`, `s1-panels.spec.ts`에 브러시 한 흐름)
- 인터페이스:
  - **M8-F1 실행 목록**: `GET /api/ops/runs`(계약 검증) → 표(시작·끝·상태·요약 한 줄) 최신순, 행 펼치기 → 단계(fetch·labels·features·train·backtest·batch·publish)별 상태·게이트 결과·산출물 해시(앞 12자 + 복사)·걸린 시간, **실패 단계 강조**(아이콘 + 글자 + 토큰 색). 실행이 없으면 펫 빈 상태 + "파이프라인을 한 번 돌리면 여기에 쌓여요"(명령 `crowdcast pipeline run` 표시).
  - **M8-F2 평가 결과**: `GET /api/ops/status`의 `evals` — 추출 평가·시나리오 평가 합계(근거 없는 발행 0, 숫자 불일치 0 등 항목별 통과/전체), 지연 p50·p95, 결과 파일 날짜·"참고용 — 평가 시나리오는 개발팀이 만든 것".
  - **M8-F3 최신성**: `freshness`(자료별 마지막 수집일·반영 지연 일수 — 지연 규칙 35일 넘으면 경고), `model`(사용 모델 버전·검증 상태·학습 범위 — 예보서와 같은 버전인지), `graph`(근거 그래프 크기).
  - 상류 503·계약 불일치는 카드별 오류 상태(한 카드 실패가 다른 카드를 숨기지 않는다 — T-407b S7 방식).
  - **M1-F4-b 브러시**: 주간 타임라인에서 끌어 기간 선택(키보드: 시작·끝 핸들 방향키) → 필터 기간이 바뀌어 판·목록·KPI가 함께 바뀜, 선택 해제 버튼, 필터 패널의 기간과 양방향.
- 수용 기준: `npm -w apps/web test`(실행 표·단계 펼침·실패 강조·빈 상태·카드별 오류, 브러시 → 필터 기간·키보드)·build·lint·색 하드코딩 0, e2e `s8.spec.ts`(가짜 API — 계약 픽스처: 실행 펼침·해시 복사·상류 오류 한 카드만) + 브러시 흐름, 스크린샷 `reports/figures/screens/T-410-{ops,brush}.png`. 실행은 오케스트레이터.
- 범위 밖: forecast `/v1/runs` 구현(T-106), Dagster 화면(T-108)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
