# T-433b: S1 다듬기(R-07) — 판이 패널에 가리지 않게, 카메라 키 충돌, 데이터 모드 범례, 제목
- 레인/워커: L4c / gpt-6-sol        - 선행: T-433 완료(develop 0bfe5d4), R-07(PASS — Med·Low 반영)
- 목표(한 문장): R-07이 짚은 S1의 장면 쪽 문제 — 첫 화면에서 전국 판 오른쪽·아래가 목록·KPI에 가림, 목록 ↑/↓가 카메라까지 움직임, 데이터 모드 색이 인원 구간을 말해 주지 않음, 구현 중심 제목 — 를 고친다.
- 읽을 것: `.harness/reports/R-07.md`(**지적 전부**), docs/plan/04 §4(떠 있는 패널이 장면 핵심을 가리지 않게)·§9(9/25 프레임 합격선)·§13, docs/plan/11 M1, apps/web/src/{pages/mini-korea-page.tsx, components/scene/{camera-rig.tsx,scene-legend.tsx,tag-visibility.ts}, features/mini-korea/}, AGENTS.md §3·§5
- 허용 경로: `apps/web/src/components/scene/`, `apps/web/src/features/mini-korea/`, `apps/web/src/pages/mini-korea-page.tsx`, `tests/e2e/s1.spec.ts`
- 인터페이스:
  1. **프레이밍**: 첫 화면·"전국 보기" 버튼(새, 장면 모서리)에서 카메라가 **떠 있는 패널을 뺀 영역**(이름표 안전 영역과 같은 패널 경계 — `ResizeObserver` 캐시 재사용)에 전국 판 전체가 들어오게 맞춘다(1440×900·1280×800). 선택으로 다가간 뒤 "전국 보기"로 돌아온다. 목록 접기(T-407b)로 패널이 줄면 다시 맞춘다.
  2. **카메라 키**: 방향키 카메라 이동은 **장면(캔버스 컨테이너)에 포커스가 있을 때만**, 이미 처리된 이벤트(`defaultPrevented`)와 입력·버튼·목록 안의 키는 건너뛴다.
  3. **데이터 모드 범례**: 예보 없는 시군구는 `--seq-*`가 아닌 기본 타일색으로 분리해 "예보 없음"으로 표시, 5단계 구간 경계를 인원(명, 천 단위 쉼표)으로 범례에 적고 "지금 필터 기준으로 다시 나눔"을 한 줄로. 구간 경계는 결정적(같은 필터 = 같은 경계).
  4. **제목**: "KPI 띠·타임라인" → "행사 현황"(M1-F4 설명은 그대로), 그 밖 구현 용어 제목이 있으면 사용자 말로. 견본 이름("견본 행사 N")은 실제 행사로 오인되지 않게 그대로 둔다(오케스트레이터 판정 — R-07 Low의 실제 행사명 제안은 받지 않는다).
  5. **(Low) 필터 하단**: 1280×800에서 "적용 N개 · 모두 초기화" 줄을 왼쪽 패널 안에서 고정(스크롤해도 보이게).
- 수용 기준: `npm -w apps/web test`(카메라 키 필터·범례 구간 계산 단위 테스트)·`build`·`lint`·색 하드코딩 0. e2e(`s1.spec.ts` 보강): 첫 화면에서 판 경계가 패널과 겹치지 않음(투영한 판 네 모서리 ⊂ 패널 밖 영역), 목록 포커스에서 ↓ → 카메라 target 그대로, 장면 포커스에서 → → target 이동, 데이터 모드 범례에 "예보 없음"과 구간 5개. 스크린샷 `T-433b-s1-{day,laptop,data}.png`. 실행은 오케스트레이터(워커는 작성·구문 확인). 성능: `--t433` 비율이 04 §9 자동 기준(p95 ≤ 1.3배) 안.
- 범위 밖: 목록 접기 버튼 자체(T-407b), SVG 지도(T-407b), S6·S7
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5

## 이어 하기 (9/25 13:30 사용자 요청으로 중단됐던 실행)
이전 실행이 도중에 멈췄다. **워크트리에 남은 변경을 먼저 읽고 이어서** 완성한다(처음부터 다시 쓰지 않는다 — 맞지 않는 부분만 고친다). 명세·수용 기준은 위 그대로.
