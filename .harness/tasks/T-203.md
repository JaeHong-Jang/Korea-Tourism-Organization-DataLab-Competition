# T-203: 모델·백테스트 v1 — 공개 시점 피처, LightGBM 분위수 + MAPIE CQR, 롤링 백테스트, 기준선 B0·B1·B2, 모델 카드
- 레인/워커: L2 / gpt-6-astra        - 선행: T-103(라벨)·T-202(판정·순간 최대 환산) 완료
- 목표(한 문장): 행사 개최 D-14에 공개된 정보만으로 일평균 방문객 p10·p50·p90을 예측하는 모델을 만들고, 연도별 롤링 백테스트 성적(좋든 나쁘든 그대로)과 모델 카드를 계약 모양으로 남겨 T-204(API)·서식4·S6이 같은 숫자를 쓰게 한다.
- 읽을 것: docs/plan/06 §2(라벨·코로나)·§3(피처·공개 시점·누수 금지)·§5(모델 전체)·§8(train·backtest 게이트)·§9(롤링 백테스트·골든), packages/contracts/schemas/{backtest-summary,model-card,factor,common}.schema.json, services/forecast/src/crowdcast/rules/{peak,judge}.py(T-202 — **호출만**, 순간 최대·판정을 다시 만들지 않는다), configs/*.yaml, `.harness/backlog.md` "T-207·T-203" 절, AGENTS.md §3·§5
- 입력(읽기 전용): `paths.PROCESSED/{labels,events,region_daily}.parquet`. 지금 라벨은 실버 위주(2023-03~2025 + 골드A 1개 축제) — 9/26~27 벌크로 2018·2019·2021·2022가, H2·H3로 골드가 늘어난다 → **같은 명령으로 다시 돌린다**(v2 = T-203b)
- 허용 경로: `services/forecast/src/crowdcast/features/`(`availability.py`·`event_features.py`·`region_features.py`·`history_features.py`·`calendar_features.py`·`build.py`), `services/forecast/src/crowdcast/models/`(`compat.py`·`train.py`·`calibrate.py`·`distribution.py`·`baselines.py`·`backtest.py`·`explain.py`·`ood.py`·`card.py`·`__main__.py`), `services/forecast/tests/{features,models}/`, `configs/model.yaml`(새 파일)
- 실행 산출물(git 제외): `models/<modelVersion>/`(분위수 모델 3개·보정값·피처 목록), `models/model_card.json`, `reports/backtest/<runId>/{backtest.json,backtest.md,points.parquet}`
- 인터페이스:
  - **호환성 먼저**(`compat.py` + 테스트): LightGBM 4.6 quantile(α 0.1·0.5·0.9)이 합성 자료에서 학습·저장·불러오기 되고, `monotone_constraints`를 주면 거부하는 우리 쪽 가드(06 §5 — 4.6은 중단된다). 이 테스트가 통과해야 나머지를 돌린다
  - **피처**(06 §3): 행사(유형·기간·주말·공휴일 일수·시간대·무료·주최 유형·회차·log 예산(결측 허용)·위험 요소), 이력(전회차 라벨 — `available_at ≤ as_of`인 것만), 개최지(`region_daily`에서 as_of 이전 8주 일평균·외지인 비중·주말 배수 — `available_at ≤ as_of`인 날만), 달력(월·연휴 길이). `as_of = 시작일 − 14`. 피처 값마다 `available_at`을 함께 두고 **`availability.py`가 위반 1건이라도 있으면 예외로 멈춘다**(06 §8 features 게이트). 없는 자료(TMAP 등)는 넣지 않는다
  - **학습 표본**: `usable_for_training & is_primary & !is_golden`. 실버 가중치는 `configs/model.yaml`(기본 0.5, 골드 1.0). 코로나(2020·2021)는 기본 제외(설정), 뺀 건수 기록
  - **모델**: 대상 log1p(일평균), num_leaves ≤ 15, min_data_in_leaf ≥ 10(설정), 시드 고정. 구간 보정은 MAPIE CQR(평가 연도 Y마다 **학습 ≤ Y−2, 보정 Y−1, 평가 Y**). p10 ≤ p50 ≤ p90 정렬(교차 제거)
  - **분포·순간 최대·판정**: 보정된 세 분위수 → `rules/peak.py`의 `sample_peak`(T-202) → `rules/judge.py`. `distribution.py`는 두 모듈을 잇는 얇은 층만(샘플 수·시드 고정). 단위테스트: 순서, `p50 > 1000`이면 P(≥1000) ≥ 50%
  - **기준선**(06 §5): B0 유형 중앙값(학습 구간만), B1 전회차 실측(공개 시점 규칙), B2 주최측 **사전 예상** — `events`에 단위가 일평균과 같은 사전 예상이 있는 쌍만(없으면 `comparablePairs = 0`으로 정직하게)
  - **백테스트**(`python -m crowdcast.models backtest`): 평가 연도는 `configs/model.yaml`(기본 2024·2025, 학습 연도가 모자라면 그 해는 건너뛰고 사유 기록). 지표 = 계약 `backtest-summary.metrics`(MdAPE·80% 포함률·포함 표본 수·환산 판정 재현율·정밀도 — 실측 일평균도 같은 환산으로 등급을 만들어 비교·기준선 대비 %p·비교 가능 쌍). 등급별(골드/실버)·규모대별(일평균 <1,000 / 1,000~5,000 / >5,000)·연도별 표를 `backtest.md`에. `golden` = 골든 행사 재현(없으면 빈 배열 + 사유)
  - **설명**: SHAP 상위 5개 → 계약 `factor`(evidenceIds는 빈 배열 — T-204가 근거 id를 붙인다). **OOD**: 유형×규모대 학습 표본 < 5 또는 피처가 학습 범위 밖
  - **모델 카드**: 계약 `model-card`(id = `mr-` + modelVersion, trainRange, features, evalYears, backtestRunId, notes에 라벨 파일 SHA-256·실버 가중치·코로나 제외 여부·"목표 포함률 80%(보장 아님)")
  - **결정성**: 같은 입력·시드 → `backtest.json` 지표 같음(테스트)
- **G0 분기(06 §11 · 06 §8 뒤 9/25 메모 — 정본)**: 지금 정의 일치 골드 5건(< 30), 실버 학습 가능 250건. v1은 (a) **단순 모델**(유형×규모대 계층 중앙값(학습 구간) + 전회차 실측이 있으면 그것, 구간 = 같은 계층 학습 잔차 분위수)과 (b) **LightGBM**(실버 가중 + 골드)을 **둘 다** 같은 롤링 백테스트·같은 지표로 `backtest.md`에 나란히 보고한다.
  - **주 모델·판정 방식은 평가 전에 규칙으로 고정**: `models/g0.py`가 라벨 QC 수치(대표·학습 가능 골드 고유 행사 수, 평가 연도별 수, 환산 기준선(1,000명) 아래·위 각 수)를 읽어 세 갈래로 정한다 — ① 골드 ≥ 60 · 평가 연도마다 ≥ 15 · 기준선 양쪽 각 ≥ 10 → LightGBM + `basis=확률`, ② 골드 < 30 → 단순 모델 + `basis=구간`, ③ 그 외(일부 충족) → LightGBM + 실버 보조, 기준선 한쪽이라도 < 10이면 `basis=구간` 아니면 `확률`. 결과(갈래·근거 수치·라벨 파일 SHA-256)를 `configs/model.yaml`이 아니라 **백테스트 실행 전에** `models/<modelVersion>/g0.json`으로 쓰고, 백테스트는 그 파일을 읽기만 한다(성적을 보고 바꾸는 경로 없음 — 테스트로 보장). H2 뒤 재판정은 T-203b 새 버전.
  - **구간 표시**(06 §6 — 정본): 등급은 **판정 함수 하나**(`rules/judge.py`, 같은 샘플의 확률)로만 정하고, `basis` 인자는 **표시만** 바꾼다 — `judgment.basis="구간"`, 문구에 % 없음·"표본 한계로 구간 기준 표시"·순간 최대 p10~p90. `peak.py`의 분위수 계산은 건드리지 않는다. 백테스트 판정 지표는 등급 하나로(방식에 따라 달라지지 않음).
  - 실버 `holiday_overlap`·명절 행사는 학습·실버 채점에서 빼고 "채점 불가" 건수로 공개(06 메모). 판정·표본 한계 문장을 모델 카드 `notes`에(서식4에 그대로 쓴다).
  - 허용 경로 추가: `services/forecast/src/crowdcast/rules/judge.py`(basis 분기만), `configs/thresholds.yaml`(구간 판정 문구), `services/forecast/tests/rules/`(구간 판정 테스트)
- 수용 기준(실행 명령):
  - `uv run --package crowdcast-forecast pytest -q services/forecast/tests/features services/forecast/tests/models services/forecast/tests/rules` 통과(**구간 표시**: 같은 샘플에서 basis만 바꿔도 등급·사유가 같음(0명 2,000개 + 1,000명 2,000개 같은 경계 샘플 포함), 인원 무관 조건 우선순위, `basis` 직렬화, 구간 문구에 `%` 없음, **G0**: 세 갈래 경계 — 골드 29/30·59/60·연도별 14/15·기준선 한쪽 9/10, `g0.json`이 백테스트 전에 쓰이고 백테스트가 바꾸지 않음): 호환성, **누수 테스트**(as_of 뒤에 공개된 값을 넣은 합성 행 → 멈춤), 공개 시점 경계(`available_at == as_of`는 허용), CQR 분할(학습 ≤ Y−2·보정 Y−1 — 평가 연도 행이 학습에 없음), 기준선 B2 단위 불일치 제외, 분포 순서·확률 일관성, 결정성
  - 실제 실행: `python -m crowdcast.models backtest` → `backtest.json`·`model_card.json`이 계약 스키마 통과(`packages/contracts/check/python_check.py`의 validate 사용 — 방법을 리포트에), 전체 실행 10분 이내(노트북)
  - 리포트에 **숫자 그대로**: 연도별 N·MdAPE·포함률(표본 수)·구간 폭 중앙값·기준선 대비, 판정 재현율·정밀도, 건너뛴 연도와 사유, 산출물 SHA-256. 성적이 나빠도 고치려고 평가 자료를 보지 않는다(튜닝은 학습·보정 구간 안에서만)
- 범위 밖: 예측 API(T-204), 일괄 예보(T-205), PyMC 도전 모델(T-211), D-7 날씨 보정(P1), 파이프라인 게이트 CLI(T-104), 라벨 수정(T-103 — 라벨 문제를 발견하면 리포트에 적고 멈추지 않는다)
- 외부 호출: 없음(패키지 설치만)
- 코드 규칙: AGENTS.md §5 (파일 하나 = 책임 하나, 300줄 넘으면 나눈다)

## 게이트 피드백 1회차 (게이트 3 sol FAIL + 오케스트레이터 게이트 2·4) — 워크트리에 결과가 있다
오케스트레이터 게이트 2: features·models·rules 240 통과, 백테스트 재실행 바이트 동일, 계약 통과. 백테스트 산출물은 이제 본 레포 `reports/backtest/`로 공유된다(워크트리 링크 — 옮겨 두었다). 결과: 2025 단순 MdAPE 44.6%·포함률 72%(62/86), LightGBM 46.7%·68.6%, 2024는 학습 2건으로 건너뜀(9/26~27 벌크로 2022년이 들어오면 늘어난다 — T-203b).
1. [High · 명세 누락 정정] `features/event_features.py:30` — 행사 자체 속성(유형·기간·주말·공휴일 일수·시간대·요금·주최 유형·회차·예산·위험 요소)을 "행사 공개일 없음"으로 **결측 처리**했다. 이 값들은 **예보를 요청할 때 주어지는 입력**(예보 대상의 정의 — T-204 명세 "행사 입력 자체는 관측이 아님"과 같다)이라 공개 시점 제한 대상이 아니다. **공개 시점 규칙은 외부 관측(`region_daily`에서 온 피처·전회차 라벨)에만** 건다. 이 속성들을 정상 피처로 쓰고, B0 유형 중앙값도 유형별로(전체 중앙값 대체 제거). 전·후 성적을 `backtest.md`에 나란히. (명세에 이 문장이 없었던 것은 오케스트레이터 잘못이다.)
2. [Med] `rules/judge.py:106` — `basis="구간"`에서도 근거 문장(reasons text)에 확률 `%`가 남는다 → 구간 표시 규칙을 사유 문장까지(순간 최대 p10~p90·"표본 한계로 구간 기준 표시"), 테스트: 구간 방식 전체 문자열에 `%` 없음.
3. [Med] `models/__main__.py:71` — G0 판정이 `labels.parquet` 해시만 보고 `events.parquet`·`labels_g0.json` 변경을 모른다 → `g0.json`에 세 파일(`labels.parquet`·`labels_g0.json`·`events.parquet`) SHA-256을 기록하고, 하나라도 현재와 다르면 **"입력이 바뀌었다 — T-103부터 다시"**로 중단(G0를 조용히 재사용하지 않는다). 테스트.
4. [Low] `tests/models/test_g0.py` — `tests/labels/test_g0.py`와 모듈 이름이 겹쳐 **forecast 전체 pytest가 수집 오류**(오케스트레이터 재현) → `test_model_g0.py`로.
5. [T-104 연계 — 파이프라인이 읽는다] (a) 피처 빌더가 `paths.PROCESSED/features_availability.json` `{checked, violations, asOfRule}`를 쓴다(백테스트 실행마다 덮어씀), (b) 백테스트가 실행마다 `reports/backtest/latest.json` `{runId, modelVersion, finishedAt}`을 쓴다(같은 입력이면 runId는 같아도 finishedAt은 새로 — 파이프라인이 "이번 실행 결과"를 이 파일로 식별).
- 확인: `pytest -q services/forecast/tests`(**전체** — 수집 오류 없이), 실제 `python -m crowdcast.models backtest` → 단순·LightGBM 성적(1번 전·후), G0 입력 해시, 두 연계 파일 — 리포트에.
