# T-411a: 완성도 1차 — S2·S3(탭 3개)·S4·S6·S7·S8·2D 지도 겹침: 반응형·키보드·접근성(axe)·모션·빈/오류 상태 통일
- 레인/워커: L4b / gpt-6-sol        - 선행: T-405·T-406·T-407·T-408·T-410·T-413·T-434·T-403 완료, 의존성 40f495a(`@axe-core/playwright`)
- 목표(한 문장): 이미 합쳐진 화면들을 **처음 쓰는 사람이 설명 없이 끝까지 가게**(11 화면 구분 원칙) 다듬는다 — 네 가지 폭에서 깨지지 않고, 키보드만으로 쓸 수 있고, axe 심각·치명 위반 0, 모션은 토큰대로·감소 설정 존중. **S1 장면·헤더(T-435 중)와 S5(T-409 중)는 건드리지 않는다.**
- 읽을 것: docs/plan/11 화면 구분 원칙 1~5(**정본**)·각 메뉴 표, docs/plan/04 §4(레이아웃)·§5(컴포넌트)·§10(모션)·§11(문구)·§12(접근성·인쇄)·§13(AI 티 금지)·§14(디자인 QA), apps/web/src/{pages/{consult,forecast,plan,validation,insights,ops}-page.tsx, features/{consult-chat,team-board,forecast-report,evidence,evidence-map,venue-3d(탭 틀만),safety-plan,validation,insights,ops,map-2d}/, components/{ui,common}/, styles/}, `reports/figures/screens/`의 최근 스크린샷(T-403-day·T-408·T-410·T-413), AGENTS.md §3·§5
- 허용 경로: 위 페이지·기능 폴더, `apps/web/src/components/{ui,common}/`, `apps/web/src/styles/`(단 `components/scene/`·`app/` 헤더·`features/my-events/`·`pages/my-events-page.tsx`·`pages/mini-korea-page.tsx`는 제외), `tests/e2e/`(새 `a11y.spec.ts`, 기존 스펙 보강)
- 할 것:
  1. **반응형**: 1440×900·1280×800·768×1024·390×844에서 가로 스크롤 0, 겹침 0(특히 2D 지도의 "13단계" 안내가 확대 버튼과 겹침, 왼쪽 아래 패널이 지도 안내를 가림 — `T-403-day.png`), 표는 좁은 폭에서 카드/가로 스크롤 영역으로.
  2. **키보드·포커스**: 모든 조작 Tab 순서 자연스럽게, `:focus-visible` 토큰 링, 탭(예보서 3개)·목차·표 행 펼치기·필터 칩 방향키/Enter/Space, 모달·서랍 포커스 가둠과 복귀.
  3. **접근성**: `@axe-core/playwright`로 각 화면(S2 첫 화면·스트림 끝, S3 세 탭, S4, S6, S7, S8) 심각·치명 위반 0(색 대비·이름 없는 버튼·표 머리글·landmark·aria), 숫자 옆 단위·출처 칩(원칙 5), 등급은 아이콘 + 글자.
  4. **모션**: 전환·등장은 04 §10 토큰 시간·이징만, `prefers-reduced-motion`이면 전환 없음.
  5. **상태 통일**: 빈·로딩·오류는 펫 + 한 줄 + 할 일 버튼(G-F4) — 화면마다 다른 문구·모양 정리, 개발자 말투("계약", "null", "API") 노출 금지.
  6. **문구**: 메뉴 이름 = 화면 제목, 버튼은 동사, 주요 행동 버튼 하나만 채움(원칙 2).
- 수용 기준: `npm -w apps/web test`·build·lint·색 하드코딩 0, e2e `a11y.spec.ts`(화면별 axe 심각·치명 0, 네 폭 가로 스크롤 0 — `document.documentElement.scrollWidth <= innerWidth`) + 기존 스펙 통과, 스크린샷 `reports/figures/screens/T-411a-{s2,s3,s4,s6,s7,s8}-{1440,390}.png`. 실행은 오케스트레이터.
- 범위 밖: S1 장면·헤더(T-435), S5(T-409), 3D 성능(T-436), 새 기능
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
