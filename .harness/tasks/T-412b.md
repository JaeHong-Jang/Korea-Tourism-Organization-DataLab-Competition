# T-412b: E2E·스크린샷 v2 — 모든 메뉴 × 세 해상도 × 낮·밤 한 장씩(서식4·PT·R-10 검토용)
- 레인/워커: L4b / gpt-6-sol        - 선행: 2주차 웹 전부 병합(develop 7068579 이후), 기존 e2e 88개 통과
- 목표(한 문장): 검토·서식4·발표에 쓸 **최신 화면 한 벌**을 결정적으로 다시 만들 수 있게, 가짜 API(계약 픽스처)로 모든 메뉴·주요 상태를 세 해상도 × 낮·밤으로 찍는 스펙 하나를 만든다.
- 읽을 것: docs/plan/11(메뉴 M1~M8·G), docs/plan/08(서식4 매핑 — 어떤 화면이 필요한지), tests/e2e/(기존 스펙의 가짜 API·픽스처 라우팅 방식 — 재사용), apps/web/playwright.config.ts(`CROWDCAST_GATEWAY_URL` 닫힌 포트), AGENTS.md §3·§5
- 허용 경로: `tests/e2e/screens-v2.spec.ts`(새), `tests/e2e/fixtures/`(공용 라우팅 도우미가 필요하면 새 파일), `reports/figures/screens/v2/README.md`(목록 — 파일 이름·화면·상태)
- 인터페이스:
  - 해상도 `1440x900`·`1280x800`·`390x844`, 테마 `day`·`night`(주소 `?theme=`), 고정 시각(`&at=2026-10-18T12:00+09:00` 낮, `…T21:00` 밤).
  - 화면·상태: S1(3D·2D·SVG·행사 선택·데이터 모드), S2(첫 화면·스트림 끝·what-if 비교), S3(예보서·근거 서랍·근거 지도·행사장 3D 견본), S4(편집기), S5(목록·재예보 카드·공유 화면), S6, S7, S8, 빈·오류 상태 대표 1장씩.
  - 저장: `reports/figures/screens/v2/<화면>-<상태>-<해상도>-<테마>.png`, `reduced-motion`·장면 준비(`data-scene-ready`) 대기로 **같은 입력 = 같은 화면**, 스크린샷 전 폰트 준비 대기.
  - 한 화면이 실패해도 나머지는 찍히게(스텝별 `test`), 실행 시간이 길면 `--grep`으로 나눠 돌릴 수 있게 태그.
- 수용 기준: 스펙 목록 확인(`npx playwright test screens-v2 --list`)·lint, README 목록. **실행·이미지 확인은 오케스트레이터**.
- 범위 밖: 앱 코드 변경(찍다가 발견한 문제는 리포트에 목록으로)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
