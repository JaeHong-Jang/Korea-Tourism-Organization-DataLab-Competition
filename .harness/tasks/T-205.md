# T-205: 다가오는 행사 일괄 예보 + `/v1/festivals/upcoming` + `/v1/regions/topojson`
- 레인/워커: L2 / gpt-6-astra        - 선행: T-204 완료
- 목표(한 문장): 행사 마스터의 다가오는 행사(9/29~11/30 시작)를 `/predict`와 **같은 조립 코드**로 한꺼번에 예보해 지도(S1)·목록·사전 등록(T-207)이 쓰는 요약 표를 만든다.
- 읽을 것: docs/plan/06 §8(batch 게이트)·§9, packages/contracts/openapi/forecast.yaml(`/v1/festivals/upcoming`·`/v1/regions/topojson`), packages/contracts/schemas/festival-summary.schema.json, services/forecast/src/crowdcast/api/assemble/(T-204 — 호출만), services/forecast/src/crowdcast/data/events.py의 `contract_event()`(T-102 — 계약 v1.6: 시간대 미상 = `미상`), AGENTS.md §3·§5
- 허용 경로: `services/forecast/src/crowdcast/analytics/upcoming.py`(+ `__main__` 진입은 `python -m crowdcast.analytics.upcoming`), `services/forecast/src/crowdcast/api/routes/{upcoming,regions}.py`, `services/forecast/src/crowdcast/api/app.py`(등록 줄만), `services/forecast/tests/{analytics,api}/`
- 실행 산출물(git 제외): `paths.PROCESSED/upcoming.parquet`(요약), `paths.PROCESSED/upcoming_forecasts.jsonl`(전체 `forecast` — forecastId당 한 줄), `paths.PROCESSED/upcoming_qc.md`
- 인터페이스:
  - `python -m crowdcast.analytics.upcoming [--from 2026-09-29] [--to 2026-11-30]`: `events.parquet`에서 시작일이 범위 안인 행 → `contract_event()` → T-204 조립(`asOf` 규칙 그대로). 바뀌지 않는 행(날짜·시군구·좌표 없음)은 사유별로 세고 넘어간다. OOD도 예보하되 `ood=true`. 같은 입력이면 같은 forecastId·같은 숫자
  - 요약 = 계약 `festival-summary`(peak 정수 반올림 규칙은 T-202 표시 규칙 그대로, `pOver1000`은 확률 원값)
  - `GET /v1/festivals/upcoming?from&to` → `upcoming.parquet`에서 범위 필터, **등급 내림차순 → pOver1000 내림차순 → 시작일** 정렬, 응답 계약 검증. 파일이 없으면 503(빈 배열로 속이지 않는다)
  - `GET /v1/regions/topojson` → `paths.EXTERNAL/boundaries/sigungu.topo.json` 그대로(`Cache-Control`·출처 헤더 `CC BY 4.0 admdongkor`)
  - batch 게이트(06 §8): 예보 수가 직전 실행의 90% 미만이면 QC 맨 위에 경고(멈추지 않음). 직전 실행 수는 QC 파일에서 읽는다
- 수용 기준(실행 명령):
  - `uv run --package crowdcast-forecast pytest -q services/forecast/tests/analytics services/forecast/tests/api` 통과: 범위 경계(시작일 = from·to 포함), 변환 불가 사유 집계, `/predict`와 같은 결과(같은 행사 1건을 두 경로로 → forecast 바이트 같음), 정렬, 파일 없음 503, 결정성
  - 실제 실행: QC에 입력 가능 수·예보 수·OOD 수·등급별 수·사유별 제외 수, 전체 실행 시간(목표 5분 이내)
  - `node scripts/dev.mjs --check --only forecast`에 `✓ forecast`
- 범위 밖: 사전 등록 선정(T-207), 지도 화면(T-433), 인사이트(T-206)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5
