# T-305: 팀장 요청 분류 + 후속 플레이북 — "왜?"·"저장"·"초안"·범위 밖을 같은 세션에서
- 레인/워커: L3 / gpt-6-astra        - 선행: T-304 완료(새 예보 플레이북·게이트 B·발행 — develop a352073)
- 목표(한 문장): 발행이 끝난 상담에서 두 번째 메시지부터 팀장이 요청을 분류해 **후속 플레이북**(왜 → 설명 문장 게이트 B → 발행 / 저장 → records / 초안 → 계획서 자리 / 범위 밖 → 안내)으로 보내고, 모든 후속 스트림이 계약의 `followup` 순서 규칙을 지킨다.
- 읽을 것: docs/plan/10 §1(팀장)·§3 "다른 플레이북"·§6, packages/contracts/{sse-events.md(R1~R11, followup), rules/sse-sequence.mjs(`ctx.mode="followup"`), fixtures-sse/{valid-followup-why,invalid-followup-*}.json, schemas/{claim,evidence,forecast-report}.schema.json, openapi/{gateway,records}.yaml}, services/gateway/src/team/{lead,report,verification,runtime}/(T-304), services/gateway/src/clients/records-client.ts, AGENTS.md §3·§5, `.harness/tasks/T-304.md` 끝의 오케스트레이터 판정(요인 문장은 라벨 그대로만)
- 허용 경로: `services/gateway/src/team/lead/`(새 `classify.ts`·`followups.ts`, `playbooks.ts`·`publish.ts`의 재사용 부분만), `services/gateway/src/team/report/`(새 `why-templates.ts`), `services/gateway/src/team/runtime/{sessions,run-request}.ts`(발행 묶음 보관·분류 분기 최소 수정), `services/gateway/src/team/analysis/{as-of,local-guide,forecaster}.ts`(요청 시각 고정만), `services/gateway/src/llm/`(분류 프롬프트·스키마 — 기존 클라이언트 재사용), `services/gateway/tests/`, `services/gateway/fixtures/`
- 인터페이스:
  - **분류(`classify.ts`)**: 세션에 발행된 예보가 있을 때(`session.completed`)의 메시지만 분류한다(되묻기 답·첫 메시지는 지금처럼 새 예보). 의도 enum = `why`·`save`·`draft`·`whatif`·`new_event`·`out_of_scope`. **키워드 규칙이 먼저**(예: 왜·이유·근거 → why / 저장·보관 → save / 초안·계획서·계획 → draft / 비 오면·요일·바꾸면 → whatif) — 규칙이 한 의도만 가리키면 LLM을 부르지 않는다(결정성·지연). 규칙이 없거나 둘 이상이면 Ollama 스키마 강제 enum 1회(예산 3초) → 실패·시간 초과는 `out_of_scope`. 분류 결과는 lead의 agent_step `note`에 남긴다(원문 없이 의도와 "규칙"/"LLM").
  - **why**: 발행된 예보 묶음(세션에 보관 — `sessions.ts`에 발행 묶음 필드 추가)으로 **템플릿 설명 문장**만 만든다(LLM 없음 — 요인 문장은 라벨 그대로라 풀어 쓰지 않는다). 문장: 요인 라벨(있으면, 상위 3), "비슷한 과거 행사의 실측을 근거로 삼았어요"(case 근거가 있으면 그 id), "개최 지역의 평시 방문 자료를 함께 봤어요"(평시 data 근거), "예측 모델이 추정한 범위를 따랐어요"(model 근거), OOD면 참고용 고지·미검증이면 모델 고지(T-304와 같은 문장·근거). **숫자 0개**, claimType 요인/설명, 같은 검증팀 → 게이트 B → 발행(knowledge `/publish`) → `claim`·`evidence`·`suggest`·`done{forecastId=기존 예보 id}`. 게이트 B가 (재작성 없이 템플릿뿐이므로) 막으면 발행 없이 `error(SERVICE_UNAVAILABLE, "설명 문장을 검증하지 못했어요")` → `done{forecastId=기존 예보 id}`(followup에서는 `done.forecastId`가 항상 기존 id — sse-sequence R7이 판정).
  - **save**: 발행된 예보의 records 스냅샷이 있으면(`getSnapshot`) 그대로, 없으면(T-304에서 저장 실패) `saveEvent`+`saveSnapshot` 재시도(예산 5초). 결과는 lead `agent_status`/`agent_step` note("예보서를 저장했어요" / "저장하지 못했어요 — 잠시 뒤 다시 눌러 주세요"). 발행 전이면 `error(OUT_OF_SCOPE, "먼저 예보를 받아야 저장할 수 있어요")`.
  - **draft**: 계획서 에이전트(T-306)가 들어올 자리 — `followups.ts`의 `planDraft` 한 곳으로 보내고, 이번 task에서는 `error(OUT_OF_SCOPE, "계획 초안은 계획서 담당이 곧 맡아요")`(T-306이 이 함수만 바꾼다).
  - **whatif**(P1, T-309): `error(OUT_OF_SCOPE, "비·요일을 바꿔 보는 기능은 준비 중이에요")`.
  - **new_event**: 지금처럼 "새 예보는 새 상담에서 시작해 주세요". **out_of_scope**: 할 수 있는 일(왜·저장·초안)을 안내하는 템플릿.
  - **`done.forecastId`**: 지금 `run-request.ts`는 이번 요청에서 forecastId가 바뀌었을 때만 id를 싣는다 → 후속 요청은 항상 기존 예보 id를 싣게 고친다(새 예보 경로는 그대로).
  - **요청 시각 고정(T-304 리뷰 Med)**: 요청 시작 때 KST 오늘을 한 번 정해 동네지기·예보관의 기준일 계산에 넘긴다(자정을 넘는 상담에서 두 팀원이 다른 기준일을 쓰지 않게).
- 수용 기준(실행 명령):
  - `npm -w services/gateway test`·`build`·`lint` 통과, `node scripts/dev.mjs --check --only gateway`에 `✓ gateway`
  - **시나리오 테스트 8개 이상**(가짜 LLM·가짜 서비스, 실제 스트림을 `sequenceProblems(events, {mode:"followup", forecastId})`에): ① 발행 뒤 "왜 이렇게 많아?" → 규칙 분류(LLM 0회) → 설명 문장 발행·`done.forecastId`=기존 id ② 같은 질문을 두 번 → 두 번 다 규칙 통과(같은 근거 — 재발행 규칙 확인) ③ "저장해 줘"(스냅샷 있음) → 저장 확인, records 쓰기 0회 ④ "저장해 줘"(스냅샷 없음) → 재저장 1회 ⑤ 발행 전 "저장해 줘" → 안내 ⑥ "계획 초안 만들어 줘" → draft 자리 안내 ⑦ "내일 날씨 어때?" → 범위 밖 안내 ⑧ 규칙이 모호한 문장 → LLM 분류 1회 / LLM 오류 → 범위 밖 ⑨ 요청 시각 고정(자정 전후 가짜 시계) ⑩ 기존 T-303·T-304 스트림 테스트 전부
  - why 발행 문장 100%가 근거 1개 이상·숫자 0개·게이트 B 통과
- 범위 밖: 계획서 에이전트(T-306), what-if(T-309), 웹 화면 버튼(L4a), 분류 품질 평가(T-308 C 평가)
- 외부 호출: 로컬 Ollama만
- 코드 규칙: AGENTS.md §5 (팀원 하나 = 파일 하나, 파일 맨 위 역할 한 줄 + 문단마다 한국어 한 줄 주석, 300줄 넘으면 분리)
- 실사용 확인은 오케스트레이터가 한다(워커 샌드박스는 소켓을 막는다) — 리포트에는 재현 명령만.

## 게이트 피드백 1회차 (게이트 3 sol FAIL — High 2·Med 1, 오케스트레이터 판정 포함) — 워크트리에 결과가 있다
오케스트레이터 게이트 2: gateway 844 통과(워커 `listen EPERM` 3건은 오케스트레이터 환경에서 통과)·build·lint·`✓ gateway`. 실사용(develop forecast·knowledge·records, 소래포구 발행 뒤): "왜 이렇게 많아?" 규칙 why 0.91초·설명 4문장 발행, "이 숫자는 어떻게 나온 거예요?" LLM why 1.25초, "저장해 줘" 저장 확인, 초안·what-if 안내, "내일 날씨 어때?" LLM 범위 밖 — 모두 `done.forecastId`=기존 id.
1. [High] `followup-save.ts:43` — 첫 저장에서 행사만 저장되고 스냅샷이 실패한 경우, 재시도의 `saveEvent`가 같은 id로 409 → 스냅샷까지 못 간다. 재시도는 **행사를 먼저 조회**(`getEvent`)해 있으면(같은 행사면) 재사용하고 없을 때만 저장한 뒤 스냅샷을 저장한다. 가짜 records가 중복 id를 409로 거부하게 고치고 그 경우를 테스트한다.
2. [High → 오케스트레이터 판정: **이번 범위 밖, backlog**] 예측 서비스의 `cutoff()`가 자기 시계를 쓰므로 요청 중 KST 자정을 넘으면 예보의 `asOf`가 게이트웨이 기준일과 달라질 수 있다 — 이때 예보관이 "D-14 기준일 불일치"로 **안전하게 실패**(틀린 숫자 없음)하고, 완전한 해결은 `/v1/predict`에 기준일을 넘기는 계약 변경이라 후속으로 한다. 이번에는 코드 변경 없음.
3. [Med] `why-templates.ts:26` — 사례 근거에는 발표·추정 인원도 있어 모두 "실측"이라 부르면 틀린다 → "비슷한 과거 행사 기록을 근거로 삼았어요"(중립 문구, 근거 카드가 종류를 보여 준다). 테스트 문구도 맞춘다.
- 확인: `npm -w services/gateway test`·build·lint(소켓 제한 실패는 리포트에 적기만).

## 게이트 피드백 2회차 (게이트 3 sol FAIL — High 1) — 워크트리에 1회차 결과가 있다
오케스트레이터 게이트 2: gateway 847 통과·build·lint. 1회차 High 1(저장 재시도)·Med(사례 문구) 해결(리뷰 판정).
1. [High] `followup-why.ts:56` — 게이트 B 통과 뒤 `/publish`가 실패하면 새 문장이 `candidate`로 세션에 남는다. S12(`services/knowledge/ontology/shapes/s12-candidate-checks.ttl`)는 세션의 **모든 candidate**가 현재 내용 revision의 검사를 가져야 하므로, 다음 why가 새 문장을 적재해 revision이 오르면 이전 후보 때문에 계속 실패한다(세션이 막힌다 — 오케스트레이터 확인). 발행이 실패하면(그리고 방어로 why를 시작할 때) 이 세션에 남은 **발행되지 않은 candidate를 `rejected`로 전이**해 정리한다(`claim-lifecycle.mjs`의 `/facts` 전이 `candidate → rejected` — 본문은 그대로, 상태만). 테스트: `/publish` 실패 주입 → 다음 why가 발행까지 완결, 남은 candidate 0, 새 예보 경로 회귀 없음.
- 확인: `npm -w services/gateway test`·build·lint(소켓 제한 실패는 리포트에 적기만).

## 오케스트레이터 직접 수정 (재디스패치 2회 소진 — 3회차 리뷰 High)
리뷰: 발행이 저장소에 반영된 뒤 응답만 유실되면 문장은 이미 `published` → 정리(`rejected` 전이)가 계속 거부돼 세션이 막힌다. 수정:
- `followup-why.ts` `rejectPending`: 전이가 **422(무결성 — 이미 후보가 아님)**로 거부되면 그 목록을 비운다(발행된 문장은 S12 대상이 아니다). 통신 실패는 지금처럼 남겨 다음 요청에서 다시 정리. 실제 knowledge(develop)에 금지 전이를 보내 계약에 맞는 422(`ServiceHttpError 422`, `nodeId`=문장 id)가 오는 것을 확인했다.
- 테스트 도구: 가짜 knowledge가 `claim-lifecycle.mjs`의 `factsTransitionProblems`로 실제처럼 금지 전이를 422로 거부(기존 테스트 모두 그대로 통과 — 숨은 금지 전이 없음), `teamFixture`·`explanationFixture`에 `afterResponse`(처리 뒤 응답 교체) 훅.
- 새 테스트: 발행 반영 뒤 응답 유실 → 다음 why 발행 완결·남은 candidate 0.
