# T-302: Ollama 클라이언트 + 받아쓰기(스키마 강제 추출·정규화) + 모델 평가 30문장
- 레인/워커: L3 / gpt-6-astra        - 선행: T-301 완료
- 목표(한 문장): 로컬 Ollama로 행사 문장을 `event-draft`로 옮기는 받아쓰기 팀원을 만들고, 한국어 행사 문장 30개로 후보 모델을 재서 기본 모델을 정한다.
- 읽을 것: docs/plan/05 §4(4-1 스모크 테스트 결과·4-2 모델 선택·4-3 가드), docs/plan/10 §2(#1 받아쓰기), packages/contracts/schemas/event-draft.schema.json, AGENTS.md §5-3(LLM 호출은 `src/llm/ollama-client.ts` 한 곳)
- 허용 경로: `services/gateway/src/llm/{ollama-client.ts,models.ts,fake-llm.ts}`, `services/gateway/src/team/analysis/dictation.ts`, `services/gateway/src/team/analysis/normalize/`(날짜·요금·시간대·유형 정규화 파일 분리), `services/gateway/evals/{extract_cases.jsonl,run-extract-eval.ts}`, `services/gateway/tests/`, `services/gateway/package.json`(의존성 `ollama` 추가만)
- 인터페이스:
  - `ollama-client.ts`: `OLLAMA_HOST`·타임아웃·`format`(JSON Schema) 강제·출력 토큰 상한, `LLM_MODE=fake`면 `fake-llm.ts`의 녹화 응답
  - 받아쓰기: LLM은 **스키마로 제한한 필드 추출만** 한다. `missing`(빠진 필수값)·날짜 상대 표현("둘째 주 토요일")·요금·시간대 정규화는 **코드**가 한다(9/24 스모크 테스트에서 LLM의 `missing` 판단이 틀렸다). 오늘 날짜는 `CROWDCAST_TODAY` 또는 실제 날짜를 사용자 메시지로 넘긴다(시스템 프롬프트에 넣지 않는다)
  - 평가: `extract_cases.jsonl` 30건(실제 한국 행사 표현, 모호한 장소 "중구" 3건, 날짜 상대 표현 5건, 요금·주최 누락 포함) → 모델별 필드 정확도·모호성 탐지율·지연(p50·p95, 첫 로딩/로딩됨 구분) 표를 `reports/evals/extract-<날짜>.md`에 쓴다. 후보: `qwen3:4b-instruct-2507-q4_K_M`, `qwen2.5:7b`(설치됨)
- 수용 기준(실행 명령):
  - `npm -w services/gateway test` 통과(가짜 LLM으로 추출·정규화·빠진 값 계산, 스키마 밖 출력은 거부 후 폼 입력 대체)
  - `npm -w services/gateway run eval:extract` 실행 → 리포트 생성, 기본 모델 추천과 근거
- 범위 밖: 팀 런타임·SSE(T-303), 다른 팀원
- 외부 호출: 로컬 Ollama만(인터넷 호출 없음), npm 설치
- 코드 규칙: AGENTS.md §5

## T-301에서 이월 (게이트 2·3 Med — 오케스트레이터·gpt-6-sol 같은 지적)
1. [Med] `services/gateway/package.json:8`·`src/config.ts` — 단독 `npm -w services/gateway run dev`가 `.env`를 읽지 않아 WSL에서 `OLLAMA_HOST`가 `127.0.0.1`로 잡힌다(Windows의 Ollama는 `.env`나 WSL 게이트웨이 주소). → 기동 시 레포 루트 `.env`를 읽고(이미 설정된 환경 변수가 우선), `dev.mjs`와 같은 순서(.env → WSL 게이트웨이 → 127.0.0.1)로 후보를 고른다. 테스트로 확인.
