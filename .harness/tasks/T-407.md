# T-407: S6 검증 센터 + 근거 대시보드 + S7 인사이트 — 표본 수를 숨기지 않는 성적표
- 레인/워커: L4b / gpt-6-sol        - 선행: R-03 완료(UI 기반), T-604 완료(근거 통계 `/v1/stats/datalab-usage` — develop), T-310 완료(게이트웨이 조회 프록시)
- 목표(한 문장): 검증(S6)과 인사이트(S7) 자리표시를 실제 기능으로 바꿔, 예보가 얼마나 맞았는지·근거가 어디까지 이어지는지를 **표본 수·한계와 함께** 보여 주고, 아직 없는 자료(골든·사전 등록·인사이트)는 지어내지 않고 정직한 빈 상태로 둔다.
- 읽을 것: docs/plan/11 M6-F1~F6·M7-F1~F3·"화면 구분 원칙", docs/plan/04 §3(등급 색 = 아이콘 + 글자)·§6(차트 규칙 — 이중 축 금지·표 보기·호버·2px 선·표시 단위)·§13, docs/plan/06 §8(사용 모델 `verdict=미검증` 고지 문구)·§9, packages/contracts/schemas/{backtest-summary(`disclosure` 새 선택 필드 — 9/25 R-04),model-card,datalab-usage,preregistration-scores,insight,datalab-spec,ops-status}.schema.json, packages/contracts/openapi/gateway.yaml(`/api/validation/*`·`/api/evidence/stats`·`/api/insights/*`·`/api/records/ledger/verify`), 실제 값 모양: `reports/backtest/promoted.json` → `reports/backtest/<runId>/backtest.json`·`models/<ver>/model_card.json`(읽기만 — 테스트 견본을 만들 때 모양 참고), apps/web/src/components/{common,charts}/(T-402 — 재사용), apps/web/src/pages/{validation,insights}-page.tsx(지금 자리표시), AGENTS.md §3·§5
- 허용 경로: `apps/web/src/features/validation/`(새 기능 폴더: 지표 카드·산점도·골든 표·사전 등록판·근거 대시보드·모델 카드 — 기능 하나 = 폴더 안 파일 하나 이상), `apps/web/src/features/insights/`(새: 인사이트 카드·활용 명세표·서식4 문장 복사), `apps/web/src/lib/validation/`(데이터 훅 — 새), `apps/web/src/pages/validation-page.tsx`·`insights-page.tsx`(자리표시를 기능으로 바꾸는 줄), `apps/web/src/styles/pages.css`(S6·S7 절만), `apps/web/src/**/__tests__/`(새 기능 테스트), `tests/e2e/s6-s7.spec.ts`, `reports/figures/screens/T-407-*.png`
- 인터페이스(모든 응답은 계약 스키마로 검증 — 위반이면 오류 상태, 숫자 0개):
  - **M6-F1 성능 지표 카드**: `GET /api/validation/backtest` → MdAPE(%)·80% 포함률(**계약은 비율 0~1 — 화면만 ×100**, "57.0% (49/86)"처럼 포함/N 병기)·판정 재현율·정밀도·기준선 대비. `disclosure`가 있으면 **표본 줄**: 평가 N(골드 g·실버 s), 건너뛴 연도와 사유, 명절 실버 채점 불가 수, B0·B1·B2 비교 쌍 수. `belowThresholdActual == 0`이면 재현율·정밀도 옆에 "실측 대상 미만 0건 — 경계 성능은 아직 말할 수 없어요", 비교 쌍 0이면 그 기준선 칸은 "—"와 "비교 쌍 없음"(0%로 그리지 않는다)
  - **M6-F2 예측 vs 실측 산점도**: `points[]`의 p50(가로)·actual(세로), 로그 축(두 축 같은 척도 — 이중 축 아님), 대각선 기준선, p10~p90 가로 수염, 등급별 점 모양, 환산 판정 사분면은 "가정(순간 최대 환산)" 라벨. 호버 툴팁(행사명·연도·등급·실측·예측·구간) + **표 보기** 전환. 점 86개 이상도 60fps 요구 없음(SVG)
  - **M6-F3 골든 표**: `golden[]` — 비었으면 **"골든 사례 0건 — 사례 재현 검증 전 임시 사용"**(06 §8 문구 그대로) + 이유 한 줄(골든 기사 확보 전). 있으면 인원 단위를 표시하고 `unitsComparable=false`는 "정성 비교"
  - **M6-F4 사전 등록 채점판**: `GET /api/validation/preregistration` — 아직 없으면(503/404) "9/29 사전 등록 뒤 채점은 실측 공개(약 31일 뒤) 이후에 나와요" 빈 상태. **해시 체인 검증 버튼**은 지금도 동작: `GET /api/records/ledger/verify` 결과(통과·건수·마지막 해시 앞 12자)
  - **M6-F5 근거 대시보드**: `GET /api/evidence/stats`(`datalab-usage`) — **두 비율을 따로**: 근거 연결률 = claimsWithEvidence / publishedClaims, 데이터랩 도달률 = claimsReachingDatalab / publishedClaims(분모 0이면 "발행 문장이 아직 없어요", 0%로 그리지 않는다), 데이터셋별 인용 막대(데이터랩 메뉴가 있는 것 먼저, 0건도 목록에 남김, 막대 끝 4px 둥글게), SHACL 통과율(null이면 "검증 기록 없음")
  - **M6-F6 모델 카드**: `GET /api/validation/model-card` — 버전·학습 범위·피처 목록(접기)·평가 연도·백테스트 runId·notes(한계 — 문단 나눠 읽기 쉽게, 원문 손대지 않음). 골든이 0건이면 카드 머리에 같은 미검증 문구. **버전 문자열은 예보서(T-406)와 같은 표시 함수를 쓸 수 있게** `lib/validation/format-model-version.ts`로
  - **M7-F1 인사이트 카드 / M7-F2 활용 명세표**: `GET /api/insights/{key}`(I1·I2)·`/api/insights/datalab-spec` — T-206 전이라 지금은 503/404 → 빈 상태("인사이트는 데이터 수집이 끝나면 채워져요 · 9/27"), **견본 숫자를 화면에 두지 않는다**. 데이터가 오면: 카드 크기는 중요도 순, 표본 수·기간 표시, 명세표는 메뉴·지표·기간·단위·용도·확인일
  - **M7-F3 서식4 문장 복사**: 인사이트 카드마다 [서식4용 문장 복사] — 개조식 한 줄(숫자 + 표본 수 + 기간)을 클립보드로, 복사됨 알림. 데이터가 없으면 버튼을 숨긴다
  - 레이아웃: 11 "화면 구분 원칙" — S6은 성적(F1·F2)이 위, 근거(F5)·사례(F3)·등록(F4)·카드(F6)가 아래 두 줄, 채운 주요 버튼은 화면당 하나(S6은 "해시 체인 검증"), 1024px 이하 한 줄로 쌓기
- 수용 기준(실행 명령):
  - `npm -w apps/web test`: 지표 카드(비율 ×100·포함/N·disclosure 표본 줄·대상 미만 0 문구·비교 쌍 0 → "—"), 산점도(점 수 = points 수, 표 보기 전환, 로그 축 값), 골든 0건 문구 그대로, 근거 대시보드(두 비율 분리·분모 0 빈 상태·0건 데이터셋 유지·SHACL null), 모델 카드 notes 원문 보존, API 실패 → 빈 상태(숫자 0개), 계약 위반 → 오류 상태, 인사이트 없음 → 복사 버튼 없음
  - `npm -w apps/web run build`·`lint`, 색 하드코딩 0(`grep -rnE "#[0-9a-fA-F]{3,6}\b|rgb\(" apps/web/src/features/validation apps/web/src/features/insights apps/web/src/lib/validation`)
  - e2e(`tests/e2e/s6-s7.spec.ts`, 워크트리마다 다른 포트·`reuseExistingServer: false`): 가짜 게이트웨이 응답(계약 모양 — `reports/backtest/<runId>/backtest.json`과 같은 필드)으로 S6 전 영역, 해시 체인 검증 버튼, S7 빈 상태. 스크린샷 `T-407-s6-{day,night}.png`·`T-407-s6-mobile.png`·`T-407-s7.png`
  - 실제 확인(리포트): develop 서비스(forecast·knowledge·records·gateway)를 띄워 S6를 열었을 때 — T-204 전이면 성적 카드가 503 빈 상태인 것이 정상 — 근거 대시보드는 실제 `datalab-usage`(지금은 발행 0)로 그려지는지 스크린샷
- 범위 밖: 예보서 화면(T-406), 인사이트 계산(T-206·T-209), 사전 등록 선정·채점(T-207), 운영 S8(T-410), 차트 라이브러리 추가(기존 `components/charts` 우선 — 필요하면 리포트에 이유)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5 (기능 폴더 = 한 기능, 파일 kebab-case, 파일 맨 위 역할 한 줄 + 문단마다 한국어 한 줄 주석, 300줄 넘으면 분리)
