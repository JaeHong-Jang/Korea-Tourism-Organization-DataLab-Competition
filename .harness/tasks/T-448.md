# T-448: 단순 모델에 전년 발표치 규모 계층 — 다가오는 행사가 전부 4등급(대규모)으로 나오는 문제
- 레인/워커: L2 / gpt-6-astra        - 선행: T-203b(발표치 피처·보정비율)·QA-1-backend(발표치 입력 연결) 병합, 사용 모델 v1-064e60073a7411037212(주 모델 = simple, G0 규칙 — 정의 일치 골드 5 < 30)
- 사용자 지적(9/25 밤): "다 대규모인데...?" — 다가오는 211건 **전부 4등급**, 순간 최대 p50 최소 5,372명(대규모 자체 기준 5,000). 원인: 주 모델이 **단순 모델**(유형·전회차 계층 중앙값)이라 행사별 규모를 거의 구분하지 못함 — 학습 라벨은 작은 행사도 있다(1,110건, 일평균 하위 10% ≈ 1,923명).
- 목표(한 문장): 단순 모델의 규모 계층에 **문체부 개최계획서 전년 발표 방문객 수**(행사 마스터 `visitors_announced` — 누적 ÷ 기간 = 일평균, T-203b 보정비율 적용)를 넣어 행사별 규모를 구분하게 하고, 같은 롤링 백테스트로 비교해 **승격 규칙(06 §8 — MdAPE +3%p·포함률 −0.05 이내)을 지킬 때만** 새 버전으로 올린다.
- 읽을 것: docs/plan/06 §5(모델)·§8(승격 게이트)·§11(G0 — 주 모델 선택 규칙), services/forecast/src/crowdcast/models/{baselines,train,backtest,evaluate,card,report_tables}.py(단순 모델 계층·T-203b 보정비율), features/announced.py, configs/model.yaml, `reports/backtest/promoted.json`, AGENTS.md §3·§5
- 허용 경로: `services/forecast/src/crowdcast/models/`, `services/forecast/src/crowdcast/features/`, `configs/model.yaml`(설정 키 추가만), `services/forecast/tests/models/`·`tests/features/`
- 인터페이스:
  - **계층 순서**: 전회차 라벨(있으면) → **발표치 규모**(전년 발표치 일평균 × 보정비율, 발표 시점이 기준일 전인 것만 — 누수 검사) → 유형 중앙값. 발표치 계층의 분위수 폭은 학습에서 발표치 ↔ 라벨 잔차로 추정(구간 포함률이 떨어지지 않게).
  - **비교**: 같은 폴드·같은 라벨로 v1 대비 MdAPE·80% 포함률(n)·판정 재현율 + **다가오는 일괄 예보 등급 분포**(후보 모델로 계산 — 파일은 쓰지 않고 표만) 보고서(`reports/backtest/<새 run>/`).
  - **승격**: 기존 승격 규칙 그대로(자동 판정) — 통과면 `promoted.json` 갱신 후보로 **리포트에만 제안**(실제 승격·일괄 예보 재생성은 오케스트레이터가 9/26 자료 수집 뒤 한 번에). 미달이면 이유와 표.
  - 모델 카드 notes에 "규모 계층: 발표치 사용 N건"과 한계(발표치 과장 가능성 — 보정비율로 줄였지만 행사장 실측 아님) 한 줄.
- 수용 기준: `pytest -q services/forecast/tests`·ruff, 테스트: 계층 선택(전회차 > 발표치 > 유형)·누수(기준일 뒤 발표치 제외)·보정비율 적용·분위수 폭, 비교 백테스트 1회 결과(표·등급 분포)를 리포트에. 사용 모델 v1 예보 바이트 동일(승격 전).
- 범위 밖: LightGBM 주 모델 전환(G0 — H2 골드 필요), 임계값 변경
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
