# T-206: 인사이트 I1~I6 + 데이터랩 활용 명세 — forecast `/v1/insights/{key}`·`/v1/datalab/spec` (T-209 합침)
- 레인/워커: L2 / gpt-6-astra        - 선행: T-203b(발표치 비율)·T-104(파이프라인)·일괄 예보(upcoming) 완료
- 목표(한 문장): S7 인사이트 화면과 서식4 1)·2)의 숫자를 채우도록, **이미 가진 API 자료만으로**(H7 추가 CSV 없이 — 9/25 결정) 인사이트 6개와 데이터랩 활용 명세를 계산해 계약대로 내보낸다. 지금 실제 서버에서 `/api/insights/*`가 503(forecast 경로 없음)이다.
- 읽을 것: docs/plan/06 §7(인사이트 표 — **정본**)·§2(단위 메타 — 정의가 같은 쌍만 비교)·§4(순간 최대 가정), docs/plan/11 M7, packages/contracts/{schemas/{insight,datalab-spec,evidence}.schema.json, openapi/forecast.yaml(`/v1/insights/{key}`·`/v1/datalab/spec`), jsonld/master-labels.json(datasets)}, services/forecast/src/crowdcast/{models/baselines.py(T-203b 발표치 비율), analytics/{upcoming,baseline}.py, paths.py}, data/processed/ 파일들(`mcst_festivals.parquet`·`labels.parquet`·`events.parquet`·`region_daily.parquet`·`upcoming_forecasts.jsonl`), AGENTS.md §3·§5
- 허용 경로: `services/forecast/src/crowdcast/analytics/insights/`(새 — 지표 하나당 파일 하나 + `spec.py` + `__main__.py`), `services/forecast/src/crowdcast/api/routes/insights.py`(새), `services/forecast/src/crowdcast/api/app.py`(라우터 등록 줄만), `services/forecast/src/crowdcast/pipeline/`(batch 단계 뒤에 인사이트 계산 한 줄), `services/forecast/tests/analytics/insights/`
- 인터페이스:
  - **계산 = CLI**: `python -m crowdcast.analytics.insights` → `data/processed/insights/{I1..I6,datalab-spec}.json`(계약 검증 뒤 저장, 원자적 쓰기). 파이프라인 batch 뒤에도 실행. **라우트는 저장된 JSON만 읽는다**(요청마다 계산하지 않음 — 파일이 없으면 404 + 이유).
  - **I1 발표 vs 실측 괴리**: 문체부 개최계획서 전년 발표치(누적 ÷ 기간 = 일평균)와 라벨 일평균의 비율 분포 — **정의가 같은 쌍만**(행사장·일 단위, 발표 시점 메타 포함), 중앙값·사분위·쌍 수(`comparablePairs`). series = 비율 구간별 개수.
  - **I2 1,000명 경계선 행사 비중**: 다가오는 일괄 예보의 등급 분포 + **주최측 예상(개최계획서 예상 인원 → 06 §4 같은 환산 가정)으로 본 등급 분포**를 둘 다 series로(모델이 지금 4등급에 치우친 한계를 headline에 밝힌다 — 숫자를 꾸미지 않는다).
  - **I3 유형별 순간 밀집도**: 유형별 동시체류율(configs 가정) × 규모 분포 중앙값. **I4 평시 대비 순증 배수**: 라벨 행사의 행사일 방문 ÷ 평시(같은 시군구, T-203 평시 계산 재사용) — 시군구 규모별. **I5 외지인 비중**: `region_daily`의 방문자 구분(현지인·외지인·외국인)으로 행사일 vs 평시 외지인 비율 — **유입 권역은 자료 없음(H7)**이라고 headline·evidence에 밝힌다. **I6 월·지역별 행사 밀도**: 시도 × 월 행사 수·예보 인파 합(다가오는 일괄 예보 + 개최계획서).
  - 모든 지표: `sampleSize`·`comparablePairs`·`period`·`computedAt`, `evidence`는 계약 `evidence`(DataEvidence — 데이터셋 id·기간·행 수, 가정은 AssumptionEvidence). 표본이 0이면 지표를 만들지 않지 말고 headline에 "표본 없음"으로.
  - **datalab-spec**: 파이프라인이 **실제로 읽은** 데이터셋만 행(방문자 15101972·TourAPI·기상청(캐시에 실제 응답이 있을 때만)·특일·문체부 개최계획서 등 — master-labels datasets id), `rowsRead`·`confirmedAt`(마지막 수집일)·`usedIn`(피처·라벨·평시·인사이트 키)·`evidenceCount`(일괄 예보 `upcoming_forecasts.jsonl`의 evidence에서 그 데이터셋을 가리키는 수).
- 수용 기준: `pytest -q services/forecast/tests`(전체)·ruff, 지표마다 작은 합성 표 단위 테스트(정의 불일치 쌍 제외·표본 0 문구·I2 두 series), 산출 JSON 7개 계약 검증, 실제 `data/processed`로 CLI 한 번 실행한 결과 요약을 리포트에(숫자·표본 수). HTTP 실행은 오케스트레이터.
- 범위 밖: H7 추가 CSV(T-107), 웹 화면(T-407이 이미 그린다 — 계약만 맞추면 뜬다)
- 외부 호출: 없음(보유 자료만)
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**(게이트 = 테스트·lint).
