# QA-2-web: 사용자 검토(9/25 밤) 반영 1차 — 구간 막대 설명, S6 산점도, S8 카드 깨짐, 검증 메모 상자 선, 모델 카드 "한계" 읽히게, S5 작은 것들
- 레인/워커: L4b / gpt-6-sol        - 선행: develop d8714a7(2주차 전부)
- 목표(한 문장): 사용자가 화면을 직접 보고 "무엇을 보여 주려는지 모르겠다·깨진다·읽을 수 없다"고 한 곳을 고친다 — 기능 추가 없이, 처음 보는 사람이 설명 없이 이해하게(11 화면 구분 원칙).
- 읽을 것: docs/plan/04 §6(차트 규칙)·§11(문구)·§13, `dataviz` 원칙(범례·직접 라벨·표 보기·호버), apps/web/src/{components/charts/range-bar.tsx(+ 쓰는 곳: consult-chat/forecast-preview·forecast-report·festival-list), features/validation/(산점도·백테스트 메모·모델 카드), features/ops/freshness-card.tsx·ops css, features/my-events/(reforecast-card·페이지 제목)}, AGENTS.md §3·§5
- 허용 경로: `apps/web/src/components/{charts,common}/`, `apps/web/src/features/{validation,ops,my-events,forecast-report,festival-list}/`, `apps/web/src/features/consult-chat/forecast-preview.tsx`(구간 막대 부분만), `apps/web/src/pages/my-events-page.tsx`(제목), `apps/web/src/styles/`, `tests/e2e/`(해당 스펙·스크린샷 보강)
- 고칠 것:
  1. **구간 막대(로그 눈금)**: 지금은 "로그 눈금" 선 위에 파란 막대·점선만 있어 뜻을 모른다 → 제목 "순간 최대 예상 인원과 법정 기준", 점선에 **"법정 기준 1,000명(안전관리계획 수립)"** 라벨, 막대에 "예상 7,857~26,000명(가운데 1.4만)" 직접 라벨, 한 줄 결론("기준보다 약 14배 — 수립 대상 구간이에요" — 숫자는 카드 값 그대로, 배수는 p50 ÷ 1,000을 소수 없이, 로그 축이라는 사실은 작은 글씨로). 목록 카드의 작은 막대도 같은 규칙(공간 없으면 기준선 라벨만).
  2. **S6 예측 vs 실측 산점도**: 86개 구간 가로선이 겹쳐 줄무늬처럼 보임 → 기본은 **점만**(포함 = 초록·벗어남 = 주황 + 모양 차이, 범례), 구간 선은 호버·포커스한 점 하나만, 대각선에 "예측 = 실측" 라벨, 점 겹침 줄이기(투명도·작은 흔들림 없이 — 같은 값은 겹침 수 표시), [표로 보기].
  3. **S8 최신성 카드 깨짐**: 좁은 칸에서 카드가 오른쪽으로 넘쳐 잘림(이미지 #12) → 카드가 칸 폭 안에서 줄바꿈, 긴 runId는 앞 12자 + 복사, "마지막 수집: 미수집"처럼 자료 없는 칸은 한 줄 "아직 수집 전"으로.
  4. **S6 백테스트 메모 상자**: 왼쪽 파란 세로선 제거(다른 상자와 같은 모양).
  5. **모델 카드 "한계"**: 지금은 모델 카드 원문(SHA·피처 결측 JSON까지)이 한 문단으로 쏟아짐 → 사람이 읽는 요약 5~7줄(평가 표본 수·골드/실버, 80% 구간 포함률, 코로나 연도 제외, 순간 최대는 추정 산식(실측 아님), 작은 행사 과대 예보 경향, 골든 사례 0건) — 값은 모델 카드 JSON 필드에서 꺼내고(지어내지 않음), 원문 전체는 [원문 보기] 접힘 안에.
  6. **S5**: 재예보 카드의 근거 칩이 원시 id(`ev-weather-…`)로 보임 → 근거 제목(없으면 "날씨 근거"), "새 예보서 보기"는 링크 버튼, 페이지 제목 "내행사" → "내 행사"(메뉴와 같게).
- 수용 기준: `npm -w apps/web test`(구간 막대 라벨·배수 문구, 산점도 기본 점만·호버 구간, 한계 요약 = 카드 필드)·build·lint·색 하드코딩 0, e2e 기존 통과 + 스크린샷 `reports/figures/screens/QA-2-{rangebar,s6-scatter,s6-card,s8,s5}.png`. 실행은 오케스트레이터.
- 범위 밖: S2 상담 화면·3D 지도(사용자 방향 결정 뒤 별도), 새 기능
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
