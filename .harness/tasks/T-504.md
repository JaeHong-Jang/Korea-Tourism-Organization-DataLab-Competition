# T-504: 안전관리계획 초안 — 저장·조회·수정(본문 = 발행 문장 확인) + PHPWord docx(서체·표·근거 각주·워터마크)
- 레인/워커: L5 / gpt-6-sol        - 선행: T-502(행사·예보 스냅샷) 완료
- 목표(한 문장): 발행된 예보 스냅샷의 문장만으로 이루어진 9개 섹션 초안을 records에 저장하고, 담당자가 한글·워드에서 바로 고칠 수 있는 docx(근거 각주·"참고용" 워터마크)로 내보낸다.
- 읽을 것: packages/contracts/schemas/{plan,plan-section,claim,evidence,forecast-report}.schema.json, packages/contracts/openapi/records.yaml(`/v1/plans`·`/v1/plans/{id}`·`/v1/plans/{id}/export.docx`), packages/contracts/fixtures/{plan-section,forecast-report,claim,evidence}/, services/records(T-501~T-503 — 계약 검증·스냅샷 저장소·트리거 방식), docs/plan/04 §문서(서체), `.harness/backlog.md` "T-504" 절, AGENTS.md §3·§5
- 허용 경로: `services/records/src/Plans/`(파일 여럿: 저장소·서비스·본문 검사·컨트롤러·docx 내보내기), `services/records/src/Routes/RecordsRoutes.php`(초안 라우트 등록 줄만), `services/records/src/Health/Controller.php`(backlog R-02 Low 한 가지), `services/records/migrations/005_plans.sql`, `services/records/tests/`, `services/records/composer.json`·`composer.lock`(`phpoffice/phpword` 추가)
- 인터페이스:
  - `POST /v1/plans` body = 계약 `plan`(요청·응답 계약 검증). 서버 확인(실패 = 422, 어느 섹션·어느 규칙인지 메시지):
    - `forecastId`의 스냅샷이 records에 있고 `eventId`·`sessionId`가 스냅샷과 같다
    - 섹션 9개 = 계약 enum 키가 **한 번씩, enum 순서대로**
    - `작성됨` 섹션: `claimIds`가 모두 그 스냅샷의 **발행(`published`) 문장**이고, `body` = 그 문장들의 `rendered`를 **claimIds 순서대로 줄바꿈(`\n`)으로 이은 것**과 정확히 같다(공백 정규화 없음). `검토 필요` 섹션은 body가 비어 있어도 된다
    - `lockedFields[].quantityId`는 그 스냅샷에 있는 수치여야 한다
    - 같은 `id`가 있으면 409. `createdAt`·`updatedAt`은 서버 KST 시각으로 덮어쓴다
  - `GET /v1/plans/{id}` → 계약 `plan`, `PUT /v1/plans/{id}` → 같은 검사 + `updatedAt` 갱신(수정 이력은 이전 본문을 `plan_revisions`에 남긴다 — 덮어쓰기 금지)
  - `GET /v1/plans/{id}/export.docx` (PHPWord):
    - A4, 본문 **함초롬바탕 11pt·줄간격 160%**, 제목·표 **맑은 고딕**(서체 이름만 지정 — 내장하지 않는다)
    - 머리말(모든 쪽) = 워터마크 문구 `참고용 초안 — 담당자 검토 필수`(계약 const 그대로), 바닥글 = 쪽 번호·예보 id·발행 시각
    - 첫 쪽: 제목, **행사 요약 표**(행사명·일시·장소·판정 등급·순간 최대 p10~p90·일평균 — 모두 스냅샷 값, 단위 포함)
    - 섹션 9개: 제목 + 본문. 문장마다 그 `evidenceIds`를 **각주**로(근거 제목·출처·자료 기간 — 스냅샷의 evidence에서), `검토 필요` 섹션은 제목 옆 "[검토 필요]"와 빈 칸 안내
    - 같은 초안이면 같은 내용: 문서 속성 생성·수정 시각 = `updatedAt`(바이트 동일까지는 요구하지 않고 `word/document.xml`이 같으면 된다)
  - backlog R-02 Low: health 응답을 반환 직전에 OpenAPI health 스키마로 검증
- 수용 기준(실행 명령):
  - `cd services/records && composer test && composer stan` 통과: 정상 저장·조회, 본문 불일치 422(한 글자·줄바꿈 차이 포함), 섹션 누락·중복·순서 오류, `작성됨`인데 claimIds 없음(계약 픽스처 `invalid-written-without-claims`), 발행 안 된 문장 id, 다른 세션의 문장, 없는 스냅샷, 409, PUT 이력 보존, docx 테스트(`ZipArchive`로 `word/document.xml`에 섹션 제목 9개·워터마크 문구·각주 참조, `word/styles.xml`에 두 서체 이름, 요약 표 값이 스냅샷과 같음)
  - `node scripts/dev.mjs --check --only records`에 `✓ records`
  - 리포트에 **테스트 DB로 만든 영종 예시 docx**를 `reports/figures/screens/T-504-plan-example.docx`로 저장(사람이 한글에서 열어 볼 파일 — 공개 원장·실데이터 아님을 문서 첫 줄에 적는다)과 해시
- 범위 밖: 섹션 문장 작성(T-306 계획서 에이전트), 편집기 화면(T-408), 실측·공유(T-505)
- 외부 호출: composer 설치 네트워크만
- 코드 규칙: AGENTS.md §5 (PHP: 파일 첫 줄 역할 주석, 메서드마다 한 줄 주석, strict_types, PSR-12)

## 게이트 피드백 1회차 (게이트 3 교차 리뷰 FAIL — gpt-6-astra, 오케스트레이터 게이트 4 지적 2 포함) — 워크트리에 결과가 있다
오케스트레이터 게이트 2: test 30·stan·✓ records 통과. 게이트 4(예시 docx를 ZIP으로 열어 확인): 워터마크 머리말·서체 이름·9섹션·요약 표·각주 연결은 있음.
1. [High] `Plans/Exporter.php:54` — XML 이스케이프가 꺼져 `영종 안전 & 의료 <검토>` 같은 제목·근거 제목에서 `document.xml`·`footnotes.xml`이 깨진다 → PHPWord 출력 이스케이프를 켜고(`Settings::setOutputEscapingEnabled(true)`), `& < > " '`가 든 제목·본문·근거로 만든 docx의 XML 파싱 테스트.
2. [High · 계약 v1.6 정정] `Plans/BodyInspector.php:84` — `lockedFields`가 수치 id만 확인해 `value: "1명"` 위조가 저장된다. 계약을 정했다(develop `packages/contracts` — 워크트리에 병합해 둔다): `name` ∈ `p10|p50|p90|value`(구간 수치는 p10·p50·p90, 단일 수치는 value), `value` = 스냅샷 수치의 **원래 숫자(JSON 숫자 표기) + 공백 + 단위**(예 `"21000 명"`). 스냅샷에서 만든 문자열과 **정확히 같지 않으면 422**(덮어쓰지 않는다). 테스트: 위조 값·없는 칸(단일 수치에 p50 등)·단위 다름.
3. [Med] `Plans/Exporter.php:26` — `PlanBody`·`PlanHeading`·`PlanTable`에 `w:styleId`가 없어 스타일 참조가 연결되지 않고 기본 동아시아 서체가 Arial이다 → 스타일이 실제로 적용되게(스타일 id 연결 또는 문단·글자에 서체 직접 지정) + 기본 글꼴 `eastAsia`도 함초롬바탕. 테스트는 서체 이름이 **본문 런에 실제로 적용되는지**(styles.xml 이름만이 아니라 document.xml 참조까지) 검사.
4. [Med] `Plans/Service.php:87` — 연관 배열 디코딩으로 JSON 객체·배열 구분이 사라져 `sections: {"0": …}`·`claimIds: {}`가 200으로 저장된다 → 원본 JSON을 **객체 그대로** 계약 검증한 뒤 내부 배열로 바꾼다(T-501·T-502 방식과 같게). 테스트 2건.
5. [Med] `Plans/Service.php:50` — 동시 수정: 같은 초안을 읽은 두 편집자의 PUT이 모두 200이고 뒤의 것이 앞의 수정을 되돌린다 → PUT 본문의 `updatedAt`이 저장된 값과 같을 때만 반영(트랜잭션 안에서 비교), 다르면 **409**. 테스트.
6. [Med] `migrations/005_plans.sql:2` — 수정 이력(`plan_revisions`)에 UPDATE·DELETE·REPLACE 방지 트리거가 없다 → T-501 스냅샷 방식 그대로(recursive_triggers 전제, REPLACE·rowid 우회 포함). 테스트.
7. [Med · 게이트 4] **날짜가 원시 문자열**로 찍힌다(`2025-10-18T19:00:00+09:00 ~ 2025-10-18T21:00:00+09:00`) → 담당자가 읽는 문서다: `2025년 10월 18일(토) 19:00~21:00`(하루), 여러 날이면 `10월 17일(금) 10:00 ~ 10월 19일(일) 21:00`, 시간대 `미상`(계약 v1.6)이면 시각 없이 날짜만. 바닥글 발행 시각도 같은 형식. 한국 시각 기준·요일 계산 테스트.
8. [Med · 게이트 4] **각주가 전부 "출처 미기재 — 자료 기간 미기재"**다(예시의 규칙·모델·가정 근거 모두). 스냅샷의 evidence에는 종류별 정보가 있다 → 종류별 각주: 규칙 = 제목 · 법정/자체 · 조항(있으면), 모델 = 제목 · 모델 버전 · 학습 범위(`period`), 가정 = 제목 · `summary`(값·범위), 데이터 = 제목 · 출처(`source`) · 기간(`period`) · 공개일, 사례 = 제목 · 행사 id · 기간. 필드가 정말 없을 때만 "미기재". 테스트: 종류 5개 각주 문구.
9. [Low] `Service.php:18` 등 새 생성자·메서드의 한국어 한 줄 주석(AGENTS §5), `BodyInspector`의 여러 줄 조건문 PSR-12 정리.
- 표시 반올림(리뷰 Med `Exporter.php:112`)은 결함이 아니다 — 사람 수라 **정수 반올림(0.5 올림)**이 규칙이다. 이 규칙을 Exporter 주석과 테스트(13000.25 → 13,000, 13000.5 → 13,001)로 명시만 한다.
- 확인: `composer test && composer stan`, `dev.mjs --check --only records`, 예시 docx 다시 만들고 해시(특수문자 제목으로 한 장 더: `T-504-plan-escape.docx`).

## 게이트 피드백 2회차 (게이트 3 astra — 1회차 9건 중 4건 해결, 5건 미해결) — 마지막 재디스패치
오케스트레이터 게이트 2: test 35·stan·✓ records. 게이트 4: 날짜(`2025년 10월 18일(토) 19:00~21:00`)·종류별 각주·특수문자 XML 확인. 허용 경로 추가: `services/records/migrations/006_plan_revisions_guard.sql`.
1. [Med] `Exporter.php:30` — `document.xml`이 참조하는 `PlanBody`·`PlanHeading`·`PlanTable`이 styles.xml에 **styleId로 정의되지 않아** 제목·표의 맑은 고딕·크기가 적용되지 않는다(본문은 기본값 함초롬바탕이라 우연히 맞음) → PHPWord에서 이름이 아닌 id로 연결되는 방식(`addFontStyle`/`addParagraphStyle`의 실제 styleId)을 확인하거나 런에 직접 서체·크기를 준다. 테스트는 **참조 id → 정의 → 서체·크기**를 함께 대조(오케스트레이터가 예시에서 확인한 방법: rStyle 값마다 styles.xml에서 `w:styleId`를 찾는다).
2. [Med · 계약 v1.6 정정] `BodyInspector.php:103` — “JSON 숫자 표기 그대로”는 PHP에서 원문을 보존할 수 없어 규칙을 바꿨다(develop `plan-section.schema.json` 설명): `value` = **정규 숫자 표기**(records의 `CardProjection::canonical` = 계약 `rules/card-projection.mjs` `canonical` — 정수 값이면 정수, 그 밖은 가장 짧은 십진, -0은 0) + 공백 + 단위. 스냅샷의 `21000.0`·`21000.00`·`2.1e4`는 모두 `"21000 명"`만 통과, `"21000.0 명"`은 422. 이 canonical 함수를 **그대로 호출**(새로 만들지 말 것). 회귀 테스트 3건.
3. [Med] `EvidenceLabel.php:22` — 모델 근거 각주가 근거의 `period`보다 `predictionRun.trainRange`를 앞세운다 → **근거 자신의 `period`를 우선**, 없을 때만 대체값. 테스트: period가 다른 스냅샷.
4. [Med] `005_plans.sql:11` — 이미 적용된 005에 트리거를 더해 기존 DB(`data/app/records.sqlite` — 1차 산출물로 이미 005가 적용됨)에는 이력 트리거가 없다 → 005는 1차 모양(테이블)으로 되돌리고, 트리거는 **새 마이그레이션 006**(`CREATE TRIGGER IF NOT EXISTS`)으로. 테스트: 1차 005만 적용된 DB에 `Migrator::run()` → 트리거 설치·UPDATE/DELETE/REPLACE 거부.
5. [Low] `ExportController.php:14` 등 남은 생성자 한국어 한 줄 주석.
- 확인: `composer test && composer stan`, `dev.mjs --check --only records`, 예시 docx 2개 다시 + 해시.
