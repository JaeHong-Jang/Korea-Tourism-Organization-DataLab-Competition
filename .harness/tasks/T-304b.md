# T-304b: 게이트 A도 검증팀이 일한다 — 출처확인·법규담당·깐깐이의 작업 단계로 기록
- 레인/워커: L3 / gpt-6-astra        - 선행: T-304·T-305 완료(develop 2e83ebb)
- 목표(한 문장): 지금 게이트 A의 S03~S09 검사는 팀장(`analysisGate`) 안에서 한 번에 돌아 작업판에 검증팀이 움직이지 않는다 — 계획 10 §3대로 **출처확인(S03·S04·S08) ∥ 법규담당(S05·S06) ∥ 깐깐이(S07·S09)**를 병렬 팀원 단계(`agent_status`·`agent_step`)로 실행·기록하고, 게이트 A 판정·순서·결과는 그대로 둔다(R-05 Med).
- 읽을 것: docs/plan/10 §2(팀원 표 5·7·8)·§3(게이트 A 행)·§5(작업판), packages/contracts/{sse-events.md, rules/sse-sequence.mjs}, services/gateway/src/team/{lead/gates.ts(`analysisGate`), verification/, runtime/executor.ts}, `.harness/reports/R-05.md`, AGENTS.md §3·§5
- 허용 경로: `services/gateway/src/team/lead/gates.ts`(게이트 A 부분), `services/gateway/src/team/verification/`(게이트 A용 실행 입력만 — 게이트 B 동작은 그대로), `services/gateway/src/team/lead/playbooks.ts`(게이트 A 호출 줄만), `services/gateway/tests/`
- 인터페이스:
  - 게이트 A = knowledge `/validate` 한 번(지금처럼 S03~S09, 같은 revision·masterVersion)을 부르고, 그 결과 위반을 **모양별로 세 팀원에게 나눠** 각 팀원의 `agent_step`(상태 done/blocked, note 예: "출처와 공개 시점을 확인했어요" / "S05 위반 1건을 찾았어요", `outputEvidenceIds`는 검사한 근거)으로 남긴다. 세 팀원은 병렬(같은 실행기, 순서 무관)이고 `/validate`를 세 번 부르지 않는다(결과 하나를 나눈다 — 지연 그대로).
  - 게이트 A의 `gate` 이벤트·판정·실패 처리(오류 카드·숫자 미전송)는 바꾸지 않는다. 팀원 단계는 `gate(A)` **앞**에 온다.
  - 작업판 말풍선(note)은 팀 말투의 짧은 문장, 원문 위반 메시지를 그대로 붙이지 않는다(개수·모양 id만).
- 수용 기준(실행 명령):
  - `npm -w services/gateway test`·`build`·`lint`, `node scripts/dev.mjs --check --only gateway`
  - 새 테스트: 정상 스트림에 게이트 A 앞 `source-check`·`rule-check`·`skeptic` 단계가 하나씩(`sequenceProblems` 통과), `/validate` 호출 1회, 위반 픽스처(S05 위반) → 법규담당 단계 blocked + 게이트 A 실패 + 숫자 미전송, 기존 T-303·T-304·T-305 스트림 테스트 전부
- 범위 밖: 게이트 B·후속 플레이북, 웹 작업판(L4a)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5
- 실사용 확인은 오케스트레이터가 한다(워커 샌드박스는 소켓을 막는다).
