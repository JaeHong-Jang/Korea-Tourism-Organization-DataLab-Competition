# T-101: data.go.kr 클라이언트(방문자·집중률·TourAPI·특일) + 캐시·호출 장부·공개 시점 + 벌크 수집 CLI
- 레인/워커: L1 / gpt-6-astra        - 선행: T-100 완료, H1(키 확인됨)
- 목표(한 문장): 일 1,000건 한도를 지키며 방문자 API를 2018년부터 벌크 수집하고, 모든 행에 공개 시점(available_at)을 붙여 Parquet으로 쌓는다.
- 읽을 것: docs/plan/06 §1(9/24 확인한 데이터 사실 — 2025 코드 체계, **부모 시 코드 12개 이중 계산 주의**)·§3(공개 시점 규칙), docs/reference/데이터_신청_안내.md, AGENTS.md §3 규칙 6(호출 한도)
- 허용 경로: `services/forecast/src/crowdcast/data/{datago_client.py,visitors.py,concentration.py,tourapi.py,holidays.py,call_ledger.py,crosswalk.py}`, `services/forecast/tests/data/`(녹화 응답 픽스처 포함)
- 인터페이스:
  - `datago_client`: serviceKey(`.env`의 `DATA_GO_KR_KEY`, 로그 금지)·MobileOS·MobileApp·`_type=json`, 페이지 순회, 재시도(tenacity), 응답 캐시(`data/cache/datago/<api>/<요청 해시>.json`), **호출 장부**(`data/cache/datago/ledger.csv`: 날짜·API·호출 수 — 하루 900건을 넘기면 멈춘다)
  - `visitors`: `locgoRegnVisitrDDList`(numOfRows 1000) → `data/processed/region_daily.parquet` 열 `sigungu_code, sigungu_name, date, tou_div(현지인|외지인|외국인), visitors, is_parent_city(bool), available_at(= date + 4일), source_hash`. **부모 시 코드(41110 등 12개)는 is_parent_city=True로 표시**하고 합계에 쓰지 않는다
  - CLI: `uv run --package crowdcast-forecast python -m crowdcast.data.visitors --from 2018-01-01 --to 2025-12-31 --max-calls 800`(중단·재시작 가능, 이미 받은 구간은 건너뜀)
  - `concentration`(15128555)·`tourapi`(searchFestival2·searchKeyword2)·`holidays`(특일): 클라이언트 함수만(호출은 필요할 때)
- 수용 기준(실행 명령):
  - `uv run --package crowdcast-forecast pytest -q services/forecast/tests/data` 통과(녹화 응답으로 페이지 순회·캐시 적중·장부 한도·부모 시 표시·available_at 계산)
  - 실호출: `--from 2025-09-01 --to 2025-09-07` 실행 → region_daily 행 수와 시군구 수(264, 부모 12 표시) 리포트에 기록, 장부의 호출 수
- 범위 밖: 전체 기간 벌크 실행(오케스트레이터가 한도를 보며 나눠 실행), 라벨 계산
- 외부 호출: data.go.kr ≤ 30건(테스트용), 패키지 설치
- 코드 규칙: AGENTS.md §5

## 게이트 피드백 1회차 (게이트 3 교차 리뷰 FAIL — gpt-6-sol + 오케스트레이터 벌크 실행에서 발견) — 워크트리에 결과가 있다
**새 데이터 사실(9/25, docs/plan/06 §1에 기록)**: 방문자 API는 2026-07-01부터 2026년 코드 체계로 준다. 기준 코드 체계는 2025년으로 유지한다.
1. [High, 새 사실] **2025 코드 대응표** `services/forecast/src/crowdcast/data/crosswalk.py`(허용 경로에 추가): 06 §1의 세 규칙을 그대로 구현한다 — (a) 광주·전남 27개 1:1(시도 안 이름 대응, 표를 코드에 상수로), (b) 화성시 41590 부모 + 41591·41593·41595·41597 구: 2025 체계로는 41590 하나(구 행은 버리고 부모 행을 쓰되, 부모 = 구 합계인지 검사해 어긋나면 로그), (c) 인천 {28110, 28140, 28260} ↔ {28125, 28155, 28275, 28290}: 2026-07-01 이후 행은 `continuity_break=true`로 표시하고 2025 코드로 옮기지 않는다. `region_daily.parquet`에 `code_system`(2025|2026 원래 체계)·`raw_sigungu_code`·`continuity_break` 열을 더한다. 테스트: 세 규칙 각각 + 2025·2026 행이 섞인 날짜 경계(2026-06-30/07-01).
2. [High] `visitors.py:79` 부모 시 판정이 2025년 고정 목록이라 2026년 화성시(41590)와 구가 모두 `is_parent_city=False` → 날짜의 코드 체계별 부모 목록(2025: 기존 12개, 2026: 기존 + 41590)으로 판정.
3. [High] 테스트 녹화 JSON이 `.gitignore`의 `*.json`에 걸려 추적되지 않았다 → 오케스트레이터가 `.gitignore`에 `!services/*/tests/**/fixtures/**`를 추가했다(develop 병합됨). 픽스처를 그대로 두면 된다.
4. [벌크 실행 실패] `--from 2026-07-01 --to 2026-09-20` 실행이 8월에서 `{"status":"error","reason":"수집 실패…"}`로 끝났다(7월까지 저장). 원인을 찾아 고치고, 실패 이유가 로그에 구체적으로 남게 한다(키는 가린다). 이미 받은 캐시(`data/cache/datago/visitors/`)로 재현 가능해야 한다 — **API 재호출은 캐시에 없는 요청만**, 이번 수정 확인 호출은 합계 30건 이하.
5. [Med] `datago_client.py:164` HTTP 429·한도 오류는 재시도하지 말고 즉시 중단(장부 보호). [Med] `datago_client.py:128` 페이지 간 같은 항목 반복 검사(API별 식별자).
6. [Low] `reports/runs/T-101-evidence.json`은 최종 산출물 기준으로 다시 쓴다(오케스트레이터 벌크 실행분 포함: 2024-01-01~2026-07-31, 579,417행 시점).
- 오늘(9/25) 장부 사용량 613건 — 남은 한도 안에서 4번 확인만.

## 게이트 피드백 2회차 (오케스트레이터 — 마지막 재디스패치) — 새 데이터 사실 반영
1회차 수정(대응표·부모 시·연속성 끊김·8월 실패 원인·429·중복)은 리포트로 확인했다. 교차 리뷰 전에 **공개 시점 규칙**을 바꾼다.
1. [High, 새 사실 — 06 §3 갱신] 9/25 현재 API는 8/25까지만 준다(8/26~9/20 0행) → 관측 반영 지연 약 31일. `available_at = date + 4일`은 백테스트에서 **아직 볼 수 없던 자료를 쓰게 하는 누수**다. → 반영 지연을 모듈 상수 하나(`VISITORS_LAG_DAYS = 35`, 06 §3 인용 주석)로 두고 `available_at = date + 35일`. 이미 받은 행도 캐시로 다시 만들어(재호출 없음) 전체 `region_daily.parquet`의 `available_at`을 새 규칙으로. 테스트 갱신.
2. [Med] 0행 응답(8/26 이후)은 "아직 공개 전"으로 캐시에 **완료 표시하지 않는다**(다음 실행 때 다시 묻게). 지금 저장된 빈 응답 캐시가 완료로 표시돼 있으면 지운다. 체크포인트에 "공개 전 구간"을 따로 기록.
3. 증빙(`reports/runs/T-101-evidence.json`)을 새 규칙 산출물 기준으로 다시 쓴다.
- API 호출은 하지 않는다(캐시만). 오늘 장부 616건.
