# T-308: C 평가 — 예보팀 시나리오 20개를 실제 스트림으로 채점(근거 없는 발행 0·숫자 불일치 0·되묻기·지연)
- 레인/워커: L3 / gpt-6-astra        - 선행: T-306·T-306c 완료(develop), T-302(추출 평가 30 — `evals/run-extract-eval.ts`·`reports/evals/extract-2026-09-24.*`)
- 목표(한 문장): 서식4 평가표의 "에이전트" 줄(추출 30·시나리오 20)을 채울 수 있게, 상담 시나리오 20개를 **실제 게이트웨이 스트림**으로 돌려 발행 문장의 근거 연결·숫자 일치·되묻기 적중·순서 규칙·지연(p50·p95)을 기계적으로 채점하는 평가기를 만든다.
- 읽을 것: docs/plan/05 §4-4(C 평가)·docs/plan/10 §7·docs/plan/08(서식4 평가 매핑), services/gateway/evals/(T-302 추출 평가 — 같은 모양으로), packages/contracts/{rules/sse-sequence.mjs, sse-events.md}, services/gateway/src/team/verification/number-check.ts(렌더 숫자 대조 — 평가기는 **다시 계산해** 확인한다), `.harness/reports/T-304.live.md`(실사용 조건), AGENTS.md §3·§5
- 허용 경로: `services/gateway/evals/`(새 `scenarios.jsonl`·`run-scenario-eval.ts`·채점 모듈 파일들), `services/gateway/package.json`(`eval:scenario` 스크립트 한 줄), `services/gateway/tests/`(평가기 자체 테스트)
- 인터페이스:
  - **시나리오 20개**(`scenarios.jsonl`, 사람이 읽을 수 있는 한 줄씩): 새 예보 12(유형 6종·지역 여러 곳 — **2026-01~06 자료가 필요한 인천 개편 지역·시작 10/18 이후 행사는 피한다**, 시작은 평가일 + 3~13일로 상대 날짜 표기), 빠진 필수값 되묻기 3(주최·유형·장소 모호), 위험 요소 2(폭죽·수면), 후속 3(why·save·draft — 앞 시나리오의 발행 세션에서), 범위 밖 2. 각 시나리오에 기대값: 되묻기 필드 목록·발행 여부·후속 의도.
  - **채점**(시나리오마다 + 합계): ① `sequenceProblems` 0(새 예보 `new`, 후속 `followup`) ② 발행 문장 100% 근거 1개 이상·보낸 `evidence`에 모두 있음 ③ **숫자 불일치 0** — 발행 문장의 `rendered`에서 숫자를 뽑아 스트림의 `forecast` 카드·`evidence` Quantity와 **평가기가 따로** 대조(게이트웨이 코드 재사용 금지 — 독립 검산), 인용 규칙 문구의 숫자는 예외 ④ `basis=구간`이면 % 0개 ⑤ 되묻기 적중(기대 필드 = 실제 `ask` 필드) ⑥ 후속 의도 적중 ⑦ 지연: 첫 요청 시작 → `forecast` 카드, → `done`(발행) p50·p95, 되묻기 왕복 제외 ⑧ LLM 호출 수(agent_step `usedLlm`).
  - **실행**: `npm -w services/gateway run eval:scenario -- --base http://127.0.0.1:<port> --out reports/evals/scenario-<날짜>.{json,md}` — 이미 떠 있는 게이트웨이(실제 forecast·knowledge·records·Ollama)에 붙는다. `--fake`면 계약 픽스처 기반 가짜 서비스로(테스트용 — 결과 파일에 "가짜" 표시).
  - **결과 md**: 합계 표(항목별 통과/전체), 실패 시나리오마다 원인 한 줄, 지연 표, 조건(모델·Ollama 적재 여부·실행 위치), "참고용 — 평가 시나리오는 개발팀이 만든 것" 고지.
- 수용 기준(실행 명령): `npm -w services/gateway test`(채점 함수 단위 테스트 — 숫자 검산이 불일치를 잡는 위반 스트림 픽스처, 순서 위반, 되묻기 불일치)·`build`·`lint`, `npm -w services/gateway run eval:scenario -- --fake` 가 20개를 돌려 결과 파일을 만든다(가짜 서비스에서 모두 통과). **실제 실행은 오케스트레이터**(워커 샌드박스는 소켓을 막는다) — 리포트에 재현 명령만.
- 범위 밖: 추출 평가 변경(T-302 그대로), 웹 S8 표시(T-410), what-if(T-309)
- 외부 호출: 로컬 서비스·Ollama만
- 코드 규칙: AGENTS.md §5

## 이어 하기 (9/25 13:30 사용자 요청으로 중단됐던 실행)
이전 실행이 도중에 멈췄다. **워크트리에 남은 변경을 먼저 읽고 이어서** 완성한다(처음부터 다시 쓰지 않는다 — 맞지 않는 부분만 고친다). 명세·수용 기준은 위 그대로.
