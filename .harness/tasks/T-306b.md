# T-306b: 계획 초안의 인력·조직 섹션 — 행안부 매뉴얼(H5) 항목을 체크리스트 규칙으로 발행해 `staffing`·`organization`을 채운다
- 레인/워커: L3 / gpt-6-astra (L2 설정·규칙 엔진, L6 기준 그래프, 계약 master-ids·labels까지 이 task 하나로 — 모두 astra 레인)        - 선행: T-306·T-306c 완료, H5 완료(`docs/research/04_행안부_지역축제장_안전관리_매뉴얼_2024_요약.md`)
- 목표(한 문장): 지금 항상 "검토 필요"인 계획 초안의 `staffing`·`organization` 섹션을, 매뉴얼이 요구하는 항목을 **자체 기준 체크리스트 규칙**(근거 = 매뉴얼 쪽수)으로 발행한 문장으로 채운다 — 요원 **인원 수는 계산하지 않는다**(매뉴얼에 비율이 없다 — p.46).
- 읽을 것: H5 요약 2·3·5절(**정본**), docs/plan/06 §6(체크리스트 규칙·"기준값이 없으면 숫자를 지어내지 않는다"), configs/checklist.yaml, services/forecast/src/crowdcast/rules/{checklist,judge}.py, services/knowledge/ontology/master/rules.ttl, packages/contracts/jsonld/{master-ids,master-labels}.json + `packages/contracts/scripts/master_labels.py`(`--check`), services/gateway/src/team/report/{templates,plan-templates,plan-writer}.ts, `.harness/tasks/T-306.md`(섹션 배치 규칙), AGENTS.md §3·§5
- 허용 경로: `configs/checklist.yaml`, `services/forecast/src/crowdcast/rules/`, `services/forecast/tests/rules/`, `services/knowledge/ontology/master/rules.ttl`, `services/knowledge/tests/`, `packages/contracts/jsonld/{master-ids,master-labels}.json`(규칙 추가만 — **CONTRACT-CHANGE 허용 범위**: 규칙 id 추가와 labels 재생성, 규칙 라벨에 선택 필드 `source`(문서 제목·쪽·URL) 추가까지), `packages/contracts/scripts/master_labels.py`(source 전달만), `services/gateway/src/team/report/`, `services/gateway/tests/`
- 인터페이스:
  - **새 규칙 6개**(kind `자체`, clause 없음, **조건 = 등급 2 이상**(순간 최대 1,000명 이상 예상 = 계획 수립 대상) — 체크리스트에 `level_min` 조건을 새로 지원, 판정이 등급을 정한 뒤 넘긴다). 문구에는 **숫자를 넣지 않는다**(숫자 검산에 걸리지 않게 — 쪽수·"1㎡당 4명"은 규칙 근거의 note·source에):
    - `rule-check-staff-plan` "안전관리요원 배치도와 구역별 인원·임무·책임자, 비상연락망 마련" (p.26·p.138) → staffing
    - `rule-check-staff-distinct` "행사진행요원과 안전관리요원 구분 표기" (p.26) → staffing
    - `rule-check-staff-focus` "요원 규모는 지자체·경찰·소방과 협의, 출입구·주차장·차량 이동로·종료 시 출구 집중 배치" (p.46·p.49~50) → staffing
    - `rule-check-org-chart` "안전관리 조직도·종합상황실·분야별 책임자 지정" (표준안 p.137) → organization
    - `rule-check-org-hq` "재난안전대책본부 구성과 유관기관 협조 체계" (표준안 p.142) → organization
    - `rule-check-capacity` "동시 최대 수용인원 산정(사람이 다니는 면적 × 면적당 인원 상한)과 초과 시 입장 제한·대기 장소" (표준안 p.134 — 1㎡당 4인 이하 권고) → routes-evacuation
    - 출처: 행정안전부 「지역축제장 안전관리 매뉴얼」(2024. 9.) https://www.mois.go.kr/frt/bbs/type001/commonSelectBoardArticle.do?bbsId=BBSMSTR_000000000015&nttId=113047
  - **초안 배치**: `plan-templates.ts`의 섹션 선택을 **규칙 id 우선**으로(권고 문장의 근거 → 판정 체크리스트 항목의 `ruleId`) — staff 규칙 → `staffing`, org → `organization`, capacity → `routes-evacuation`, 그다음 기존 키워드 규칙(지금 "주차장"이 들어간 staff 문장이 교통으로 가는 것을 막는다). 문장이 들어간 섹션은 `작성됨`, 없으면 지금처럼 `검토 필요`.
  - 기존 규칙·문구·예보 결과는 바뀌지 않는다(등급 1 행사는 새 항목 0개).
- 수용 기준: `pytest -q services/forecast/tests`·`services/knowledge/tests`·ruff, `npm -w services/gateway test`·build·lint, `python packages/contracts/scripts/master_labels.py --check`, `npm --prefix packages/contracts run check`. 테스트: 등급 1 → 새 항목 0 / 등급 2 → 6개·규칙 근거 연결, 초안 배치(staff 문장 → staffing, 주차 키워드에 끌려가지 않음), 새 문장이 게이트 B(근거·숫자 검산)를 통과, SHACL 기준 그래프 검증 통과. 실제 상담 → docx 확인은 오케스트레이터.
- 범위 밖: 요원 수 계산(매뉴얼에 비율 없음), 순간(1시간) 정의 차이 고지(사용자 결정 대기), 웹 근거 카드에 source 표시(T-411)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
