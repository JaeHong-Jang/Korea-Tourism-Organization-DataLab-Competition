# T-602: SHACL S01~S12 + 위반 픽스처 + /validate(스냅샷 범위) + /publish(원자적)
- 레인/워커: L6 / gpt-6-astra        - 선행: T-601 완료
- 목표(한 문장): 09 §5의 규칙 12개를 SHACL로 구현해 규칙마다 위반을 잡아내고, 세션 revision과 기준 그래프 버전(masterVersion)을 고정한 범위로 검증·발행하는 API를 만든다.
- 읽을 것: docs/plan/09 §0(SHACL은 구조만)·§1(검증 범위)·§2(IRI)·§5(S01~S12)·§7(발행 상태)·§10(API), packages/contracts/schemas/gate-report.schema.json, packages/contracts/openapi/knowledge.yaml, services/knowledge(T-601 결과)
- 허용 경로: `services/knowledge/ontology/shapes/`(규칙 하나당 파일 하나: `s01-claim-evidence.ttl` …), `services/knowledge/src/knowledge/{validate,api}/`, `services/knowledge/tests/`(위반 픽스처 `tests/fixtures/shapes/`)
- 인터페이스:
  - `POST /v1/sessions/{id}/validate?revision=&masterVersion=&shapes=` → 계약 `gate-report`(검사한 revision·masterVersion 기록). revision은 T-601의 **내용 revision**(문장 상태 전이로는 오르지 않는다) — S12는 문장 검사 revision = 지금 내용 revision을 본다. 검증 입력 = 해당 세션 named graph의 그 revision 스냅샷 + 그 masterVersion의 기준 그래프 + TBox. revision이나 masterVersion이 현재와 다르면 409
  - `POST /v1/sessions/{id}/publish?revision=&masterVersion=` → S12 통과 시 `cc:status "candidate"` → `"published"`(candidate만 — `rules/claim-lifecycle.mjs`의 `publishProblems`와 같은 판정, `fixtures-integrity/sequences/`의 발행 단계로 확인)를 한 트랜잭션으로 바꾸고 `gate-report`(새 revision) 반환. 실패면 위반 목록, 범위가 바뀌었으면 409
  - pySHACL `advanced=True`(SHACL-SPARQL 사용: S09·S12)
- 수용 기준(실행 명령):
  - `uv run --package crowdcast-knowledge pytest -q services/knowledge/tests` 통과: 규칙마다 **정상 그래프 통과 + 위반 픽스처 거부**(S09는 관측값 누락·날짜 형식 오류·미래 공개일·다른 행사 관측값 4종), revision 불일치·masterVersion 불일치 시 validate·publish 409, gate-report 응답이 계약 스키마 통과
  - 정상 그래프는 계약 픽스처(`fixtures/event/valid-yeongjong.json` — S09 지역 대조용, `fixtures/forecast/valid-yeongjong.json`, `fixtures/claim/valid-published.json`을 candidate로 바꾼 것)를 T-601 변환·적재로 만든다(손으로 쓴 TTL만으로 통과시키지 않는다)
  - 숫자 속성의 `sh:datatype`은 09 §2 숫자 리터럴 규칙(`xsd:double`·`xsd:integer`)을 따른다
  - 검증 시간: 세션 그래프 300트리플 기준 검증 시간을 측정해 리포트에 적는다
- 범위 밖: SPARQL 화면용 질의(T-603), 통계(T-604)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5 (TTL도 첫 줄 역할 주석, 규칙 하나 = 파일 하나)

## 게이트에서 함께 확인할 것 (R-02 Med — gpt-6-sol)
- `services/knowledge/src/knowledge/api/app.py:34` — `/facts`의 422 `gate-report`를 테스트에서만 검증하고 실제 반환 경로에서는 검증하지 않는다. 이 task의 `/validate`·`/publish` 응답과 함께, **모든 응답(오류 포함)을 반환 직전에 계약 스키마로 검증**하는 한 곳(예: `api/contract.py`의 응답 헬퍼)을 두고 `/facts` 422도 그 경로를 타게 한다.
