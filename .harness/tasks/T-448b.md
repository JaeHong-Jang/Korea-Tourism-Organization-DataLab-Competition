# T-448b: 발표치 규모 계층을 기존 "조건부 행사 속성" 규칙으로 — 211건 전부 4등급 해소
- 레인/워커: L2 / gpt-6-astra        - 선행: T-448(레인 브랜치 커밋 6208f60 — 발표치 계층·비교·미리보기 도구), 사용 모델 v1-064e60073a7411037212
- 배경: T-448은 발표치 계층에 `visitors_announced_available_at ≤ 기준일`을 요구했다(task 문구대로). 그런데 마스터에 공개일이 없어 **사용 0건** → 여전히 다가오는 211건 전부 4등급. 이 요구는 프로젝트 기존 규칙보다 엄격했다(오케스트레이터 task 오류): 기본 백테스트는 이미 **조건부 백테스트**(행사 속성이 주어졌을 때 — 공개 여부 미입증 속성 사용)이고 LightGBM은 `visitors_announced`를 그렇게 쓰며, 공개일 검사는 **파일명 날짜 민감도** 표가 따로 맡는다(backtest.md "참고: 파일명 날짜 기준 민감도").
- 오케스트레이터 사전 확인(메모리 실험, 파일 미기록): 발표치를 행사 속성으로 쓰면 다가오는 행사 계층 = 발표치 160·유형 51, 등급 1/2/3/4 = 16/9/49/137. 최하위(남이흥 장군 문화제 200명/2일, 구리 빛 축제 10,000명/69일)·최상위(마포나루 새우젓축제 820,000명/3일) 원값은 단위 오류가 아니라 실제 신고치로 보인다.
- 목표(한 문장): 발표치 계층의 사용 조건을 **다른 행사 속성과 같은 규칙**(조건부 = 주어진 속성, 파일명 민감도 = 기존 가림 규칙)으로 바꿔 백테스트에서 실제로 계층을 쓰게 하고, v1과 같은 폴드로 비교해 승격 규칙을 지키는지 판단한다.
- 읽을 것: T-448 커밋 diff(`git show 6208f60`), `.harness/reports/T-448.md`, `reports/backtest/bt-v2-a1bff30fb60c293e76f3/{comparison,backtest}.md`, models/{baselines,backtest,scale_compare,scale_preview}.py, features/{announced,build}.py, docs/plan/06 §5·§8·§11, AGENTS.md §3·§5
- 허용 경로: `services/forecast/src/crowdcast/models/`, `services/forecast/src/crowdcast/features/`, `configs/model.yaml`(키 추가만), `services/forecast/tests/models/`·`tests/features/`
- 인터페이스:
  - **사용 조건**: 발표치 계층은 해당 평가 정의에서 모델이 받는 `visitors_announced`를 그대로 따른다 — 조건부 백테스트·다가오는 예보는 주어진 값, 파일명 민감도는 기존 가림(날짜 없는 해·2020·2024 결측, 파일명 날짜 > 기준일 결측)을 거친 값. 별도 `visitors_announced_available_at` 요구는 없앤다(있으면 여전히 기준일 뒤 값 제외). 0·결측·기간 미상은 유형으로 대체(기존대로).
  - **비교**: `python -m crowdcast.models.scale_compare`(T-448 도구)로 v1 대비 **조건부와 파일명 민감도 두 표** 모두 MdAPE·80% 포함률(n)·판정 재현율·폭 중앙값, 계층 사용 N. 다가오는 211건 등급 분포(저장 예보 대비), 그리고 **순간 최대 p50 하위 10·상위 10 행사 표**(이름·발표치 원값·일수·보정 일평균·p10/p50/p90·등급·계층).
  - **안전 조건(추가)**: 판정 재현율(실제 대상 이상인데 대상 미만으로 예보 = 놓침)이 v1보다 떨어지면 조건부·민감도 어느 쪽이든 **보류**. 폭이 넓어지는 것은 허용(구간 포함률 기준 그대로).
  - **승격**: 기존 자동 게이트 + 위 안전 조건 통과 시 **리포트에 승격 후보로 제안만**(`promoted.json`·일괄 예보 재생성은 오케스트레이터가 9/26 자료 수집 뒤). 사용 모델 v1 예보 바이트 동일 유지.
  - 모델 카드 notes: "규모 계층: 발표치 사용 N건(조건부 행사 속성 — 개최계획서 전년 방문객 신고치, 공개일 미입증)" + 한계 한 줄(신고치 과장 가능·장기 행사는 일평균이 주말 최대를 낮춰 볼 수 있음).
- 수용 기준: `pytest -q services/forecast/tests/models services/forecast/tests/features`·ruff 통과, 테스트: 조건부에선 공개일 없어도 계층 사용 / 민감도 가림 뒤엔 미사용 / 기준일 뒤 공개일이면 미사용 / 재현율 하락 시 보류. 비교 1회 결과(두 표·등급 분포·하위/상위 표)를 리포트에. 전체 `pytest -q services/forecast/tests`가 샌드박스에서 멈추면 그 사실만 적는다(오케스트레이터가 재실행).
- 범위 밖: 임계값·환산 산식 변경, LightGBM 주 모델 전환(G0), 마스터·전처리 수정(L1)
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
