# T-447: 위치 기반 행사 찾기("진천에서 가까운 축제") + 팀장의 대화형 답(reply — 로컬 LLM, 숫자 없음 검사)
- 레인/워커: L3 / gpt-6-astra        - 선행: T-441 병합(recommend·eventId), 계약 303bcfd(SSE `reply`·메시지 `near`)
- 사용자 요청(9/25 밤): "그냥 축제 진천에서 가까운 곳 가고 싶은데"라고 하면 **가까운 곳**을 따져야 한다(GPS나 지명) — 지금 봇이 이상하다. 그리고 **AI가 동작한다는 느낌**이 없다 — 대화가 사람에게 말하듯.
- 읽을 것: packages/contracts/{sse-events.md(끝의 `reply`·`near` 절 — **정본**), schemas/sse-event.schema.json(`reply.text` 숫자 금지 pattern)}, services/gateway/src/team/lead/(분류·recommend·start), forecast `/v1/geocode`(지명 → 후보 좌표·시군구), 게이트웨이 Ollama 클라이언트·LLM 호출 상한(docs/plan/10 §4), AGENTS.md §3·§5
- 허용 경로: `services/gateway/src/`, `services/gateway/tests/`, `services/gateway/evals/`(시나리오 추가)
- 인터페이스:
  - **위치 조건**: 문장에서 지명("진천", "진천군", "부산 해운대", "강남역 근처")을 뽑아 `/v1/geocode`로 좌표(여러 후보면 가장 그럴듯한 시군구, 애매하면 되묻기 버튼), 또는 메시지 `near {lat,lng}`. "가까운/근처/주변" 또는 위치가 있으면 **거리 순 정렬**(행사 좌표와 대원거리), 기본 반경 60km(결과 0이면 120km로 넓힘), `reason`에 "진천군에서 약 N km"(숫자는 reason 문자열 — summary 값이 아니라 계산한 거리이므로 소수 없이 km, 계산식은 테스트로 고정). 지역 이름만 있고 "가까운"이 없으면 지금처럼 그 지역 안 필터.
  - **reply(팀장 말)**: 모든 응답(recommend·새 예보 발행·후속·오류 안내)에 `reply` 한 번 — 로컬 LLM(Ollama, 빠른 모델)에 **결과 요약용 사실 목록(행사 이름·등급 이름·조건 — 숫자 제외)**만 주고 "친근하게 두 문장 이하, 숫자 쓰지 말 것"으로 생성 → **검사**(숫자 0개·길이·행사 이름 외 고유명사 새로 만들지 않음·금지어(확정적 안전 보장 등)) 통과하면 `source:"llm"`, 아니면 규칙 문장(`template`). LLM 호출 상한에 포함(초과 시 template). 새 예보에서는 발행 뒤 `done` 전에.
  - 분류 보강: "가까운/근처/주변/여기서" + 지명 → recommend(주최자 문장보다 우선하지 않음 — "열어요" 등이 있으면 주최자).
- 수용 기준: `npm -w services/gateway test`·build·lint, 테스트: 지명 추출·지오코딩(가짜)·거리 정렬·반경 넓히기·near 우선·reason 거리 문구, reply 생성(가짜 LLM — 숫자 섞인 답 → template 대체, 정상 → llm)·모든 모드에 reply 한 번·순서 규칙 통과·LLM 상한, `evals/scenarios.jsonl`에 위치 추천 2개. 실제 Ollama 실행은 오케스트레이터.
- 범위 밖: 화면(T-446)
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
