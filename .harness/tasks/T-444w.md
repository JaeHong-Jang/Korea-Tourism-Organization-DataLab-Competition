# T-444w: "근거 그래프" 화면 — 전체 온톨로지·기준 그래프 시각화(React Flow + elkjs), 검색·종류 필터·노드 상세
- 레인/워커: L4d / gpt-6-sol        - 선행: T-413(근거 지도 — React Flow·elk 방식), 계약 6051257(`knowledge-graph`·gateway `/api/evidence/graph`), knowledge T-444·게이트웨이 T-441 병행(이 task는 계약·가짜 응답으로 만든다)
- 사용자 요청(9/25 밤): "온톨로지 그래프 시각화는 어디 갔어?"
- 읽을 것: apps/web/src/features/evidence-map/(T-413 — 노드 모양·elk 배치·표로 보기·키보드), packages/contracts/schemas/knowledge-graph.schema.json, apps/web/src/{app/app.tsx(라우트·메뉴), pages/validation-page.tsx}, docs/plan/09(무엇이 무엇을 근거로 하나 — 설명 문구), docs/plan/04 §3·§13, AGENTS.md §3·§5
- 허용 경로: `apps/web/src/features/knowledge-graph/`(새), `apps/web/src/pages/knowledge-graph-page.tsx`(새), `apps/web/src/app/`(라우트 `/graph` + 검증 화면에서 들어가는 링크, 메뉴에 "근거 그래프" — 메뉴가 너무 많으면 검증 메뉴의 두 번째 탭으로), `apps/web/src/features/evidence-map/`(예보서 근거 지도 → "전체 근거 그래프 보기" 링크 한 줄), `apps/web/src/styles/`, `tests/e2e/`(새 `graph.spec.ts`)
- 인터페이스:
  - `GET /api/evidence/graph`(계약 검증) → 종류별 모양·색 토큰(클래스·규칙·조항·데이터셋·가정·모델·단계·파일·에이전트) 노드, 간선 라벨(한국어), elk 계층 배치(클래스 층 → 개체 층), 처음엔 **클래스 + 규칙·조항·데이터셋·모델**만 보이고 종류 칩으로 켜고 끄기, 검색(두 글자부터 — 노드로 이동·강조), 노드 클릭 → 오른쪽 상세(이름·종류·설명·원문 링크·연결된 노드 목록), 연결 경로 강조, [표로 보기].
  - 머리 설명 한 줄: "이 서비스가 쓰는 규칙·법령·데이터·모델이 어떻게 이어지는지 보여 줘요 — 예보서의 모든 문장은 이 그래프의 근거에 닿아요". masterVersion·생성 시각 표시.
  - 성능: 노드 600까지 60fps 이동(가상화·배치 1회 메모), `prefers-reduced-motion`.
- 수용 기준: `npm -w apps/web test`(그래프 → 노드·간선 변환·필터·검색)·build·lint·색 하드코딩 0, e2e `graph.spec.ts`(가짜 API: 화면 → 종류 필터 → 검색 → 노드 상세 → 원문 링크, 표로 보기) + 스크린샷 `reports/figures/screens/T-444-{graph,detail}.png`. 실행은 오케스트레이터.
- 범위 밖: knowledge·게이트웨이(T-444·T-441)
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
