# T-430b: 예보팀 펫 13마리를 고래로 — 사용자가 준 고래 그림을 바탕으로 공통 몸통을 다시 그린다
- 레인/워커: **L4d**(새 레인 — `components/pets/`만, L4c는 T-433b 중이라 겹치지 않게) / gpt-6-sol        - 선행: T-430 완료(펫 13종·상태 5종)
- 목표(한 문장): 사용자가 고른 고래 그림(`docs/reference/assets/whale-mark.png`, 70×61)을 바탕으로 예보팀 펫의 **공통 몸통을 고래로 다시 그려** 13마리 모두를 고래 가족으로 바꾸고, 팀 색·역할 표식·상태 5종(대기·작업·완료·보류·오류)과 쓰는 곳의 인터페이스는 그대로 둔다.
- 그림 설명(원본 PNG는 작다 — 이 설명과 함께 파일을 보고 **SVG로 새로 그린다**, PNG를 그대로 붙이지 않는다): 둥근 머리가 큰 고래 한 마리가 오른쪽을 보고, 꼬리가 몸 뒤(왼쪽 위)로 말려 올라가 있다. 몸통은 파랑 계열, 배 아래로 청록·노랑·주황의 둥근 물결 띠가 겹쳐 받치고, 머리 위에 작은 물 분수가 있다. 선이 굵고 단순한 평면 일러스트(그라데이션 없음).
- 읽을 것: docs/plan/04 §7(예보팀 캐릭터 — **정본**, 크기 32·48·96, 상태 5개)·§3(팀 색 토큰 — 검증팀 초록은 이름표와 함께)·§13(AI 티 금지), apps/web/src/components/pets/(T-430 — `pet-body.tsx`·역할 13개·`pets.css`·`__tests__`), packages/contracts/design-tokens.css(`--team-*`·펫 관련 토큰), apps/web/src/features/team-board/(쓰는 곳 — 바꾸지 않는다), AGENTS.md §3·§5
- 허용 경로: `apps/web/src/components/pets/`(몸통·표정 SVG·CSS·테스트), `apps/web/src/pages/dev-page.tsx`는 **건드리지 않는다**, `tests/e2e/`(펫 견본 스크린샷 한 곳), `packages/contracts/design-tokens.css`는 건드리지 않는다(새 색이 필요하면 리포트에 요청 — 기존 토큰으로 그린다)
- 인터페이스:
  - `PetBody({team, state, mark, baton, size})`의 **props·내보내기 그대로**(역할 13개 파일·작업판·빈 화면이 그대로 동작). viewBox 96×96 유지.
  - 고래 몸통 = 팀 색(`--team-*` — 팀장 남색·분석 파랑·검증 초록·보고 주황), 배의 **역할 표식(`mark`)은 지금처럼 가슴(배) 위치**에 크게 보이게, 물결 받침은 모든 펫 공통(청록·노랑·주황 — 기존 토큰 중 가까운 것), 팀장은 지휘봉(`baton`) 대신 머리 위 작은 깃발이나 지휘봉을 고래에 맞게.
  - 상태 5종: 대기 = 깜빡임, 작업 = 살짝 통통 + **물 분수가 뿜어짐**, 완료 = 눈웃음, 보류 = **지느러미를 들어** 손 들기(지금의 손 들기 자리), 오류 = 엑스 눈. `prefers-reduced-motion`이면 움직임 없이 표정만.
  - 32px에서도 역할 표식·표정이 알아볼 수 있게(선 굵기·단순화), 낮·밤 테마 모두.
- 수용 기준: `npm -w apps/web test`(기존 펫 테스트 + 13역할 × 5상태 렌더·팀 색·표식), `build`·`lint`·색 하드코딩 0(`grep -rnE "#[0-9a-fA-F]{3,6}\b|rgb\(" apps/web/src/components/pets` — 토큰 외 0), e2e로 펫 견본 화면 스크린샷 `reports/figures/screens/T-430b-pets-{day,night}.png`(T-430과 같은 견본 경로)와 상담 작업판 한 장 `T-430b-board.png`. 실행은 오케스트레이터(워커는 작성·구문 확인).
- 범위 밖: 헤더 로고(사용자 확인 전 — 바꾸지 않는다), 3D 장면 속 고래 봇(T-434a)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5
- **구현 우선 모드**: 게이트는 테스트·빌드·lint만, 화면 검토는 마지막 전체 QA에서.
