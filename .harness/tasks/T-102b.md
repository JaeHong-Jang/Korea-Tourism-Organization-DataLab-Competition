# T-102b: 지명 사전 빌드 속도 — 첫 geocode 25초를 1초 안으로(결과는 바이트 그대로)
- 레인/워커: L1 / gpt-6-astra        - 선행: T-102 완료(행사 마스터·geocode·경계 — develop)
- 목표(한 문장): `default_gazetteer()` 빌드가 25.7초(리눅스 경로에서도 같음)라 forecast를 띄운 뒤 첫 `/v1/geocode`가 게이트웨이 동네지기 예산(8초)을 넘어 첫 상담이 항상 `DEADLINE_EXCEEDED`로 끝난다 — **후보 결과는 한 글자도 바꾸지 않고** 빌드를 3초 안, 첫 geocode를 2초 안으로 줄인다.
- 원인(오케스트레이터 프로파일, 9/25): `Gazetteer.__init__`가 축제 장소 6,180건마다 `lookup_admin`을 부르고, `lookup_admin`이 매번 `admin.to_dicts()`(도형 WKB 포함, 누적 2.9초)와 별칭마다 `re.search(rf"(?<![가-힣]){re.escape(alias)}(?![가-힣])", raw)`를 새로 만든다 → 정규식 컴파일 108,607회(파이썬 `re` 캐시 512개가 계속 밀려남), `_optimize_charset` 21.4초. 전체 31.9초(cProfile).
- 읽을 것: services/forecast/src/crowdcast/data/{admin_dict,geocode,events}.py, 같은 이름의 tests/data/, AGENTS.md §3·§5
- 허용 경로: `services/forecast/src/crowdcast/data/admin_dict.py`, `services/forecast/src/crowdcast/data/geocode.py`, `services/forecast/tests/data/`
- 인터페이스: 공개 함수 이름·인자·반환(`lookup_admin`·`resolve_admin`·`candidates`·`Gazetteer`)은 그대로. 행정 사전 행·별칭 패턴을 **사전 프레임당 한 번** 만들어 재사용한다(예: 프레임 → (행 목록, 별칭별 컴파일된 패턴) 캐시, 또는 `Gazetteer`가 준비한 행을 넘기는 선택 인자). 도형 WKB는 이름 조회에 쓰지 않는다.
- 수용 기준(실행 명령):
  - `uv run --package crowdcast-forecast pytest -q services/forecast/tests`(전체) 통과, `ruff check`
  - **동등성 테스트**: 실제 `data/processed`(없으면 건너뜀)에서 바꾸기 전 구현(테스트 안에 옛 함수를 그대로 옮겨 둔다)과 새 구현의 `Gazetteer.venues`·`lookup_admin` 결과가 **같다** — 축제 장소 전부 + 행사 마스터 venue 전부
  - 시간: 실제 `data/processed`로 `default_gazetteer()` 빌드 ≤ 3초, 빌드 뒤 `candidates("인천 남동구 소래포구 해오름광장")` ≤ 0.1초 — 측정값을 리포트에
  - `python -m crowdcast.data.events`를 돌리지 않는다(9/26 수집 전 — 산출물 해시가 바뀌면 안 된다). 대신 `make_event` 경로의 단위 테스트 통과
- 범위 밖: 후보 점수·규칙 변경, 앱 시작 시 미리 데우기(L2 `api/`), 새 지명 자료
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5 (파일 맨 위 역할 한 줄 + 문단마다 한국어 한 줄 주석, 300줄 넘으면 분리)
