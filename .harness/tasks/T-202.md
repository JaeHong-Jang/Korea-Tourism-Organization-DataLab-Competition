# T-202: 순간 최대 환산(샘플링) + 판정 엔진 + 규모 외 위험 체크리스트 + 근거 조각
- 레인/워커: L2 / gpt-6-astra        - 선행: T-201 완료
- 목표(한 문장): 일평균 예측 분포에서 순간 최대 샘플을 만들고(가정 2개), 법정·자체 규칙으로 4등급을 판정하며, 모든 판정·가정에 근거 조각을 붙인다.
- 읽을 것: docs/plan/06 §4(피크일 계수·동시체류율)·§5(최종 분포 하나에서 모든 숫자)·§6(판정·체크리스트)·§12(근거 조각), docs/plan/09 §5(S05·S06·S07), packages/contracts/schemas/{forecast,judgment,evidence,common}.schema.json
- 허용 경로: `services/forecast/src/crowdcast/rules/{__init__.py,peak.py,judge.py,checklist.py,evidence.py}`, `configs/{peak_profiles.yaml,thresholds.yaml,checklist.yaml}`, `services/forecast/tests/rules/`
- 인터페이스:
  - `peak.sample_peak(daily_quantiles, event, n=4000, seed)` → 순간 최대 샘플(피크일 계수 × 동시체류율, 둘 다 범위 샘플링) + 사용한 가정 id 목록(기준 그래프 id 그대로 — `packages/contracts/jsonld/master-ids.json`의 `as-peak-day-factor`, `as-concurrency-<유형>`: 불꽃 fireworks·공연 performance·대학 university·먹거리 food·꽃 flower·전통 traditional·기타 other)
  - `judge.judge(samples, event)` → 계약 `judgment`(level·label·ruleIds·reasons·checklist — 법정 사유는 `clauseId` = `law-disaster-act-enf-73-9` 필수, 규칙 id는 `rule-legal-1000`·`rule-legal-hazard`·`rule-internal-*`) + P(≥1000)·P(≥5000). 불·폭죽·가연성·산·수면 → 인원 무관 "수립 대상"(법정). 사유 문구는 템플릿 고정
  - `evidence.rule_evidence(...)`, `evidence.assumption_evidence(...)` → 계약 `evidence`(kind rule·assumption, id는 `ev-` + 입력 해시로 결정적, 필수 필드는 스키마대로 전부 — 해당 없는 칸은 null, `checkResult`는 null)
  - 설정: `configs/*.yaml`(값 옆에 출처·가정 여부 주석)
- 수용 기준(실행 명령):
  - `uv run --package crowdcast-forecast pytest -q services/forecast/tests/rules` 통과(규칙·조항·가정 id는 모두 `master-ids.json`에 있어야 한다): 불꽃 = 인원 무관 대상, p10 ≤ p50 ≤ p90, p50 > 1,000이면 P(≥1000) ≥ 0.5, 같은 seed면 같은 결과, 판정·근거 출력이 계약 스키마 통과
- 범위 밖: 모델 학습(T-203), API 라우트(T-204)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5

## BLOCKED 해소 1회차 (오케스트레이터, 2026-09-24) — 워크트리의 1차 결과(peak·evidence·설정 2개) 위에서 이어서 한다
- 체크리스트 규칙 7개를 기준 그래프에 등록했다(`packages/contracts/jsonld/master-ids.json`, `services/knowledge/ontology/master/rules.ttl`): `rule-check-vehicle`·`rule-check-water-slope`·`rule-check-night-lighting`·`rule-check-fire`·`rule-check-single-exit`·`rule-check-stage-crowd`·`rule-check-rain-shelter`. 켜는 조건은 docs/plan/06 §6 표 — `configs/checklist.yaml`에 id·문구·조건을 그대로 옮긴다(조건 키는 event 필드와 날씨 필드 이름 그대로).
- 우천 입력: 계약 `weather`(pop·pty·source)를 쓴다. 인터페이스를 `judge.judge(samples, event, weather=None)`, `checklist.build(event, weather=None)`로 한다. `weather`가 None이거나 `source`가 `없음`이면 우천 항목을 켠다(모르면 대비). 계약은 바꾸지 않는다.
- 대규모(등급 4)면 `rule-internal-5000`의 "교통·경찰·소방 사전 협의" 항목을 더한다(계약 픽스처 `ck-traffic`과 같다).
- 남은 것: `judge.py`·`checklist.py`·`configs/checklist.yaml`과 판정 통합 테스트(원래 수용 기준 전부) — 불꽃 = 인원 무관 대상, p10 ≤ p50 ≤ p90, P(≥1000) 조건, 같은 seed 같은 결과, 판정·근거 출력이 계약 스키마 통과, 체크리스트 규칙 id가 모두 master-ids.json에 있음.

## 게이트 피드백 1회차 (게이트 3 교차 리뷰 FAIL — gpt-6-sol, 오케스트레이터 게이트 4도 같은 지적) — 워크트리에 결과가 있다
1. [High] `rules/judge.py:33` — 표시 문구 반올림 때문에 4,000개 표본 중 1개만 넘어도 `0%`, 3,999개가 넘어도 `100%`로 나온다(영종 불꽃 예시도 `100%`). 추정값을 확정처럼 보이게 한다. → 판정에는 원래 확률을 그대로 쓰고, `display`는 경계를 보존한다: 0 < p < 0.001이면 `0.1% 미만`, 0.999 < p < 1이면 `99.9% 초과`, 표본이 모두 넘거나 모두 못 넘으면 `거의 확실(표본 4,000개 모두)`처럼 표본 기준임을 밝힌다. 나머지는 소수 첫째 자리(`98.5%`). 경계 테스트.
2. [Med] `rules/checklist.py:31` — 계약상 유효한 날씨라도 `pop`·`pty`가 모두 null이고 `source`만 `중기예보`면 우천 점검이 꺼진다(06 §6 "날씨를 모르면 켬"과 다름). → 강수 정보(`pop`·`pty`)가 둘 다 없거나, 날씨 시각(`at`)과 행사 시작이 예보 유효 범위(단기 3일·중기 10일) 밖이면 켠다. 테스트.

## 게이트 피드백 2회차 (게이트 3 재리뷰 FAIL — gpt-6-sol) — 마지막 재디스패치
1회차 High·Med는 반례로 해결 확인(1/4,000·3,999/4,000 표시, 강수 null·유효 범위 밖 우천 켬).
1. [High] `rules/judge.py:43` — 반올림 때문에 399/4,000(9.975%)이 `10.0%`인데 등급은 소규모, 1,999/4,000(49.975%)이 `50.0%`인데 수립 권고로 나온다(표시가 등급 경계와 모순). → 표시 문구는 **등급을 가르는 경계(10%·50%)를 넘지 않게** 만든다: 경계 바로 아래는 `10% 미만`·`50% 미만`, 경계 이상은 `10% 이상`처럼 방향을 보존하거나 반올림 대신 내림/올림을 경계 쪽으로 고른다. 테스트: 1,000명·5,000명 두 임계값 × 경계 직전·직후 표본 수에서 "표시값을 다시 읽으면 같은 등급"인지.
