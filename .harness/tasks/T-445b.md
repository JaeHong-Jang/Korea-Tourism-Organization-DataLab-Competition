# T-445b: 살아 있는 도시 다듬기 — 이름표 정리(주요 장소만), 사람·차가 보이는 크기, e2e 3개
- 레인/워커: L4c / gpt-6-sol        - 선행: T-445 병합
- 오케스트레이터 확인(`reports/figures/screens/T-445-seoul-day.png`): 크림색 3D 건물은 성남 참고처럼 잘 선다. 그러나 ① 작은 가게·시설 이름(분홍·빨강 POI)이 화면 전체에 빽빽해 지저분하다 ② z15에서 **사람·차가 거의 안 보인다**(너무 작음) ③ e2e 실패 3개: `s1-3d-map.spec.ts` "기본 3D 지도에서 서울·부산 건물과 행사·차량을 본다"(행사 선택 뒤 요약에 "견본 행사 2"가 안 나옴), `app-shell.spec.ts` "home day/night 화면"(요소 없음 — 오류 문맥은 이 워크트리 `apps/web/test-results/`).
- 참고: 성남 `/mnt/c/Users/User/Desktop/대학교/3학년/KAIST 성남 시 공모전/-2026-2nd-Seongnam-KAIST-AI-Competition/src/lib/diorama/city-life.ts`의 `peopleScale`(사람 크기를 도로 폭과 따로 — 보이게 키움)·색 대비, 성남 화면처럼 이름표는 적게(주요 장소 카드 몇 개).
- 허용 경로: `apps/web/src/features/map-3d/`, `apps/web/src/features/map-2d/map-style.ts`, `apps/web/src/styles/`(지도), `tests/e2e/`(s1-3d-map·app-shell)
- 고칠 것:
  1. **이름표**: 기본 POI 레이어는 숨기고, 동네 이름(구·동)은 옅게 줌 조건으로, 장소 카드는 역·큰 공원·랜드마크만(최대 12) + 행사 카드(우선). 겹치면 행사 카드가 이긴다.
  2. **사람·차 보이게**: 사람 크기를 줌에 맞춰 키우고(z15에서 화면 약 4~6px 이상 — 성남 peopleScale처럼 실제 축척과 따로, 범례에 "사람 크기는 보이게 키움"), 옷 색 대비를 높이고(크림색 건물·회색 도로 위에서 잘 보이게), 차량도 같은 원칙. 행사장 둘레 집결이 한눈에 보이게.
  3. **e2e 3개** 고치기(원인이 앱이면 앱을, 테스트가 옛 화면이면 테스트를).
- 수용 기준: `npm -w apps/web test`·build·lint·색 하드코딩 0, e2e는 오케스트레이터(스크린샷 `T-445b-{seoul-day,festival}.png`), 성능 `--t440` p95 ≤ 1.0배 유지.
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
