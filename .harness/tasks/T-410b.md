# T-410b: S8 평가 상세 — 평가 결과의 항목별 통과/전체·지연·실행 모드, 사용 모델 검증 상태(선택 필드 계약 반영)
- 레인/워커: **L5 워크트리** / gpt-6-sol (게이트웨이 eval-reader·ops 라우트 + 평가 실행기 + 웹 S8 — 모두 TS)        - 선행: T-410·T-308 완료, 계약(`ops-status` 선택 필드 `evals.checks`·`evals.latencySeconds`·`evals.mode`·`model.verdict`)
- 목표(한 문장): S8 "평가 결과" 카드가 근거 없는 발행·숫자 불일치 합계뿐 아니라 **항목별 통과/전체**(순서·근거·숫자·구간·되묻기·의도·발행·실행)와 **지연 p50·p95**, 실제/가짜 실행 구분을 보이고, "최신성" 카드에 사용 모델 **검증 상태**(통과·미검증)를 보이게 한다.
- 읽을 것: packages/contracts/schemas/ops-status.schema.json(선택 필드 설명 — **정본**), `reports/evals/scenario-2026-09-25-fake.json`(summary 모양 — checks·latency), services/gateway/{src/clients/eval-reader.ts(지금 `reports/evals/latest.json`을 읽음 — 파일이 없다), src/routes/(ops 상태 라우트), evals/run-scenario-eval.ts·run-extract-eval.ts(결과 파일 쓰는 곳)}, `reports/backtest/promoted.json`(verdict), apps/web/src/features/ops/(T-410 카드), AGENTS.md §3·§5
- 허용 경로: `services/gateway/src/{clients,routes}/`(ops·평가 읽기 부분만), `services/gateway/evals/`(결과 쓰는 곳 — latest 파일 갱신만), `services/gateway/tests/`, `apps/web/src/features/ops/`, `apps/web/src/lib/ops-api.ts`, `tests/e2e/s8.spec.ts`
- 인터페이스:
  - **평가 실행기**: 시나리오 평가가 끝날 때 `reports/evals/latest.json`을 원자적으로 갱신(그 실행의 결과 그대로 + `mode`: `--fake`면 fake). 추출 평가는 건드리지 않는다(요약은 시나리오 평가 하나).
  - **게이트웨이**: `latest.json` → `evalSummary`(기존 필수 필드 + `checks`=summary.checks, `latencySeconds`=summary.latency.forecast·publishedDone, `mode`) — 파일 없거나 모양이 다르면 `evals: null`(오류 아님). `model.verdict` = promoted.json의 verdict(없으면 생략). 응답 계약 검증.
  - **웹 S8**: 평가 카드에 항목별 표(항목 이름은 한국어 라벨 — 순서 규칙·근거 연결·숫자 일치·구간 표시·되묻기·후속 의도·발행·실행), 지연 두 줄(초, 소수 1자리), "가짜 서비스 실행" 배지(fake일 때 — 서식4 숫자로 쓰지 말라는 안내 한 줄), 최신성 카드에 검증 상태 배지(미검증이면 06 §8 문구 "골든 사례 0건 — 사례 재현 검증 전 임시 사용").
- 수용 기준: `npm -w services/gateway test`·build·lint, `npm -w apps/web test`·build·lint·색 하드코딩 0, 테스트: latest 없음 → null·있음 → 선택 필드 채움·fake 표시·verdict. e2e `s8.spec.ts` 보강(가짜 API). 실행은 오케스트레이터.
- 범위 밖: 평가 자체 변경, 새 평가 항목
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
