# T-402: UI 키트(도메인 컴포넌트) + format.ts + 빈 화면·오류 화면에 펫 연결
- 레인/워커: L4a / gpt-6-sol        - 선행: T-401, T-430 완료
- 목표(한 문장): 04 §5의 도메인 컴포넌트를 토큰만으로 만들고, 숫자·날짜 표기 규칙을 한 곳(`format.ts`)에 모은다.
- 읽을 것: docs/plan/04 §3·§5·§6·§8·§11·§13, docs/plan/11 화면 구분 원칙, packages/contracts/generated/ts(타입), packages/contracts/fixtures(견본 데이터)
- 허용 경로: `apps/web/src/components/common/`(컴포넌트 하나당 파일 하나), `apps/web/src/components/charts/range-bar.tsx`, `apps/web/src/lib/format.ts`, `apps/web/src/pages/dev/`(견본 페이지 `/dev/kit`), 각 폴더의 `__tests__/`
- 인터페이스: `LevelBadge` · `RangeBar`(log 축, 1,000 기준선, 주최측 예상 ▲) · `KeyNumber` · `EvidenceChip` · `EvidenceCard`(6종) · `FestivalCard`(compact/regular) · `EventChipCard` · `AskButtons` · `FactorList` · `SimilarEventCard` · `InsightCard` · `KpiTile` · `ChecklistItem` · `SourceTip` · `EmptyState`·`ErrorState`(T-430 펫 사용) · `format.ts`(`formatPeople` "2.1만 명"/"1,450명", `formatRange`, `formatDate` "10.18(토) 19:00", `formatPercent` 정수·구간)
  - 모든 컴포넌트는 계약 타입을 받고, 로딩·빈 값·오류 상태를 가진다. 숫자는 받은 값을 그대로 표시(계산 금지)
- 수용 기준(실행 명령):
  - `npm -w apps/web test` 통과(format 규칙 표 테스트, 컴포넌트마다 렌더·상태·접근성 이름)
  - `/dev/kit`을 낮·밤 1366×768로 찍어 `reports/figures/screens/T-402-kit-<day|night>.png`
  - 색 하드코딩 0, lint 통과
- 범위 밖: 화면 조립(T-404~T-407), 3D
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5

## T-401에서 이월 (게이트 3 교차 리뷰 Med 4 + 게이트 4 오케스트레이터 의견) — 이 task에서 함께 고친다
1. [Med] `src/lib/theme/theme-provider.tsx:50` — `window.location.search`는 라우터 이동을 구독하지 않아 `?theme`·`?at`가 바뀌어도 최대 1분간 이전 테마가 남는다 → `useLocation().search`로 바꾼다.
2. [Med] `src/styles/responsive.css:49` — 모바일에서 장면 안내 문구("인원 규모는 예보값 비례 · 움직임은 연출")가 필터 패널 뒤에 가려진다 → 겹치지 않게 두고 모바일 스크린샷으로 확인.
3. [Med] `src/styles/base.css:27`, `components.css:39` — 밤 테마에서 `.eyebrow`·블록 ID 글자색(`--brand`) 대비가 2.9~3.6:1 → 작은 강조 글자는 `--link`(밤 대비 4.5:1 이상).
4. [Med] `src/lib/api-client.ts:11` — 응답을 `as T`로만 단언한다 → 계약 스키마(Ajv 2020, `@crowdcast/contracts/schemas/*`)로 검증하고 불일치는 오류로 돌려준다(AGENTS §5-3).
5. [게이트 4] 사용자 화면에 명세 코드(`M1-F2`, `S2 ·`, `S3 · DEMO`)가 보인다 → 화면에서는 숨기고 `data-feature="M1-F2"` 속성으로만 남긴다(개발 모드 `?debug=1`에서만 표시). 04 §13 "사람이 만든 것처럼".
- 의존성 설치(오케스트레이터 추가): 레포 루트에서 `npm install -w apps/web <패키지>`로 한다. 그 명령이 바꾸는 **루트 `package-lock.json`만 허용**(`apps/web/package-lock.json`처럼 따로 lock을 만들지 않는다).
- 펫(`apps/web/src/components/pets/`, T-430 완료)은 가져다 쓰기만 하고 고치지 않는다. 고칠 점은 리포트의 `CONTRACT-CHANGE:`처럼 `PET-CHANGE:`로 적는다.

## 게이트 피드백 1회차 (게이트 3 교차 리뷰 FAIL — gpt-6-astra, 오케스트레이터 게이트 4 지적 포함) — 워크트리에 결과가 있다
1. [High] `components/charts/range-bar.tsx:21` — `hostExpected`를 숫자만 받아 집계 단위를 잃는다. 견본의 주최측 2,000명은 **기간누적**인데 **순간 최대** 예측과 같은 축에 ▲로 찍힌다(단위가 다른 숫자를 한 축에서 비교하게 함 — 04 §13·정직한 표시 위반). → 계약 수치 객체(`quantity`: unit·timeUnit·spatialScope·valueKind·estimated)를 받아 **timeUnit·spatialScope가 같을 때만** 축에 표시, 다르면 축 밖에 "주최측 예상 2,000명(기간 누적) — 단위가 달라 직접 비교하지 않아요"를 적는다.
2. [Med] 단위 보존 — `insight-card.tsx:22`·`key-number.tsx:32`·`event-chip-card.tsx:29`가 `명/일`을 `formatPeople()`로 보내 `/일`이 사라진다 → `format.ts`에 `formatQuantity(quantity)`(값·구간·단위·시간 단위·"추정" 표기를 한 번에)를 두고 모든 수치 표시가 그것을 쓴다.
3. [Med] `range-bar.tsx:79` — "기준 1,000명" 라벨이 기준선이 아니라 오른쪽 끝에 붙는다, 축 눈금·로그 축 표시·툴팁이 없다 → 기준선과 라벨을 같은 x 좌표에, 로그 눈금(100·1천·1만·10만, 단위 포함)과 각 표시(구간·중앙·▲·기준선)의 호버 툴팁(dataviz 규칙: 대상보다 큰 hit 영역, 표 보기 유지).
4. [Med] 추정 표시 — `similar-event-card.tsx:32`가 `measured.estimated=true`여도 "실측"으로만 쓴다, RangeBar·FestivalCard mini 구간에 순간 최대 "추정 산식 기반"이 빠진다 → `estimated`면 "추정"을 붙이고 순간 최대에는 항상 "추정 산식 기반".
5. [Med] `evidence-card.tsx:55` — 6종 카드가 공통 요약·id만 보여 준다 → 04 §8대로 종류별 본문: 데이터(출처·메뉴·기간·공개일·값과 단위), 모델(버전·학습 범위), 규정(규칙·조항·법정/자체), 사례(과거 행사·실측/발표·단위 비교 가능 여부), 가정(값·범위·근거), 검증(검사 종류·통과·revision). 테스트는 종류별 **유효한 계약 견본**(Ajv 통과)으로.
6. [Med] 근거 연결 — `insight-card.tsx:33`·SimilarEventCard·FactorList·ChecklistItem이 근거를 원시 id 문자열로 출력한다 → 발행된 근거와 번호를 연결한 `EvidenceChip`으로(누르면 카드), 숫자 옆 ⓘ(`SourceTip`)로 출처.
7. [Low] `pages/mini-korea-page.tsx:56` — `data-feature` 속성까지 debug 조건 안에 있다 → 속성은 항상, 코드 텍스트만 `?debug=1`에서.

## 게이트 피드백 2회차 (재리뷰 PASS의 Med — 차트 정확성, 마지막 재디스패치)
재리뷰는 PASS(단위 불일치 ▲ 차단 확인)지만, 서식4 캡처에 들어갈 차트라 좌표 오류를 지금 고친다.
1. [Med] `kit.css:22` — 폭 20px 버튼을 절반 이동시키면서 선을 `border-left`로 그려 **중앙선·기준선이 실제 좌표보다 10px 왼쪽**에 보인다(스크린샷에서도 보임) → 클릭 영역(버튼)은 유지하고 선은 버튼 **중앙**에(예: `::before`로 `left:50%`). 좌표 테스트: 기준선 요소의 중심 x = 로그 축 좌표(±1px).
2. [Med] `range-bar.tsx:61` — ▲ 좌표는 `value`, 툴팁·표는 `formatQuantity()`가 고른 `p50` → `value=2000, p50=5000`이면 2,000 위치에 5,000명으로 표시. → 대표값 하나(`representativeValue(quantity)` — value 있으면 value, 없으면 p50)를 좌표·툴팁·표에 똑같이 쓴다.
3. [Med] `similar-event-card.tsx:61` — 사례 칩 번호가 `1`로 고정, `/dev/kit`의 `[4]` 대상 카드(`ev-rule-internal-5000`)가 없어 눌러도 안 열린다 → 발행 근거의 **공통 번호표**(예보서 단위 `evidenceNumber(id)`)를 모든 칩이 쓰고, 참조된 카드는 모두 렌더.
4. [Low] `insight-card.tsx:45` 표본 수·`FactorList` 기여도·`KpiTile` 접근성 이름도 공통 포맷터로.
- 스크린샷 3장 다시 찍기.
