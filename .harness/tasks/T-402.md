# T-402: UI 키트(도메인 컴포넌트) + format.ts + 빈 화면·오류 화면에 펫 연결
- 레인/워커: L4a / gpt-6-sol        - 선행: T-401, T-430 완료
- 목표(한 문장): 04 §5의 도메인 컴포넌트를 토큰만으로 만들고, 숫자·날짜 표기 규칙을 한 곳(`format.ts`)에 모은다.
- 읽을 것: docs/plan/04 §3·§5·§6·§8·§11·§13, docs/plan/11 화면 구분 원칙, packages/contracts/generated/ts(타입), packages/contracts/fixtures(견본 데이터)
- 허용 경로: `apps/web/src/components/common/`(컴포넌트 하나당 파일 하나), `apps/web/src/components/charts/range-bar.tsx`, `apps/web/src/lib/format.ts`, `apps/web/src/pages/dev/`(견본 페이지 `/dev/kit`), 각 폴더의 `__tests__/`
- 인터페이스: `LevelBadge` · `RangeBar`(log 축, 1,000 기준선, 주최측 예상 ▲) · `KeyNumber` · `EvidenceChip` · `EvidenceCard`(6종) · `FestivalCard`(compact/regular) · `EventChipCard` · `AskButtons` · `FactorList` · `SimilarEventCard` · `InsightCard` · `KpiTile` · `ChecklistItem` · `SourceTip` · `EmptyState`·`ErrorState`(T-430 펫 사용) · `format.ts`(`formatPeople` "2.1만 명"/"1,450명", `formatRange`, `formatDate` "10.18(토) 19:00", `formatPercent` 정수·구간)
  - 모든 컴포넌트는 계약 타입을 받고, 로딩·빈 값·오류 상태를 가진다. 숫자는 받은 값을 그대로 표시(계산 금지)
- 수용 기준(실행 명령):
  - `npm -w apps/web test` 통과(format 규칙 표 테스트, 컴포넌트마다 렌더·상태·접근성 이름)
  - `/dev/kit`을 낮·밤 1366×768로 찍어 `reports/figures/screens/T-402-kit-<day|night>.png`
  - 색 하드코딩 0, lint 통과
- 범위 밖: 화면 조립(T-404~T-407), 3D
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5
