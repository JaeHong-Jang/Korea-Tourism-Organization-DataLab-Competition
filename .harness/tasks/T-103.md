# T-103: 라벨 빌드 — 골드A·B 파서, 실버(행사 전 기준선·SNR), 병합, 골든 제외, 단위 메타데이터, Pandera, QC(재실행 가능)
- 레인/워커: L1 / gpt-6-astra        - 선행: T-102 완료(행사 마스터 `events.parquet` — event_id가 조인 키)
- 목표(한 문장): 데이터랩 축제 현황·DIY·일별 방문자에서 "행사 기간 일평균 방문객" 정답을 등급별로 만들고, 단위·출처·품질을 붙여 T-203(학습·백테스트)이 그대로 쓰는 `labels.parquet`를 만든다. 자료가 늘면(H2 축제 CSV 64개, H3 DIY, 9/26~27 벌크) **같은 명령으로 다시 돌린다**.
- 읽을 것: docs/plan/06 §2(라벨 표·단위 메타데이터·코로나)·§3(공개 시점)·§8(labels 게이트)·§10 `labels` 표·§11 G0, services/forecast/src/crowdcast/data/{events,crosswalk,diy_targets,visitors}.py(호출만), `paths.py`, AGENTS.md §3·§5
- 입력(읽기 전용):
  - 골드A: `paths.DATA` 아래 `*데이터랩*` 폴더들의 `*_연도별 방문자 추이.csv`(UTF-8 BOM, 열 이름 원문 그대로 — `축체기간(일)` 오타 포함). 지금 1개 축제(연천구석기축제 2018·2019·2022~2025), 사람이 더 넣는다(H2)
  - 골드B: `paths.PROCESSED/diy_targets.csv`(T-100이 만든 대상 60건). 사람이 채울 열을 이 task가 정한다: `diy_daily_mean`, `diy_total`, `diy_days`, `diy_area`(데이터랩 DIY에서 지정한 영역 설명), `diy_checked_at`(YYYY-MM-DD), `diy_note`. 빈 행은 건너뛴다. 원본 열·순서는 바꾸지 말고 **뒤에 붙인 템플릿**을 `paths.PROCESSED/diy_labels_template.csv`로 내보낸다(사람이 이 파일을 채워 같은 이름으로 저장 → 재실행)
  - 실버: `paths.PROCESSED/region_daily.parquet`(2025 코드 체계, 현지인·외지인·외국인 일별 — 지금 2023-03~2026-08-25, 벌크로 늘어남) + `events.parquet`
  - 골든: `--golden-file`(event_id JSON 배열, H8 뒤). 없으면 골든 0건으로 기록
- 허용 경로: `services/forecast/src/crowdcast/labels/`(파일 하나 = 책임 하나: `gold_a.py`·`gold_b.py`·`silver.py`·`merge.py`·`schema.py`·`matching.py`·`__main__.py`), `services/forecast/tests/labels/`(작은 픽스처 포함), `services/forecast/pyproject.toml`(의존성 줄만 — 필요할 때)
- 실행 산출물(git 제외): `paths.PROCESSED/labels.parquet`, `paths.PROCESSED/labels_qc.md`, `paths.PROCESSED/diy_labels_template.csv`
- 인터페이스: `python -m crowdcast.labels [--golden-file F]` → `labels.parquet` 열 = 06 §10 `labels`(event_id, label_tier[goldA|goldB|silver], daily_mean, total, days, local, nonlocal, foreign, definition, method, snr, quality_flag) + `year`, `time_unit`(일), `spatial_scope`(행사장|지정영역|시군구), `kind`(사후 집계), `source_file`·`source_row`(추적), `available_at`, `is_primary`, `is_golden`, `usable_for_training`, `covid_period`
  - **골드A 매칭**: 축제명 → `events`의 event_id(같은 연도, T-102 `normalized_name` 규칙, 시도 일치). 못 맞추면 버리지 말고 QC `미매칭` 목록(축제명·연도) — 라벨 행은 만들지 않는다. `definition` = 일평균(파일의 `일평균 방문자수`), 검산: `전체 ÷ 축제기간`과 1% 넘게 다르면 `quality_flag`
  - **골드B**: `diy_daily_mean`이 있으면 그 값, 없고 `diy_total`·`diy_days`가 있으면 나눗셈(`method`에 적는다). 대상 행 → event_id는 diy_targets의 축제명·연도·시도로 같은 매칭 함수
  - **실버**(06 §2): 행사 기간 날마다 `순증 = 방문(세 구분 합) − 기준선`, 기준선 = **시작일 전 28일 안의 같은 요일** 값들의 중앙값(공휴일 제외 — 공휴일 표는 `holidays` 패키지 KR, 없으면 추가), 라벨 = 순증의 평균. `snr` = 라벨 ÷ 기준선 표본 표준편차(같은 요일 잔차를 모은 것). **순증 > 3 × 표준편차만 `usable_for_training`**. 대상 = 시작·종료가 확정되고 기간 ≤ 14일, 시군구 코드가 있고, 기준선 표본 ≥ 3일, `continuity_break` 아닌 행사. 부모 시(`sigungu_match=parent`)는 부모 합계를 그대로 쓴다. 빠진 사유별 건수를 QC에
  - **병합**: 같은 event_id에 여러 등급이면 모두 남기고 `is_primary` 하나(골드B > 골드A > 실버). 골든 event_id는 모든 등급에서 `is_golden=true`·`usable_for_training=false`
  - **공개 시점**: 라벨이 다음 해 피처(전회차 실측)로 쓰일 때를 위해 `available_at` — 골드A·B = 종료일 + 180일(가정, 코드 상수·QC에 "가정" 표기), 실버 = 종료일 + 35일(06 §3 API 지연)
  - `covid_period` = 2020·2021년 행사(빼지 않고 표시만 — 06 §2, 제외 판단은 T-203)
  - **Pandera 스키마**(`schema.py`): 열 타입·enum·`daily_mean > 0`(실버는 음수 허용하되 `usable_for_training=false`), (event_id, label_tier) 유일, `is_primary`는 event_id당 1개. 검증 실패면 파일을 쓰지 않고 종료 코드 1
  - **결정성**: 같은 입력이면 같은 바이트(정렬·부동소수 반올림 규칙 고정)
- 수용 기준(실행 명령):
  - `uv run --package crowdcast-forecast pytest -q services/forecast/tests/labels` 통과: 골드A 파서(실제 연천 CSV 열 이름 그대로 만든 작은 픽스처·BOM), 매칭 실패 목록, 골드B 템플릿 왕복, 실버 기준선(같은 요일·공휴일 제외·행사 전만 — 행사 기간이 28일 창에 걸려도 행사일은 기준선에 안 들어감), 3σ 경계, 병합 우선순위·골든 제외, Pandera 실패 시 미기록, 결정성(두 번 실행 해시 같음)
  - 실제 실행: `python -m crowdcast.labels` → QC에 **등급 × 연도 표**, 정의 일치 고유 행사 수(골든 제외), 실버 후보 → 사용 가능 수와 제외 사유별 건수, 음수 비율(06 §8 게이트 < 5% — 후보 중), G0 판정 문장(06 §11 기준으로 지금 어디인지: 골드 ≥ 60 / 일부 / < 30)
  - 리포트에 산출물 SHA-256과 행 수
- 범위 밖: 피처·학습(T-203), 지자체 발표 보정 비율(T-203·T-206), 파이프라인 CLI 게이트(T-104), 데이터랩 추가 CSV 수집(사람 H2·H3)
- 외부 호출: 없음(패키지 설치만). data.go.kr 호출 금지 — 공유 장부는 오케스트레이터 벌크가 쓴다
- 코드 규칙: AGENTS.md §5

## 게이트 피드백 1회차 (게이트 3 교차 리뷰 FAIL — gpt-6-sol, 오케스트레이터 게이트 2·4 결정 포함) — 워크트리에 결과가 있다
오케스트레이터 게이트 2: test 701·ruff 통과, 임시 데이터 루트 재실행 해시 동일(결정성), 골드A 값 = 연천 CSV 원문(2018 33,741.25 …). 게이트 4: 음수 25.7%는 버그가 아니라 저신호 잡음으로 보인다(유의 신호 양수 250·음수 9) → **06 §8 labels 게이트와 9/25 메모를 바꿨다(정본 — 먼저 읽을 것)**.
1. [High] `gold_b.py:120` — `diy_checked_at`이 종료일 + 180일보다 늦으면 확인 전 예보에 골드B가 쓰일 수 있다 → `available_at = max(종료일 + 180일, diy_checked_at)`. `diy_checked_at`이 없으면 골드B 행을 만들지 않는다(미완성 목록). 테스트.
2. [Med] `qc.py:38` — G0 집계가 비대표 골드A·학습 제외 골드B까지 센다 → **`is_primary & usable_for_training & !is_golden`인 골드**만, 행사 = event_id 고유로. 평가 연도별 수와 **환산 기준선(순간 최대 1,000명 — 일평균이 아니라 T-202 환산 뒤) 아래·위 각 수**도 같은 기준으로(06 §11 세 조건 — T-203 `g0.py`가 이 표를 읽는다: 열 이름을 QC 표와 `labels_g0.json`(새 산출물, 기계용)으로 고정).
3. [Med] `qc.py:38` — 골드B `diy_area`가 행사장 범위인지 확인하지 않고 정의 일치로 센다 → 템플릿에 `diy_area_matches_venue`(예/아니오) 열을 추가하고 **예**만 정의 일치(아니오·빈칸은 라벨은 만들되 `spatial_scope=지정영역`, 정의 일치 집계 제외).
4. [Med · 계획 변경] **실버 부호 검사**(06 §8 메모 ①~⑤)를 `qc.py`에 구현하고 게이트 판정으로 쓴다: ① 유의 신호(|SNR| > 3) 중 음수 비율 < 5% ② 전체 후보 음수 비율 ≤ 40% ③ 유의 신호 수 ≥ 직전 실행의 80%(직전 `labels_g0.json`이 없으면 "첫 실행 — 비교 없음") ④ σ = 0·SNR 결측·유의 신호 0건 건수 ⑤ 연도·시도별 순증 분포 표. 모두 **학습·명절 제외 전 후보**의 부호 있는 원래 순증으로. 각 검사의 분자·분모, 유의 음수 사례별 행사명·기준선·σ·일정 표. 실패하면 파일을 쓰지 않고 종료 코드 1(지금 데이터로는 ①②③ 통과가 기대값 — 아니면 리포트에 이유).
5. [Med · 계획 변경] **명절 겹침**: 행사일이 설·추석 연휴(`holidays` KR 표의 설날·추석 이름 날짜와 그 대체공휴일 — 고정 범위)와 하루라도 겹치는 실버는 `quality_flag`에 `holiday_overlap`, `usable_for_training=false`(부호 무관). QC에 연도·유형별 제외 수와 남은 표본. 원인 문장은 "가설"로.
- 확인: `pytest -q services/forecast/tests/labels`, 실제 실행 → QC 새 표·`labels_g0.json`·SHA-256, 결정성(두 번 실행 해시 같음).
- **1회차 정정(워커 실행 중 계획 리뷰로 바뀜 — 06 §8 메모가 정본)**: 4번의 ②는 **중단이 아니라 경고**(전체 후보 음수 > 50%, 임시 운영 기준), ③은 절대 건수가 아니라 **유의 신호 비율**을 직전 실행의 같은 연도 모집단과 비교(첫 실행은 유의 신호 ≥ 30건), ④ σ = 0은 SNR null·`zero_sigma`·학습 제외, 유의 0건이면 중단. **오류 주입 테스트** 추가: 순증 부호 뒤집기 → ① 중단, 기준선 날짜 1주 밀기·지역 코드 섞기 → ③ 중단.

## 게이트 피드백 2회차 (게이트 3 sol FAIL + 오케스트레이터 확인 — 1회차 정정이 반영되지 않았다) — 마지막 재디스패치
오케스트레이터 게이트 2: test 743·ruff, 두 번 실행 해시 동일. 게이트 4: `silver_qc.py`가 **정정 전 규칙**으로 구현됐다(아래 1~3). **정본 = docs/plan/06 §8 뒤 "9/25 T-103 실측과 게이트 정리" 메모의 ①~⑤** — 먼저 읽을 것.
1. [Med · 정정 미반영] `silver_qc.py:42` — 전체 후보 음수 비율을 **중단(40%)**으로 쓴다 → **경고**로(> 50%, "임시 운영 기준 — 검증 전"), `enforce_silver_gate`에서 뺀다.
2. [Med · 정정 미반영] `silver_qc.py:124` — 신호 유지를 **절대 건수**로 비교한다 → **유의 신호 비율**(유의 ÷ 후보)을 직전 실행의 **같은 연도 모집단**끼리 비교해 80% 미만이면 중단. 첫 실행은 **유의 신호 ≥ 30건**이 최소 조건(미만이면 중단).
3. [Med · 정정 미반영] **오류 주입 테스트**가 없다 → ① 순증 부호 뒤집기 → ① 유의 음수 게이트 중단, ② 기준선 날짜 1주 밀기 → ③ 중단(또는 ①), ③ 지역 코드 섞기(행사 ↔ 다른 시군구) → ③ 중단. 합성 region_daily로.
4. [High] `silver.py:191` — σ = 0인 양수 실버가 `snr=null`인데 학습 가능으로 남고 테스트도 허용 → `zero_sigma` 표시·`usable_for_training=false`(06 메모 ④), 테스트 고침.
5. [Med] `silver_qc.py:89` — 명절 제외 뒤 학습 가능 수에 비대표 실버를 포함(226 → 대표 조건이면 224) → QC의 학습 표본 수는 `is_primary & usable_for_training` 기준.
6. [Med] `silver_qc.py:124` — 같은 스냅샷이면 이전 `labels_g0.json`의 판정을 검증 없이 재사용 → 이전 JSON의 필드·타입·분자·분모를 검증하고 판정을 **다시 계산**(재사용은 비교 기준값만).
- 확인: `pytest -q services/forecast/tests/labels`, 실제 실행 → QC 부호 검사 표(①~⑤·경고 구분)·`labels_g0.json`·해시, 두 번 실행 결정성.
