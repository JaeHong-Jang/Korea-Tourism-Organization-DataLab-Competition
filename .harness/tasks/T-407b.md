# T-407b: 목록·SVG·S6·S7 다듬기(R-07) — 목록 접기, SVG 선택 정리, 자료 등급 색, 인사이트 카드별 상태
- 레인/워커: L4b / gpt-6-sol        - 선행: T-404·T-407 완료, R-07(PASS — Med·Low 반영)
- 목표(한 문장): R-07이 짚은 대시보드 쪽 문제 — 목록을 접어 장면을 넓힐 수 없음, 목록 ↑/↓ 이벤트가 카메라까지 감, SVG에서 지역을 눌러도 행사 선택이 남음, S6 산점도가 판정 등급 색을 자료 등급에 씀, S7 한 카드 실패가 전부를 숨기고 빈 상태가 회색 상자뿐 — 을 고친다.
- 읽을 것: `.harness/reports/R-07.md`(**지적 전부**), docs/plan/04 §3(의미 고정 색 — 등급 색은 판정에만)·§4·§13, docs/plan/11 M1·M6·M7, apps/web/src/features/{festival-list,map-2d,validation,insights}/, apps/web/src/styles/pages.css(S6 산점도 색 213행 부근 — 이 task는 해당 선택자만), packages/contracts/design-tokens.css(`--cat-1`~`--cat-7`), apps/web/src/components/pets/(펫 — 가져다 쓰기만), AGENTS.md §3·§5
- 허용 경로: `apps/web/src/features/{festival-list,map-2d,validation,insights,kpi-timeline}/`, `apps/web/src/pages/{validation,insights}-page.tsx`, `apps/web/src/styles/pages.css`(S6·S7 선택자만), `tests/e2e/`(s6-s7·s1의 목록·SVG 부분)
- 인터페이스:
  1. **목록 접기**: 행사 목록 머리에 접기/펼치기 버튼(aria-expanded, 키보드) — 접으면 목록 본문을 숨겨 패널이 줄고(장면 프레이밍은 T-433b가 패널 경계로 다시 맞춘다), 상태는 브라우저에 기억(try/catch).
  2. **목록 키**: 목록 안에서 처리한 ↑/↓는 `preventDefault()`(카메라가 같이 움직이지 않게 — T-433b는 `defaultPrevented`를 건너뛴다).
  3. **SVG 지역 선택**: SVG에서 시군구를 클릭·키보드로 고르면 `selectFestival(null)`로 행사 선택·요약을 정리한다(3D와 같은 동작).
  4. **S6 산점도 색**: 골드A·골드B·실버를 `--level-*` 대신 **범주형 토큰 `--cat-*`**로(모양·글자 범례는 그대로) — 등급 색은 판정에만(04 §3).
  5. **S7 카드별 상태**: I1·I2 각각 로딩·오류·빈 상태를 따로 — 하나가 실패해도 다른 결과는 보인다. 빈·대기 상태는 회색 상자 대신 펫(`components/pets`) + 한 줄 + 할 일 버튼(G-F4).
- 수용 기준: `npm -w apps/web test`(목록 키 preventDefault·SVG 선택 정리·S7 카드별 상태 단위 테스트)·`build`·`lint`·색 하드코딩 0. e2e: 목록 접기 → 목록 본문 숨김·기억, SVG 지역 선택 → 요약 없음, S6 산점도 범례 색이 `--cat-*`, S7 I1 실패 주입 → I2 보임. 스크린샷 `T-407b-{s1-collapsed,s6,s7}.png`. 실행은 오케스트레이터.
- 범위 밖: 장면 프레이밍·카메라 키(T-433b), 2D MapLibre(T-403)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5

## 이어 하기 (9/25 13:30 사용자 요청으로 중단됐던 실행)
이전 실행이 도중에 멈췄다. **워크트리에 남은 변경을 먼저 읽고 이어서** 완성한다(처음부터 다시 쓰지 않는다 — 맞지 않는 부분만 고친다). 명세·수용 기준은 위 그대로.
