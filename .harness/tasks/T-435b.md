# T-435b: 화면 다듬기(전체 QA 2회차 스크린샷) — 구름·빗줄기 모양, 패널 잘림, 상담 문장 숫자 쉼표 + 3D 성능 상한 조정
- 레인/워커: L4c / gpt-6-sol        - 선행: T-435·T-436·T-411b 병합(develop)
- 목표(한 문장): 오케스트레이터가 스크린샷과 성능 측정으로 찾은 문제를 고친다 — "사람이 만든 미니어처"(04 §13)답지 않은 구름·빗줄기, 1366·390 폭 패널 잘림, 상담 문장 숫자 표기, 그리고 합격선을 넘는 3D 장면의 품질 상한.
- 읽을 것: `reports/figures/screens/T-435-s1-rain-night.png`·`T-411b-s1-390.png`·`T-309-compare.png`(아래 문제), `reports/figures/perf/T-434-frame-time.md`·`T-435-frame-time.md`·`T-436-frame-time.md`(있으면), docs/plan/04 §2·§9(프레임 합격선 — 같은 실행 T-433b 대비 p95 ≤ 1.3배)·§13, apps/web/src/components/scene/(weather·effects·venue·quality), apps/web/src/features/{mini-korea,festival-filters,consult-chat,forecast-report}/, AGENTS.md §3·§5
- 허용 경로: `apps/web/src/components/scene/`, `apps/web/src/features/{mini-korea,festival-filters,consult-chat,forecast-report,venue-3d}/`, `apps/web/src/lib/`(문장 렌더 도우미), `apps/web/src/styles/`, `tests/e2e/`(해당 스펙 보강), `scripts/perf/scene-frame-time.mjs`
- 고칠 것:
  1. **구름**: 지금 회색 사각판이 판·이름표를 덮는다 → 둥글고 부드러운 구름 덩어리(여러 개의 납작한 구·둥근 판, 가장자리 투명), 불투명도 낮게, **이름표·깃발보다 아래 층**, 판 영역 위에만.
  2. **빗줄기·눈**: 판 밖 허공에 흩어진 막대 → 판 위 영역(카메라 절두체 ∩ 판 경계)에만, 가늘고 짧게, 밤에는 약하게 빛나게.
  3. **패널 잘림**: 1366×768 S1 필터 패널에서 "등급" 칸이 범례 패널에 가려짐, 390 필터 시트 맨 윗줄(기간·유형)이 잘려 보임 — 패널 안 스크롤·높이 계산 고치기.
  4. **상담 문장 숫자**: S2 말풍선이 `claim.rendered` 그대로라 "21000명"(S3는 "21,000명") — S3의 `claimText`와 같은 규칙(자리표시자 값에 천 단위 쉼표만)을 공용 도우미로 옮겨 S2·S3 모두 쓰게(연도·날짜 숫자는 건드리지 않는다 — 자리표시자로 묶인 수치만).
  5. **3D 성능**(측정이 합격선을 넘은 장면만): 디오라마 건물을 타일 단위 **병합 지오메트리**로(한 타일 = draw call 하나), 품질별 건물·입자 상한(보통 = 건물 높이 단순화·입자 절반, 낮음 = 입자 0), 보통 품질은 후처리 끔. 목표: `--t434`·`--t435`·`--t436` 모두 같은 실행 T-433b 대비 p95 ≤ 1.3배.
  6. **장면 도구 가림**: T-436의 [장면 저장]·품질 선택 버튼이 행사 목록 패널(`M1-F3`) 밑에 깔려 클릭이 막힌다(e2e `scene.spec.ts` "장면 저장은 내용이 있는 PNG를 내려받는다" 실패 — 목록 패널이 포인터를 가로챔) → 떠 있는 패널이 없는 영역(3D/2D 전환 버튼 옆 등, 패널 경계 캐시 재사용)으로 옮기고 네 폭에서 가리지 않게.
- 성능 측정(9/25 밤, 오케스트레이터): `--t436` 0.82배·`--t435` 0.81배 **통과**, `--t434` **2.10배 미달**(디오라마 건물 — 5번의 병합 지오메트리로).
- 수용 기준: `npm -w apps/web test`(구름·입자 영역 제한, 문장 렌더 도우미 — 자리표시자만 쉼표·연도 그대로, 품질 상한)·build·lint·색 하드코딩 0, e2e 기존 통과 + 스크린샷 `reports/figures/screens/T-435b-{s1-rain-night,s1-1366,s1-390,s2-claims}.png`, 성능 세 조건. 실행은 오케스트레이터.
- 범위 밖: 새 효과·기능
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
