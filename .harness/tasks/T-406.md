# T-406: S3 예보서 + 근거 서랍 + 계획 초안 docx — 발행된 예보서 스냅샷만으로, 화면 숫자 = JSON
- 레인/워커: L4a / gpt-6-sol        - 선행: T-405 완료(S2 상담), T-306 완료(계획서 에이전트), T-603·T-310 완료, T-306c(세션 없이 계획 초안 — 병행 중, 없으면 버튼만 비활성)
- 목표(한 문장): `/f/:forecastId`에서 records의 **발행 예보서 스냅샷**(`GET /api/forecasts/{id}` = 계약 `forecast-report`) 하나로 행사 카드·판정 배너·핵심 수치·설명 문장(근거 칩)·유사 행사·개최지 평시·체크리스트·다음 할 일·근거 서랍(카드 6종)을 그리고, 인쇄(A4 한 장)·링크 복사·계획 초안 docx 받기를 제공한다 — 화면의 모든 숫자는 스냅샷 JSON 그대로다.
- 읽을 것: docs/plan/11 M3(**정본**)·M4-F1, docs/plan/03 S3, docs/plan/04 §3(등급 = 아이콘 + 글자)·§문서(서체)·§13(AI 티 금지), docs/plan/06 §6(`basis=구간`이면 확률 % 금지)·§8(미검증 고지 문구), docs/plan/10 §4(예보서는 `forecast`와 발행된 `claim`·`evidence`만으로), packages/contracts/{schemas/{forecast-report,claim,evidence,forecast-card,similar-event,region-baseline}.schema.json, jsonld/master-labels.json(**조항·규칙(법정/자체)·데이터셋 이름과 원문 주소** — 9/25), fixtures/forecast-report/}, apps/web/src/{pages/forecast-page.tsx(자리만 있음), components/common/(T-402 UI 키트 — evidence-chip·evidence-card·evidence-number·key-number·level-badge·checklist-item·factor-list·similar-event-card·source-tip), components/charts/range-bar.tsx, lib/{api-client,format,evidence-for-quantity}.ts}, `.harness/backlog.md`의 "T-406" 절 전부, AGENTS.md §3·§5
- 허용 경로: L4a 소유 — `apps/web/src/{app,lib,styles}/`, `components/{ui,charts,common}/`, `features/{forecast-report,evidence,safety-plan,consult-chat}/`(새 폴더 포함), `pages/{forecast-page,consult-page,plan-page}.tsx`, `tests/e2e/`(새 `s3.spec.ts`)
- 인터페이스:
  - **데이터**: `GET /api/forecasts/{id}` 하나(계약 검증 — 실패면 오류 상태). knowledge를 따로 부르지 않는다(9/25 결정 — 스냅샷에 문장·근거가 다 있다). 조항·규칙·데이터셋 이름은 `@crowdcast/contracts/jsonld/master-labels.json`.
  - **M3-F1-a 판정 배너**: 등급(아이콘 + 글자) + 판정 사유 문장(발행 `판정` claim) — 사유마다 근거 칩, 법정/자체 구분(`master-labels` rules.kind). **미검증 고지**: `forecast.predictionRun.modelVerdict = "미검증"`이면 인원·판정 옆에 "골든 사례 0건 — 사례 재현 검증 전 임시 사용"(06 §8 문구 그대로, 근거 서랍을 열지 않아도 보이게) + "학습 자료가 큰 행사 위주라 작은 행사는 크게 예보될 수 있어요"(T-203b 전까지).
  - **M3-F1-b 핵심 수치**: 순간 최대 p10~p90(+ p50), 일평균 방문객, 피크 시간(`peakHours` 없으면 "자료 없음" — 지어내지 않는다). 값 = `forecast.peakConcurrent`·`dailyMean` 그대로(천 단위 쉼표만, 단위·추정 표시), `basis=구간`이면 확률 % 표시 없음(LevelBadge 포함 — 백로그 "대규모 99%" 해결: 구간이면 %를 빼고, 확률이면 "순간 최대 1,000명 이상일 확률"을 밝힌다).
  - **M3-F1-c 설명 문장**: 발행 claim 전부(`layout` 순서) — 문장마다 근거 칩, 칩 번호 = 서랍 카드 번호(`evidence-number`).
  - **M3-F1-d**: 유사 행사(`similar` — 단위 메타·"정성 비교" 표시), 개최지 평시(`baseline` — 없으면 "평시 자료 없음"), 구성·시간대 곡선은 스냅샷에 있으면 "가정" 표시와 함께, 없으면 생략(지어내지 않는다).
  - **M3-F1-e 체크리스트**: 발행 `권고` claim + `forecast.judgment.checklist` — 항목마다 근거 칩.
  - **M3-F1-f**: 다음 할 일(`brief.actions` — `href` 있으면 링크), **인쇄**(A4 한 장 인쇄 CSS — 판정·수치·미검증 고지·체크리스트가 한 장에, 칩 번호는 각주 형태로), **링크 복사**(현재 주소), **계획 초안 docx 받기**: `POST /api/forecasts/{id}/plan`(T-306c) → `docxHref`로 내려받기, 경로가 없거나 실패하면 버튼 비활성 + 이유.
  - **M3-F2 근거 서랍**: 카드 6종(데이터·모델·규정·사례·가정·검증 — `evidence.kind`), 규정 카드는 조항 제목·게시처·원문 링크(`master-labels` — 새 탭), 데이터 카드는 데이터셋 이름·기간·공개 시점. 칩 → 서랍 카드 이동 시 **출발 칩 요소를 기억**해 서랍 닫기·Esc·"읽던 곳으로"로 포커스·스크롤을 그 칩으로 되돌린다(같은 근거 칩이 여러 곳에 있다 — 백로그 R-03 Med).
  - **300줄 규칙 정리(백로그)**: `pages/consult-page.tsx`(307줄)를 상담 스트림 상태 훅과 입력 부분으로 나눈다(동작 그대로), `styles/pages.css`에서 S3 부분을 `features/forecast-report/*.css`로 옮긴다(biome 경고를 늘리지 않는다).
- 수용 기준(실행 명령):
  - `npm -w apps/web test`: 화면 숫자 = 픽스처 JSON(천 단위 쉼표 외 변형 없음), `basis=구간`이면 % 0개, 미검증 고지 조건, 칩 번호 = 카드 번호, 규정 카드 조항 이름·링크, 빈 자료(평시 없음·peakHours 없음) 문구, docx 버튼 상태 3가지(성공·404·오류)
  - `build`·`lint`(경고 늘리지 않기), 색 하드코딩 0(`grep -rnE "#[0-9a-fA-F]{3,6}\b|rgb\(" apps/web/src/features/forecast-report apps/web/src/features/evidence` — 토큰 외 0)
  - e2e `tests/e2e/s3.spec.ts`(가짜 API — 계약 픽스처 `forecast-report`, 다른 워크트리와 겹치지 않는 포트): 판정·수치·문장·칩 → 서랍 → Esc로 칩 복귀(키보드), 인쇄 미리보기(`page.emulateMedia({media:"print"})` 스크린샷 `T-406-print.png`), 링크 복사, docx 버튼(가짜 `POST /api/forecasts/{id}/plan` → 다운로드 이벤트), 스크린샷 `reports/figures/screens/T-406-{report,drawer,print}.png`(1440×900, 낮)
- 범위 밖: 근거 지도 탭(T-413), 행사장 3D 탭(T-434), 편집기(T-408), 게이트웨이·records 변경
- 외부 호출: 없음(링크만)
- 코드 규칙: AGENTS.md §5 (파일 맨 위 역할 한 줄 + 문단마다 한국어 한 줄 주석, 300줄 넘으면 분리)
- e2e·스크린샷 실행은 오케스트레이터가 한다(워커 샌드박스는 브라우저를 못 띄운다) — 워커는 e2e를 작성하고 구문만 확인한다.

## 이어 하기 (9/25 13:30 사용자 요청으로 중단됐던 실행)
이전 실행이 도중에 멈췄다. **워크트리에 남은 변경을 먼저 읽고 이어서** 완성한다(처음부터 다시 쓰지 않는다 — 맞지 않는 부분만 고친다). 명세·수용 기준은 위 그대로.
