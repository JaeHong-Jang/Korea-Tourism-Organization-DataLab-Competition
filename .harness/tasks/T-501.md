# T-501: 기록·문서 서비스(PHP Slim 4) 골격 + SQLite 마이그레이션 + 계약 검증
- 레인/워커: L5 / gpt-6-sol        - 선행: R-01 통과, H6(PHP 8.5.4 설치 확인됨)
- 목표(한 문장): `php -S 127.0.0.1:8030 -t services/records/public`로 뜨는 Slim 4 골격을 만들고, SQLite 스키마와 계약 검증 도구를 둔다.
- 읽을 것: docs/plan/05 §2·§8, packages/contracts/openapi/records.yaml, packages/contracts/check/php/check.php(opis 등록 방식 참고), AGENTS.md §3·§5(PHP 8.5, strict_types, PSR-12)
- 허용 경로: `services/records/` 전체(`composer.json`, `composer.lock`, `public/index.php`, `src/`, `migrations/`, `bin/`, `tests/`, `phpunit.xml`, `phpstan.neon`)
- 인터페이스:
  - 의존성: slim/slim 4, slim/psr7, php-di/php-di, opis/json-schema 2, (dev) phpunit, phpstan. `composer test` = phpunit, `composer stan` = phpstan level 6
  - `GET /health` → `{"status":"ok","version":"0.1.0"}`; 모르는 경로는 JSON 404
  - `src/Support/Db.php`: PDO SQLite `data/app/records.sqlite`(테스트는 메모리), `bin/migrate.php`가 `migrations/*.sql`을 순서대로 적용
  - `migrations/001_init.sql`: events, forecast_snapshots(계약 `forecast-report` JSON 전체를 저장, 불변 — 트리거로 UPDATE/DELETE 막기), plans, plan_sections, ledger_entries 테이블
  - `src/Support/ContractValidator.php`: opis로 `packages/contracts/schemas` 검증(요청·응답 양쪽에 쓴다)
  - 폴더: `src/Health/`, `src/Support/` (도메인별 폴더는 다음 task에서)
- 수용 기준(실행 명령):
  - `cd services/records && composer install && composer test && composer stan` 통과: health 응답 스키마, 마이그레이션 적용 후 스냅샷 UPDATE 거부, ContractValidator가 fixtures valid 통과·invalid 거부
  - 레포 루트에서 `node scripts/dev.mjs --check` 출력에 `✓ records`
- 범위 밖: 행사·초안·원장 API(T-502~T-504), 다른 레인 파일
- 외부 호출: composer 설치를 위한 네트워크만
- 코드 규칙: AGENTS.md §5 (PHP: 파일 첫 줄 역할 주석, 메서드마다 한 줄 주석, declare(strict_types=1))
