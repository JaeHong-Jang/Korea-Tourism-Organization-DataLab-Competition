# T-408: S4 계획 초안 편집기 — 목차 9개·상태, 발행 문장(수정 불가)·잠금 숫자·근거 각주, 작성자 메모 자동 저장, docx
- 레인/워커: L4a / gpt-6-sol (records `PUT /v1/plans/{id}`·docx의 메모 처리까지 — L5 경로 포함)        - 선행: T-306·T-306c·T-406 완료, 계약 01489b8(plan-section `notes`)
- 목표(한 문장): `/f/:forecastId/plan`에서 계획 초안 9개 섹션을 목차로 보여 주고, 발행 문장·잠금 숫자는 그대로 둔 채 섹션마다 **작성자 메모**(근거 없음 표시)를 쓰고 자동 저장해 docx로 받게 한다.
- 읽을 것: docs/plan/11 M4(**정본**)·화면 구분 원칙, docs/plan/04(문서 서체·§13 AI 티 금지), packages/contracts/{schemas/{plan,plan-section}.schema.json(`notes` 설명), openapi/{records,gateway}.yaml(`PUT /v1/plans/{id}` — notes만 수정, 그 밖 다르면 422)}, services/records/src/Plans/*(ItemController::update·Service·Repository·Exporter·BodyInspector), apps/web/src/{pages/plan-page.tsx(자리만), features/forecast-report/report-toolbar.tsx(초안 만들기 흐름), features/evidence/(근거 칩·서랍 재사용), lib/api-client.ts}, AGENTS.md §3·§5
- 허용 경로: `apps/web/src/{pages/plan-page.tsx, features/safety-plan/(새 폴더 포함), features/forecast-report/report-toolbar.tsx(편집기로 가는 링크 한 줄), styles/}`, `tests/e2e/`(새 `s4.spec.ts`), `services/records/src/Plans/`, `services/records/tests/`
- 인터페이스:
  - **데이터**: 페이지 진입 → `POST /api/forecasts/{id}/plan`(이미 있으면 그대로, T-306c) → `GET /api/plans/{planId}` + 근거 칩용 `GET /api/forecasts/{id}`(스냅샷). 저장 = `PUT /api/records/plans/{planId}`(게이트웨이 records 중계 허용 목록 — 안 되면 `/api/plans/{id}` PUT 경로가 계약에 있는지 확인하고 없으면 리포트에 CONTRACT-CHANGE 요청, 임의로 만들지 않는다).
  - **M4-F2-a 목차**: 섹션 9개 + 상태 배지(작성됨·검토 필요), 클릭 → 해당 섹션으로 스크롤·포커스, 현재 섹션 강조.
  - **M4-F2-b 섹션**: 발행 문장(`body` 줄 = claim)은 읽기 전용 + 문장마다 근거 칩(서랍 재사용, 칩 번호 = 예보서와 같음), 잠금 숫자(`lockedFields`)는 🔒 표시 칩(수정 불가 — 선택·복사만), 그 아래 **작성자 메모** 입력(textarea — 새 편집기 라이브러리 없이), 메모 칸 위에 "작성자 메모 — 근거 없음" 라벨.
  - **M4-F2-c 저장·내보내기**: 입력 멈춘 뒤 1초 자동 저장(요청 중복 방지·실패 시 재시도 버튼·"저장됨 HH:MM" 표시), 페이지 떠날 때 저장 안 된 메모 경고, [docx 받기] = `/api/plans/{id}/export.docx`.
  - **records**: `PUT`은 섹션 `notes`만 반영 — key·title·status·claimIds·body·lockedFields가 저장본과 다르면 422(메시지에 첫 차이 필드). docx는 섹션 본문 다음에 "작성자 메모(근거 없음)" 소제목과 메모를 넣는다(비었으면 생략). 메모는 기존 BodyInspector 검사 대상이 아니다.
  - 빈·오류·로딩 상태: 펫 + 한 줄 + 할 일 버튼(G-F4).
- 수용 기준: `npm -w apps/web test`(목차 이동·잠금 칩 비편집·자동 저장 디바운스·실패 재시도·이탈 경고)·build·lint·색 하드코딩 0, `cd services/records && composer test && composer stan`(notes만 수정 → 200, body 바꾸면 422, docx에 메모 소제목), e2e `s4.spec.ts`(가짜 API: 목차 클릭 → 섹션, 메모 입력 → PUT 1회, docx 다운로드 이벤트) + 스크린샷 `reports/figures/screens/T-408-{editor,saved}.png`. e2e 실행은 오케스트레이터.
- 범위 밖: 발행 문장 편집(근거 규칙상 불가), 여러 사람 동시 편집, 한글 HWP
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**(게이트 = 테스트·build·lint).
