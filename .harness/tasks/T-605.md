# T-605: 파이프라인 계보 → 기준 그래프 — Dagster 계보(`reports/runs/lineage.json`)를 PROV로 올려 "이 모델·예보가 어느 실행·자료에서 나왔나"를 질의
- 레인/워커: L6 / gpt-6-astra        - 선행: T-108 완료(계보 JSON — 형식은 `docs/reference/dagster.md`), T-603·T-604(기준 그래프·통계) 완료
- 목표(한 문장): 파이프라인 단계(자산)를 `prov:Activity`, 입력·출력 파일을 `prov:Entity`(sha256)로 기준 그래프에 올려, 사용 모델(`cc:ModelRun`)과 일괄 예보가 **어떤 실행·어떤 자료 파일·어떤 데이터셋**에서 나왔는지 SPARQL 한 번으로 이어지게 하고, 게이트 실패 단계도 그대로 남긴다.
- 읽을 것: docs/plan/09(온톨로지 — PROV 매핑 표·기준 그래프 버전 규칙, **정본**), docs/reference/dagster.md(계보 JSON 형식), `reports/runs/lineage.json`(실제 예 — fetch 게이트 실패 포함), services/knowledge/src/knowledge/{api/model_runs.py(기동 시 모델 카드 적재 방식), paths.py, store·master 관련}, services/knowledge/ontology/(TBox·master·shapes), packages/contracts/jsonld/{context,types}.json, AGENTS.md §3·§5
- 허용 경로: `services/knowledge/src/knowledge/`(새 `lineage/` 등), `services/knowledge/ontology/`(PROV 매핑에 필요한 TBox·SHACL 추가만), `services/knowledge/tests/`
- 인터페이스:
  - **적재**: knowledge 기동 시(모델 카드 적재 다음) `DATA_ROOT/reports/runs/lineage.json`이 있으면 적재 — 같은 파일 해시면 건너뜀, 바뀌면 기준 그래프 버전 +1(기존 규칙). CLI `python -m knowledge.lineage load <path>`도.
  - **매핑**: 자산 → `cc:PipelineStage`(⊂ `prov:Activity`, 이름·단계 순서·게이트 통과/메시지·마지막 runId), 입력/출력 파일 → `prov:Entity`(경로·sha256, sha256 없으면 경로만 + "해시 없음"), `prov:used`·`prov:wasGeneratedBy`·단계 간 `prov:wasInformedBy`. 데이터 파일이 데이터셋(master-ids datasets)에서 온 것이면 `prov:wasDerivedFrom` 데이터셋. 사용 모델 `cc:ModelRun` ← train/backtest 단계 연결(모델 버전·산출물 해시로 매칭).
  - **질의 도우미**: `lineage_for_model(modelVersion)` → 단계·파일·데이터셋 목록(깊이 순), SHACL: 모든 `cc:PipelineStage`는 게이트 결과를 가진다.
  - **통계**: `/v1/stats/graph`의 기존 응답 필드는 그대로(계약) — 계보 삼중항은 master 삼중항 수에 포함.
- 수용 기준: `pytest -q services/knowledge/tests`·ruff, 테스트: 실제 `lineage.json` 사본으로 적재 → 사용 모델에서 데이터셋까지 경로 질의 결과, 같은 파일 재적재 = 버전 그대로, 게이트 실패 단계 보존, SHACL 통과. 실제 서비스 기동 확인은 오케스트레이터.
- 범위 밖: 웹 표시(근거 지도에 계보 노드 — 후속), Dagster 쪽 변경
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
