# T-203b: 모델 v2 — 전년 방문객수(문체부 개최계획서) 피처로 "모두 4등급" 편향 줄이기 + 골드B 영역 일치만 학습
- 레인/워커: L2 / gpt-6-astra        - 선행: T-203(v1)·T-204b·T-207 완료(develop), 9/25 사용자 결정(H3 DIY 취소 — `.harness/backlog.md` "사람 자료 재결정")
- 목표(한 문장): 다가오는 행사 211건이 전부 4등급(일평균 1.1만~2.5만)으로 나오는 규모 편향을, **행사 전에 공개되는 문체부 개최계획서의 방문객수(前년) `visitors_announced`**(행사 마스터 7,786건)를 행사 입력 피처로 넣고 단순 모델의 규모 계층 선택에도 써서 줄이고, 백테스트 v1 대비 변화를 그대로 기록한다.
- 읽을 것: `.harness/backlog.md`의 "⚠ T-203b 최우선"·"T-203b (H3 …)" 절(**정본 조치**), docs/plan/06 §2(발표치는 라벨이 아니다 — "발표 ÷ 실측 비율은 학습 구간 안에서만")·§3(행사 속성은 입력 — 조건부 정의)·§5·§8(게이트)·§11(G0), `.harness/tasks/T-203.md`(파이프라인·발행 규칙 그대로), services/forecast/src/crowdcast/{features,models,labels/gold_b.py}, AGENTS.md §3·§5
- 허용 경로: T-203과 같은 `services/forecast/src/crowdcast/{features,models}/`, `services/forecast/src/crowdcast/labels/gold_b.py`(영역 일치 표시만), 같은 이름의 `tests/`, `configs/`(모델 설정)
- 인터페이스:
  1. **피처**: `visitors_announced`(없으면 결측 — 지어내지 않는다) + `log1p` 변환, 공개 시점 = 개최계획서 공개(행사 속성, 입력). 학습 라벨과의 관계는 모델이 배우되 "발표치 = 실측"으로 쓰지 않는다(라벨 아님).
  2. **단순 모델 규모 계층**: 전회차 골드가 없을 때 유형 중앙값 대신 `visitors_announced` 규모대(학습 구간 안의 발표÷실측 비율로 보정 — 06 §2)를 쓴다, 발표치도 없으면 지금처럼 유형 중앙값.
  3. **골드B 영역**: `spatial_scope="지정영역"`인 골드B는 학습·전회차 실측 피처에서 빼고 참고 자료로 보존(영역 일치 확인된 행사장 골드B만).
  4. **백테스트 v2**: T-203 명령 그대로(`python -m crowdcast.pipeline` 또는 T-203 CLI) → 새 모델 버전·카드·백테스트, 사용 모델과 비교(06 §8 허용 악화폭 — 넘으면 승격 안 됨, 결과는 그대로 기록). OOD 범위에 새 피처 포함.
  5. **다가오는 행사 재실행**: 승격되면 `python -m crowdcast.analytics.upcoming` 재실행 → 등급 분포·OOD 수를 리포트에(목표: 1~4등급이 섞여야 하지만 **숫자를 맞추려 조정하지 않는다** — 결과 그대로).
- 수용 기준: `pytest -q services/forecast/tests`(전체 — 소켓 제한 실패는 리포트에 적기만)·ruff, 새 테스트(피처 결측 처리·공개 시점·보정 비율이 학습 구간만 쓰는지·지정영역 골드B 제외), 백테스트 v1 대 v2 표(MdAPE·포함률·B0/B1 대비)와 다가오는 행사 등급 분포 전·후를 리포트에, 산출물 해시.
- 범위 밖: 9/26 방문객 수집 뒤 재학습(오케스트레이터가 같은 명령으로), LightGBM 구조 변경, 확률 문장
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5
- **구현 우선 모드**: 게이트는 테스트·lint만, 교차 리뷰는 마지막 전체 QA에서.
