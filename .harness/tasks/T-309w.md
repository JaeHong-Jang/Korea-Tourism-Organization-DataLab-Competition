# T-309w: 상담 화면 what-if — 후속 칩("일요일이면? / 밤이면? / 유료면? / 비 오면? / 비슷한 행사는?")과 원래·바뀐 예보 비교 카드
- 레인/워커: L4a / gpt-6-sol        - 선행: T-405(S2 상담)·T-406 완료, 게이트웨이 what-if는 **T-309 병행** — 이 task는 계약 SSE 픽스처(`new`·`followup` 모드)로 만든다.
- 목표(한 문장): S2에서 예보가 발행된 뒤 후속 칩에 what-if 질문을 더하고, what-if로 새 예보가 오면 **원래 예보와 바뀐 예보를 나란히** 보여 준다(M2-F1-c P1).
- 읽을 것: docs/plan/11 M2(F1-c·F4 — **정본**)·화면 구분 원칙, docs/plan/04 §3·§6·§13, packages/contracts/{sse-events.md, fixtures-sse/}, apps/web/src/{pages/consult-page.tsx, features/consult-chat/(스트림 상태·후속 칩·카드), components/common/(range-bar·key-number·level-badge)}, AGENTS.md §3·§5
- 허용 경로: `apps/web/src/features/consult-chat/`, `apps/web/src/pages/consult-page.tsx`(300줄 넘지 않게 — 넘으면 기능 폴더로 나눈다), `apps/web/src/styles/`(상담 부분), `tests/e2e/`(새 `s2-whatif.spec.ts`)
- 인터페이스:
  - **칩**: 발행 뒤 후속 칩에 what-if 5개(행사 조건에 맞게 — 이미 일요일이면 "토요일이면?", 이미 밤이면 "낮이면?", 무료면 "유료면?"), 칩 = 그 문장을 보내기(직접 입력과 같은 경로).
  - **비교 카드**: 한 세션에서 두 번째 `forecast` 카드가 오면(새 예보 id) 원래·바뀐 예보를 나란히 — 행사 조건 차이(바뀐 칸 강조), 등급(아이콘 + 글자), 순간 최대·일평균 구간 막대 두 개(같은 축), 숫자 = 두 카드 JSON 그대로(차이 숫자를 화면에서 계산해 문장으로 만들지 않는다 — 막대로만 비교), [바뀐 예보서 열기]. `followup`(비·유사) 답은 기존 문장 흐름 그대로.
  - 작업판은 what-if 동안 예보관·검증팀 펫이 다시 움직인다(기존 `agent_status` 그대로).
- 수용 기준: `npm -w apps/web test`(칩 문맥 선택, 두 번째 forecast 카드 → 비교 카드·숫자 = 픽스처, followup은 비교 카드 없음)·build·lint·색 하드코딩 0, e2e `s2-whatif.spec.ts`(가짜 SSE: 발행 → "일요일이면?" 칩 → 비교 카드 → 바뀐 예보서 링크) + 스크린샷 `reports/figures/screens/T-309-compare.png`. 실행은 오케스트레이터.
- 범위 밖: 게이트웨이 what-if(T-309)
- 외부 호출: 없음
- 코드 규칙: AGENTS.md §5. **구현 우선 모드**.
