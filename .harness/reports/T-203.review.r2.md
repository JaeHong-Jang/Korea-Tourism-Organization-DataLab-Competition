직전 지적 4건의 판정입니다.

- **High 행사 속성 공개 시점: 해결.** 조건부 성적을 계약의 주 지표로 유지하고, 파일명 날짜 민감도를 별도 표와 `points.parquet`에 기록했습니다. 보고서와 모델 카드에도 공개일 미입증 한계가 명시돼 있습니다.
- **Med 달력: 부분 해결.** 임시공휴일은 제외하지만, 늦게 제공된 달력 목록이 법정 공휴일 피처를 바꿀 수 있습니다.
- **Med 구 형식 G0: 해결.** `labels`·`events`·`labels_g0` 해시가 모두 일치해야 진행합니다.
- **Med 완료 표시: 부분 해결.** `latest.json`은 해시 기록 뒤 원자적으로 교체하지만, 재실행 중 이미 공개된 산출물은 보호되지 않습니다.

[High] [models/__main__.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/models/__main__.py:109) — 같은 입력의 재실행은 같은 `runId` 디렉터리와 공유 `models/model_card.json`을 `latest.json` 교체 전에 덮어씁니다. 이후 해시 기록이 실패하면 기존 완료 포인터가 변경되거나 일부만 기록된 산출물을 계속 가리킵니다. 현재 실패 테스트는 포인터만 검사합니다 — 실행별 임시 디렉터리에 모든 산출물과 해시를 완성·검증한 뒤 발행하고, 실패 시 기존 공개 산출물의 바이트와 해시가 유지되는지도 검사하세요.

[Med] [features/calendar_features.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/features/calendar_features.py:29) — `holiday_calendar.available_at`을 확인하지 않은 채 법정 공휴일 집합과 교집합을 취합니다. D-14 뒤 제공된 목록을 넣는 것만으로 2025년 5월 3~6일의 `holiday_days`가 2→1, `holiday_streak`이 4→3으로 바뀌는 것을 재현했습니다 — 백테스트 달력은 법정·대체공휴일 규칙 집합만 사용하세요.

계약 분리와 AGENTS.md §5의 파일 길이·주석 규칙에서 추가 위반은 찾지 못했습니다. 전체 pytest는 실행하지 않았고, 위 달력 사례는 읽기 전용 실행으로 확인했습니다. 파일은 수정하지 않았습니다.

VERDICT: FAIL