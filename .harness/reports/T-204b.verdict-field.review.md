[Low] `services/forecast/tests/api/test_upcoming_routes.py:31` — 새 필드가 있는 parquet만 검사해 `modelVerdict` 열이 없는 구형 parquet 호환성은 테스트되지 않는다 — 해당 열을 뺀 parquet로 200 응답과 필드 생략을 확인하는 회귀 테스트를 추가하라.

정적 검토에서 계약 위반이나 정렬 변경은 발견되지 않았다. `upcoming.py`는 정확히 300줄이며 변경 주석도 §5 규칙에 맞는다. 요청대로 실행 검증은 하지 않았다.
VERDICT: PASS