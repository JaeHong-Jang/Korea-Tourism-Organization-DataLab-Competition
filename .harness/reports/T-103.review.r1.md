[High] services/forecast/src/crowdcast/labels/silver.py:191 — σ=0인 양수 실버가 `snr=null`인데도 학습 가능으로 남습니다. 기존 테스트도 이를 허용합니다 — `zero_sigma`를 기록하고 학습에서 제외하도록 코드와 테스트를 고치세요.

[Med] services/forecast/src/crowdcast/labels/silver_qc.py:89 — 명절 제외 후 학습 가능 수에 비대표 실버를 포함합니다. 실제 산출물은 226건으로 보고하지만 대표·학습 가능 조건을 함께 적용하면 224건입니다 — QC의 학습 표본 수에 `is_primary` 조건을 적용하세요.

[Med] services/forecast/src/crowdcast/labels/silver_qc.py:124 — 같은 스냅샷이면 이전 `labels_g0.json`의 신호 유지 판정을 검증 없이 재사용합니다. 현재는 `schema_version`만 확인하므로 손상된 판정 값도 게이트에 반영될 수 있습니다 — 이전 JSON의 필드·값을 검증하고 판정의 분자·분모를 확인하세요.

VERDICT: FAIL