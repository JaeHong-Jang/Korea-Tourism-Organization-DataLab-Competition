## T-205 결과: PARTIAL

- 변경 파일: [upcoming.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/analytics/upcoming.py), [test_upcoming_publish.py](/home/data/crowdcast-wt/L2/services/forecast/tests/analytics/test_upcoming_publish.py)
- 구현: 배치 전체에 비차단 배타 잠금 적용, runId 해시를 모델 식별 필드 3개로 제한, QC 중단 안내 추가. 잠금 충돌·해제와 재승격 후 바이트 유지 검증.
- 수용 기준: `uv run --package crowdcast-forecast pytest -q services/forecast/tests --ignore=services/forecast/tests/api` → **1009 passed**.
- 수용 기준: `ruff check services/forecast/src services/forecast/tests`, `git diff --check` → 통과.
- 수용 기준: 전체 pytest → `TestClient.__enter__`에서 대기, 60초 제한 종료(124). `node scripts/dev.mjs --check --only forecast` → health 실패(1).
- 검증 환경: `UV_CACHE_DIR=/tmp/crowdcast-t205-uv-cache`, `UV_OFFLINE=true`.
- 의존성 변경: 없음
- CONTRACT-CHANGE: 없음
- BLOCKED: 샌드박스가 내부 소켓 전송을 `PermissionError: [Errno 1] Operation not permitted`로 차단. 전체 pytest·health는 오케스트레이터 환경에서 재검증 필요.
- 남은 일·주의: 지시대로 실제 배치는 실행하지 않음. T-204b 이후 재실행 필요.