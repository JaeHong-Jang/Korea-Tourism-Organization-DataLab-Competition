## 2회차 판정

- **배치 전체 잠금: 해결.** QC 조회부터 세 파일 발행까지 배타 잠금을 유지합니다. 잠금 충돌 시 발행 경로에 들어가지 않고 기존 산출물을 보존하는 테스트도 추가됐습니다.
- **`promotedAt` 제외: 해결.** runId 해시에는 `modelVersion`·백테스트 `runId`·`verdict`만 사용하며, 승격 시각 변경 후 runId와 예보 파일 바이트를 비교하는 회귀 테스트가 있습니다.

## 새 결함

- [Med] [upcoming.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/analytics/upcoming.py:46) — 예보 수치에 쓰이는 `peak_profiles.yaml`·`thresholds.yaml`·`checklist.yaml`이 runId 해시에서 빠져 있습니다. 설정이 바뀐 뒤 재실행하면 내용이 다른 예보에 같은 runId가 붙을 수 있습니다. — 해당 설정의 해시를 runId 입력에 포함하고 변경 회귀 테스트를 추가하십시오.
- [Med] [upcoming.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/analytics/upcoming.py:278) — 예보 0건이면 JSONL이 빈 파일이라 runId를 담지 못합니다. 새 JSONL만 교체된 시점에 중단되면, 읽는 쪽은 parquet와 JSONL의 **runId 비교만으로** 불일치를 감지할 수 없습니다. — T-207 읽기 단계에서 parquet 행 수와 JSONL 레코드 수·forecastId도 대조하십시오.

파일 수정이나 실행 검증 없이 정적 검토했습니다. 오케스트레이터가 허용한 중단 시 파일 혼합 자체는 High로 재분류하지 않았습니다.

VERDICT: PASS