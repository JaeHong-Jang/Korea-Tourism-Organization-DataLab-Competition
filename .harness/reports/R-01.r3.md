**High 1건이 남아 T-001 재작업이 필요합니다.** 파일은 수정하지 않았습니다.

제공 로그의 79개 검사 항목은 모두 통과했습니다. 직접 실행한 Node 스키마 43개·무결성 22개·SSE 순서 6개와 PHP 스키마 43개도 기대 판정과 일치했습니다. 아래 결함은 추가 Node 반례로 확인했습니다. 경로는 별도 표시가 없으면 `packages/contracts/` 기준입니다.

**1) 체크리스트**

| 항목 | 판정 | 근거 |
|---|---|---|
| ① 기능·세부 기능 포함 | FAIL | `/facts`의 개별 Claim·Evidence 적재가 09의 참조 무결성 요구를 충족하지 못합니다(`rules/integrity.mjs:116`). |
| ② 근거·발행 상태·단위 메타데이터 | PASS | 근거, Claim 상태, Quantity 단위·추정·가정·발표일 필드는 있습니다. 참조 검사 결함은 아래에 별도 기재했습니다. |
| ③ 세 언어 판정 일치 | PASS | 제공 로그에서 스키마 픽스처 43개의 TS·Python·PHP 판정이 일치하고, Node·PHP 재실행도 같습니다. |
| ④ 도메인 이름 | PASS | 행사·예보·근거·관측·판정·작업 기록에 맞는 이름을 사용합니다. |

**2) 2차 지적 재검토**

| 지적 | 상태 | 근거 |
|---|---|---|
| 부분 해결 1 — Event 적재·JSON-LD 변환 | 해결 | `/facts`에 `event`가 추가됐고, 행사·유사 사례·평시의 타입 및 변환 기대값이 정의됐습니다(`openapi/knowledge.yaml:49`, `jsonld/types.json:3`, `:36`, `:44`). |
| 부분 해결 5 — 참조 존재·세션 검사 | **부분 해결** | 모델 실행·가정·보고서 Claim 세션 검사는 추가됐지만, 개별 `claim`·`evidence`는 검사 없이 통과합니다(`rules/integrity.mjs:45`, `:67`, `:116`). 아래 High 참조. |
| 부분 해결 8 — 발행 전 전송 금지 검사 | 부분 해결 | 순서 검사와 정상·위반 픽스처가 추가됐습니다. 다만 발행 뒤 이전 게이트로 돌아가는 순서를 놓칩니다(`scripts/check.mjs:91`, `rules/sse-sequence.mjs:20`). |
| 부분 해결 10 — 활용 명세·운영 데이터 | 해결 | 지표·기간·단위·용도와 평가·최신성·그래프 크기 계약 및 API가 추가됐습니다(`schemas/datalab-spec.schema.json:21`, `schemas/ops-status.schema.json:8`, `openapi/gateway.yaml:285`). |
| 새 High 1 — `gate: integrity` 응답 불일치 | 해결 | `gate`와 위반 `check`에 `integrity`가 추가됐고 정상 픽스처가 세 언어 검사를 통과합니다(`schemas/gate-report.schema.json:21`, `:54`). |
| 새 Med 1 — 유사 행사 Quantity 오판 | 해결 | 문서 전체의 Quantity를 수집합니다. 유사 행사 수치 인용 정상 사례와 null 필드 반례가 기대대로 판정됩니다(`rules/integrity.mjs:11`, `:70`). |
| 새 Med 2 — 카드·배치·요약 불일치 | 해결 | 카드 투영 비교와 배치·요약 참조 검사가 추가됐으며 기존 반례를 거부합니다(`rules/integrity.mjs:85`, `:100`, `:104`). |

**3) 새 결함·잔존 결함**

- **[High] [rules/integrity.mjs:116](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/integrity.mjs:116>) — 실제 개별 적재 경로에서 참조 검사를 생략함 —** `claim`·`evidence`는 분기가 없어 항상 `[]`를 반환합니다. Claim의 세션·예보를 다른 ID로 바꾸고 없는 근거·Quantity를 연결한 반례, Evidence의 데이터셋·Quantity를 없는 ID로 바꾼 반례가 모두 **스키마 통과·무결성 오류 0개**였습니다. `.harness/tasks/T-601.md:10`은 이 함수와 같은 판정을 요구하므로 그대로 구현하면 `/facts`의 422 거부 요구를 어깁니다. **현재 세션 ID와 세션·기준 그래프의 타입별 참조 정보를 입력으로 받아, 개별 적재에도 존재·타입·세션 검사를 적용해야 합니다.**

- **[Med] [rules/integrity.mjs:87](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/integrity.mjs:87>) — 예보서의 중복 근거는 ID만 비교하고 내용 불일치를 허용함 —** 최상위 `evidence` 중 Forecast에도 있는 ID는 92행에서 검사 대상에서 제외됩니다. 정상 예보서의 최상위 `ev-rule-legal-hazard.ruleId`만 `rule-not-registered`로 바꿔도 **스키마 통과·무결성 오류 0개**였습니다. 본문의 근거와 근거 서랍·스냅샷의 내용이 달라질 수 있습니다. **동일 ID의 근거 내용 일치를 검사하고 모든 근거 사본의 참조를 검증해야 합니다.**

- **[Med] [rules/integrity.mjs:14](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/integrity.mjs:14>) — 충돌하는 Quantity ID를 마지막 값으로 덮어씀 —** 유사 행사 실측에 예보 일평균과 같은 ID를 주고 `p50`을 다른 값으로 설정한 반례가 **스키마 통과·무결성 오류 0개**였습니다. 카드 투영은 정상이어도 자리표시자의 수치 대상이 모호해집니다. **동일 ID의 Quantity는 내용이 같을 때만 중복을 허용하고, 값·단위가 다르면 거부해야 합니다.**

- **[Med] [rules/sse-sequence.mjs:20](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/sse-sequence.mjs:20>) — 게이트 역행과 이전 발행 승인 재사용을 놓침 —** 정상 스트림의 `publish` 뒤에 숫자 위반을 담은 `gate(B, passed=false)`를 넣고 Claim 전송을 계속해도 **모든 이벤트 스키마 통과·순서 위반 0개**였습니다. `published`가 그대로 남기 때문입니다. **새 예보 스트림의 허용 단계 전이를 명시하고, 발행 뒤 게이트 역행을 거부하는 반례를 추가해야 합니다.**

- **[Med] [rules/integrity.mjs:39](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/integrity.mjs:39>) — 방문객 구성의 근거 참조를 검사하지 않음 —** `composition.evidenceId`를 `ev-missing`으로 설정한 Forecast가 **스키마 통과·무결성 오류 0개**였습니다. 방문객 구성 화면의 근거 칩이 끊어질 수 있습니다. **`composition`이 있으면 해당 근거가 허용된 범위에 존재하는지 검사해야 합니다.**

VERDICT: FAIL -> T-001