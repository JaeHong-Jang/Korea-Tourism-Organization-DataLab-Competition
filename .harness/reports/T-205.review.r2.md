1회차 지적을 항목별로 판정했습니다.

- **일정 출처·공개일 및 QC 집계: 해결.** 예보된 행의 `date_source`·`date_available_at`을 parquet와 QC에 기록하고, `asOf` 뒤 공개된 일정도 제외하지 않고 집계합니다.
- **공통 runId·임시 파일·실패 복구: 부분 해결.** 세 산출물에 runId를 넣고, 세 임시 파일을 모두 쓴 뒤 교체합니다. 주입된 일회성 쓰기·교체 오류의 복구 테스트도 있습니다. 다만 아래 High 결함 때문에 세 파일의 일관성이 항상 보장되지는 않습니다.
- **X-Run-Id: 해결.** 응답 본문에 사용한 parquet 스냅샷의 runId를 헤더로 보냅니다.

[High] [upcoming.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/analytics/upcoming.py:62) — 세 번의 `os.replace` 사이에 프로세스가 종료되면 서로 다른 runId의 파일이 남습니다. 기간이 다른 두 배치가 동시에 발행해도 교체 순서가 엇갈려 같은 상태가 될 수 있습니다. 현재 복구 코드는 예외를 잡은 경우에만 작동합니다. — 발행을 단일 원자적 완료 표식으로 확정하고 중단 후 복구할 수 있게 하거나, 발행 잠금과 복구 절차를 마련하십시오.

[Med] [upcoming.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/analytics/upcoming.py:57) — runId 해시에 포인터 전체를 넣어 `promotedAt`까지 반영합니다. 같은 입력·모델을 다시 승격해 시각만 바뀌어도 runId와 산출물 바이트가 달라져 결정성 요구를 어깁니다. — `modelVersion`·백테스트 `runId`·`verdict`처럼 결과에 영향을 주는 안정적인 필드만 해시하고 재승격 회귀 테스트를 추가하십시오.

파일 수정이나 실행 검증은 하지 않았습니다.

VERDICT: FAIL