## T-102b 결과: DONE

- 변경 파일: [admin_dict.py](/home/data/crowdcast-wt/L1/services/forecast/src/crowdcast/data/admin_dict.py:179), [test_admin_index.py](/home/data/crowdcast-wt/L1/services/forecast/tests/data/test_admin_index.py:55). 스냅샷을 먼저 생성하고 행·패턴을 해당 스냅샷에서 구성. 변경 시점 회귀 테스트 추가.
- 수용 기준: 캐시 테스트 **5개 통과**. 실제 데이터 전체 동등성·`make_event` 포함, API 제외 **981개 통과**.
- 전체 검증: `uv run --package crowdcast-forecast pytest -q services/forecast/tests` → 첫 API의 `TestClient.__enter__` 대기로 중단. 빈 FastAPI 앱에서도 재현했으며, 전체 통과로 판정하지 않음.
- 정적 검사: `uv run --package crowdcast-forecast ruff check services/forecast`, `git diff --check` → 통과.
- 성능: 빌드 **1.003초**, 빌드 후 후보 조회 **0.050초**, 첫 공개 `candidates()` 호출 **1.215초**(빌드 포함).
- 의존성 변경: 없음
- CONTRACT-CHANGE: 없음
- BLOCKED: 구현 없음. 전체 API 검증은 실행 환경에서 재확인 필요.
- 남은 일·주의: `events` 모듈 실행·외부 호출 없음. 행정 사전·축제·행사 마스터·QC 산출물 4개의 SHA-256 모두 변경 없음.