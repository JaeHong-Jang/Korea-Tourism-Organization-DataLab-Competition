9개 항목 중 **4개 해결, 5개 미해결**입니다.

- [Med] [Exporter.php:30](/home/data/crowdcast-wt/L5/services/records/src/Plans/Exporter.php:30) — **3번 미해결.** 두 DOCX 모두 `document.xml`의 `rStyle`이 참조하는 `PlanBody`·`PlanHeading`·`PlanTable` 정의에 `w:styleId`가 없습니다. 기본 함초롬바탕 11pt는 설정됐지만 제목·표의 맑은 고딕과 지정 크기는 연결되지 않습니다. `PlanParagraph`의 `pStyle` 연결과 160% 줄간격은 정상입니다 — 스타일 ID를 연결하거나 인라인 서식을 적용하고, 테스트에서 참조 ID와 정의·서체·크기를 함께 대조해야 합니다.

- [Med] [BodyInspector.php:103](/home/data/crowdcast-wt/L5/services/records/src/Plans/BodyInspector.php:103) — **2번 미해결.** 숫자 재인코딩이 계약 v1.6의 “JSON 숫자 표기 그대로”를 보존하지 않습니다. 정상 저장된 스냅샷의 숫자가 `21000.00` 또는 `2.1e4`이면 원문 그대로인 잠금값은 422, 원문과 다른 `21000.0 명`은 200입니다 — 스냅샷의 숫자 원문을 보존하여 정확 비교하고 소수·지수 표기 회귀 테스트를 추가해야 합니다.

- [Med] [EvidenceLabel.php:22](/home/data/crowdcast-wt/L5/services/records/src/Plans/EvidenceLabel.php:22) — **8번 미해결.** 모델 근거의 `period`보다 `predictionRun.trainRange`를 우선합니다. 근거 기간이 `2020-01-01 ~ 2023-12-31`인 스냅샷을 API가 200으로 저장하지만 각주는 `2018-01-01 ~ 2024-12-31`로 출력합니다 — 명세대로 해당 근거의 `period`를 우선하고, 대체값은 기간이 없을 때만 사용해야 합니다.

- [Med] [005_plans.sql:11](/home/data/crowdcast-wt/L5/services/records/migrations/005_plans.sql:11) — **6번의 신규 적용 결함.** 이미 적용된 마이그레이션에 트리거를 추가하여 기존 DB에는 반영되지 않습니다. 수정 전 005를 적용한 메모리 DB에서 `Migrator::run()`을 재실행해도 이력 트리거는 0개이며 이력을 `{}`로 UPDATE할 수 있었습니다 — 후속 마이그레이션 허용 경로를 요청하고 기존 DB에도 트리거를 설치해야 합니다.

- [Low] [ExportController.php:14](/home/data/crowdcast-wt/L5/services/records/src/Plans/ExportController.php:14) — **9번 미해결.** 내보내기 컨트롤러 생성자의 한국어 역할 주석이 여전히 없습니다 — AGENTS §5에 맞춰 추가해야 합니다.

| 항목 | 판정 | 확인 근거 |
|---|---|---|
| 1 XML 이스케이프 | 해결 | 두 DOCX의 모든 XML 파싱 성공, 특수문자 원문 보존 |
| 2 잠금값 | 미해결 | 위조 값·없는 칸·단위는 거부하지만 숫자 원문 표기를 변경 |
| 3 스타일 | 미해결 | 문자 스타일 3개의 참조 ID 정의 누락 |
| 4 객체·배열 구분 | 해결 | `sections` 객체·`claimIds: {}` 모두 422 |
| 5 PUT 충돌 | 해결 | 트랜잭션 안에서 시각 비교, 오래된 PUT 409 및 이력 보존 |
| 6 이력 불변성 | 미해결 | 새 DB에서는 UPDATE·DELETE·REPLACE·UPSERT·rowid 별칭 우회 모두 차단, 기존 DB 적용 누락 |
| 7 날짜 | 해결 | KST 변환·요일·여러 날·시간 미상·연도 경계 확인 |
| 8 종류별 각주 | 미해결 | 종류별 문구는 추가됐지만 모델 근거 기간을 잘못 선택 |
| 9 주석·PSR-12 | 미해결 | BodyInspector 조건문은 정리됐으나 생성자 주석 일부 누락 |

선별 PHPUnit **9개·68개 검증 통과**. 두 DOCX의 SHA-256은 워커 리포트와 일치합니다. 정수 반올림도 `13000.25 → 13,000`, `13000.5 → 13,001`로 확인했습니다. 전체 test/stan·서비스 기동 검사는 재실행하지 않았습니다. 파일 수정과 `.env` 접근은 하지 않았습니다.

VERDICT: FAIL