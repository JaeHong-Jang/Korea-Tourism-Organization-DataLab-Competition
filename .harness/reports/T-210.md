## T-210 결과: PARTIAL

- 변경 파일: `api/{app.py,routes/weather.py}`, `data/weather/{cache,client,transport,service,stale_cache,regions}.py`, `tests/api/weather/{conftest,test_routes,test_fallback}.py`, `tests/data/weather/test_service.py` — 모두 forecast 허용 경로 안입니다.
- 수용 기준: **전체 1,517개·날씨 192개 통과**, Ruff·포맷 검사 통과. 기본 pytest는 `TestClient.__enter__`에서 시간 초과되어, 기존 uvloop를 적용한 [검증 러너](/tmp/t210_uvloop_pytest.py)로 실행했습니다.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 정보 없음은 정본에 맞춰 `source: "없음"`으로 반환합니다. 중기는 [최저·최고기온](https://www.data.go.kr/data/15059468/openapi.do)을 조회·캐시하지만, 단일 `temp`는 null로 유지했습니다. 최저·최고 필드 추가를 제안합니다.
- BLOCKED: 중기 기온의 응답 필드 또는 단일 `temp` 산식 결정.
- 남은 일·주의: 기상청 실제 호출 **0건**. 9/26 00시 이후 forecast 기동 후 재현:

```bash
curl -fsSG 'http://127.0.0.1:8010/v1/weather' \
  --data-urlencode 'lat=37.57' \
  --data-urlencode 'lng=126.98' \
  --data-urlencode "at=$(date -Iseconds)"
```