p95 **53.7 → 170.2ms, 약 3.17배 회귀**를 확인했습니다. 아래 비용 순위는 코드 기반 추정입니다. Three 형상을 메모리에서 생성해 삼각형 수를 계산했으며, `renderer.info` 실측은 로컬 연결이 `EPERM`으로 차단되어 수행하지 못했습니다.

- [Med] [festival-models/model-part.tsx:14](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/festival-models/model-part.tsx:14) — **성능 원인 1순위 추정: 개별 메시와 반복 그림자 패스.** 불꽃·공연·먹거리·꽃·대학·전통·기타의 부품 수는 각각 `4·6·6·7·4·3·3개`. 견본 분포 `5·5·4·4·4·4·4건`을 적용하면 모형 **142메시**, 깃발·깃대 **60메시**다. 인형 2메시까지 기본 패스 **204회**, high 낮의 그림자 패스까지 **최대 408 draw calls/frame 추가**된다. 기존 지형까지 합치면 장면 코드 기준 약 **37→445회**, 그림자 캐스터는 **17→221개 객체**다. 정적 장면이어도 그림자는 기본 `autoUpdate=true`로 재생성된다 — 반복 부품을 병합·인스턴싱하고, 장면·광원 변경 때만 그림자를 갱신한 뒤 비교 측정할 것. 수치는 모든 객체가 해당 절두체에 포함되는 조건이다.

- [Low] [crowd/doll-crowd.tsx:82](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/crowd/doll-crowd.tsx:82) — **성능 원인 2순위 추정: 인형의 삼각형 처리량.** 몸통·다리 36개와 머리 32개로 인형당 68삼각형, 2,000개면 **136,000삼각형/패스**다. 모형·깃발 2,720개를 더하면 **138,720개/패스**, 그림자 포함 **277,440개/프레임 추가**다. 인스턴싱은 draw call을 줄이지만 이 처리량은 남는다 — 그림자 캐시와 거리별 형상 단순화 효과를 분리 측정할 것. `instanceMatrix.needsUpdate`는 49행의 effect에서 입력 변경 시에만 설정되므로 **매 프레임 행렬 업로드가 원인은 아니다.**

- [Med] [name-tag.tsx:49](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/name-tag.tsx:49) — **성능 원인 3순위, 카메라 이동 중에 한정.** 매 프레임 배열·객체를 만들고 표시 ID가 같아도 `setVisible`을 호출해 React 렌더를 유발한다. AGENTS §5의 매 프레임 할당 금지에 어긋난다. 설치된 drei `Html`은 표시 이름표마다 매 프레임 투영하지만, 좌표·줌이 그대로면 DOM 스타일 갱신을 생략한다. 따라서 정지 상태의 30개 이름표 DOM 갱신을 주원인으로 볼 근거는 없다 — 계산 버퍼를 재사용하고 표시 ID가 바뀔 때만 상태를 갱신할 것.

- [Med] [name-tag.tsx:21](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/name-tag.tsx:21) — 이름표 충돌 높이 **38px**가 실제 CSS 높이 **약 47.2px**보다 작다. 같은 x에서 y 차이가 40px인 4등급·1등급 입력을 실행하면 둘 다 반환되어 약 7px 겹친다 — 실제 DOM 크기와 충돌 기준을 일치시키고, 이 경계 사례에서도 높은 등급만 남는지 검증할 것.

- [Med] [crowd/doll-layout.ts:30](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/crowd/doll-layout.ts:30) — 행사 내부 간격만 확보하고 다른 행사 군중과의 충돌은 검사하지 않는다. 동일 좌표·각 `peakP50=100000`인 두 행사를 실행하면 모형 중심은 15단위 떨어지지만 인형은 각각 1,000개가 되어 **몸통이 겹치는 쌍 187개**가 발생한다 — 인형 수에 따른 군집 반경을 배치에 반영하거나 행사 간 점유 검사를 추가할 것.

- [Med] [festival-scene.tsx:73](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/festival-scene.tsx:73) — 접근성 목록은 항상 시각적으로 숨겨져 있고 포커스 가능한 요소가 없어 일반 키보드 사용자가 확인할 경로가 없다. 또한 [mini-korea-canvas.tsx:229](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/mini-korea-canvas.tsx:229)의 WebGL 미지원·경계 오류 분기에서는 목록 자체가 사라진다 — 목록을 Canvas 성공 여부와 독립적으로 제공하고, 키보드로 펼쳐 확인할 수 있게 할 것.

- [Low] [crowd-scale.ts:31](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/crowd-scale.ts:31) — 상한을 만족하지 못해도 `N=1e12`에서 종료해 결과를 반환한다. 501개 행사·low 입력은 실제로 501개를 반환한다. 행사별 최소 1개와 상한 500개를 동시에 만족할 수 없는 입력이다 — 지원 목록 크기나 초과 처리 정책을 명시하고, 상한을 충족한 결과처럼 반환하지 말 것.

- [Low] [scene-legend.css:8](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/scene-legend.css:8) — `padding: 5px 8px`가 간격 토큰 전용 규칙을 위반한다. [festival-scene.tsx:49](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/festival-scene.tsx:49)도 훅·3D 레이어·DOM 목록을 한 파일에 두어 §5의 컴포넌트별 파일 분리 규칙과 맞지 않는다 — 간격 토큰을 사용하고 책임별로 분리할 것.

준수 확인: 모형 7종 매핑, 배치·행렬·8색 선택 결정성, 빈 목록 0개, 범례와 HonestNote 통합을 확인했습니다. 견본의 `(N, 인형 수)`는 high `(1000, 2000)`, medium·low `(5000, 400)`으로 상한을 충족합니다. 인형은 명시적 dispose, 모형은 R3F 해제 경로가 있으며 명백한 누수는 발견하지 못했습니다. 색 하드코딩 검색은 0건입니다.

낮·밤·확대 스크린샷을 확인했고, 코드에는 특정 상표의 고유 표식이나 부품을 재현한 근거가 없습니다. 다만 확대본은 테스트가 Canvas 영역을 400%로 바꾼 결과여서 정상 줌 검증 근거로는 제한적입니다. **lint·타입 검사·읽기 전용 함수 검증은 통과**했습니다. 전체 테스트·빌드·e2e·성능 스크립트는 재실행하지 않았으며, 파일 수정과 `.env` 접근은 하지 않았습니다.

VERDICT: FAIL