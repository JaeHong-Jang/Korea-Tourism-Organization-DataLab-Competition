## T-204 결과: PARTIAL

- 변경 파일: `analytics/{baseline,similar}.py`, `api/assemble/{forecast,http,history}.py`, `tests/api/{test_baseline,test_similar,test_predict_evidence,test_knowledge_predict}.py` — 모두 forecast 서비스 내부.
- 수정 내용: 완전한 28일 평시 창 탐색·`NO_COMPLETE_WINDOW` 응답, 순위 밖 전회차 근거 포함·중복 제거, 유사도 점수·비교 문장 추가.
- 수용 기준:
  - `uv run --package crowdcast-forecast pytest -q services/forecast/tests` → **1112 통과, 1 실패**.
  - `ruff check services/forecast`, `git diff --check` → 통과.
  - `node scripts/dev.mjs --check --only forecast` → `✓ forecast`.
  - 실제 영종 HTTP 20회 → 응답 바이트 동일, **p95 0.902초**. 관측 3개, 근거 5종, `modelVerdict=미검증`.
  - develop knowledge를 별도 프로세스·메모리 저장소로 실행: 실제 영종 행사 → 유사 사례 → 평시 → 예보 적재 모두 200, 게이트 A 통과.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 상위 5건 밖 전회차 정의를 전달할 `forecast.relatedCases[]`(`similar-event` 형식)와 계약·knowledge 참조 등록 지원 제안.
- BLOCKED: 동점 6건 테스트에서 전회차 근거는 포함되지만 knowledge가 **`caseEventId`·`quantityIds` 정의 없음으로 422**를 반환한다. 현재 계약은 해당 정의를 예보에 담을 수 없으며, 계약·knowledge는 수정 허용 범위 밖이다.
- 남은 일·주의: 해당 실패를 [회귀 테스트](/home/data/crowdcast-wt/L2/services/forecast/tests/api/test_knowledge_predict.py:63)에 남겼다. 계약과 knowledge 연계 수정 후 재검증이 필요하다.