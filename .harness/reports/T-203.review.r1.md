1회차 확인 결과: **① 행사 입력 피처 복원은 부분 해결**, ② 구간 판정의 사유·근거 `%` 제거 해결, ③ G0 3종 해시 대조는 신규 산출물에서 해결·구 산출물 전환 경로에서 미해결, ④ 테스트 이름 충돌 해결, ⑤ 연계 파일 2개 생성은 해결·완료 표시 순서는 미해결입니다. B0 유형 중앙값은 `rolling_split`의 학습 구간(평가 연도 Y−2까지)에서만 계산합니다.

[High] services/forecast/src/crowdcast/features/event_features.py:29 — 행사 마스터의 예산·유형 등을 공개일 없이 모두 D-14 입력으로 인정합니다. 2025년 평가 대상 86건 중 85건에 예산이 있지만, 86건 모두 일정 공개 시각이 기록되지 않았습니다. 발표 방문객 81건은 마스터에 있어도 **피처에는 직접 들어가지 않으며**, 실제 개최 여부도 피처로 읽지 않습니다. 다만 예산과 사후 수정 일정이 D-14에 알려진 값인지 입증할 수 없어 누수 없는 백테스트라고 판정할 수 없습니다 — 당시 행사 입력 스냅샷을 대조하고, 입증되지 않은 필드는 결측 처리하세요.

[Med] services/forecast/src/crowdcast/features/calendar_features.py:24 — `holiday_calendar.available_at`을 버리고 휴일·연휴 피처를 계산합니다. D-14 이후 지정된 공휴일도 들어올 수 있습니다 — 외부 달력의 공개 시각을 검사하고 사후 공개 달력 테스트를 추가하세요.

[Med] services/forecast/src/crowdcast/models/g0.py:89 — 구 형식 G0에서 새 형식으로 넘어갈 때는 `labels`·`events`만 비교합니다. 이 경로에서는 `labels_g0.json` 변경을 차단하지 못합니다 — 세 번째 해시를 검증할 수 없으면 전환을 중단하세요.

[Med] services/forecast/src/crowdcast/models/__main__.py:174 — 산출물 해시 기록이 성공하기 전에 `latest.json`을 갱신합니다. 해시 기록이 실패하면 미완료 실행이 최신 성공 실행으로 표시됩니다 — 모든 산출물 검증이 끝난 뒤 완료 표시를 발행하세요.

파일은 수정하지 않았습니다. 수용 기준 테스트는 직접 재실행하지 않았으며, 워커 리포트의 통과 기록과 코드·산출물을 대조했습니다.

VERDICT: FAIL