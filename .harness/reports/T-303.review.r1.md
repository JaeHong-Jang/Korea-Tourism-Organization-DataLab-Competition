## 피드백 7건

[High] services/gateway/src/team/lead/gates.ts:4 — ① 해결: S04를 포함한 7개 shape를 검사하고, 제목·발행기관 누락 시 숫자를 차단하는 테스트가 있다(team-failures.test.ts:12). — 추가 조치 없음.

[High] services/gateway/src/team/runtime/fake-forecast.ts:23 — ② 미해결: 기본값은 `live`로 바뀌었지만, `fake`의 장소 검사는 `씨사이드파크` 부분 문자열만 본다. 픽스처 밖 장소에도 예시 수치가 연결될 수 있다. — 장소 후보와 예보 요청을 동일한 허용 행사 조건으로 제한한다.

[Med] services/gateway/src/team/analysis/draft-answer.ts:53 — ③ 해결: 요금·예산은 미상값을 쓰고, 시각은 한 번 묻는다. 영종 문장의 질문 2개를 검증한다(team-questions.test.ts:11). — 추가 조치 없음.

[Med] services/gateway/src/team/analysis/dictation-step.ts:28 — ④ 해결: 빈 유형만 원문의 단일 명시 유형어로 보완하고, 복수 유형은 채우지 않는 테스트가 있다(team-questions.test.ts:75). — 추가 조치 없음.

[Med] services/gateway/src/team/analysis/local-guide.ts:71 — ⑤ 해결: 행사 `/facts` 성공 후 `ready`를 호출해 분석 조건을 고정한다. 적재 실패와 부분 적재 재시도 테스트가 있다(team-retry.test.ts:7, 37). — 추가 조치 없음.

[Med] services/gateway/src/team/runtime/events.ts:80 — ⑥ 해결: 전송·trace 쓰기에 취소와 짧은 종료 예산을 적용하고, `run-request.ts:67`의 `finally`가 잠금을 해제한다. 멈춘 trace와 SSE 소비에 대한 재사용 테스트가 있다(team-io.test.ts:10, 52). — 추가 조치 없음.

[Low] services/gateway/src/clients/request-json.ts:45 — ⑦ 해결: `bodySchema`가 있으면 `undefined`도 검증하며, fetch 전에 거부하는 테스트가 있다(outbound-validation.test.ts:13). — 추가 조치 없음.

## 추가 결함과 경계 확인

[High] services/gateway/src/team/runtime/fake-forecast.ts:23 — `부산 씨사이드파크`도 영종 장소 후보를 받고, 같은 파일의 38–40줄을 통과해 불꽃 행사에 예시 수치를 받을 수 있다. — 장소 문자열의 지역 불일치를 거부하고 이 경로를 회귀 테스트에 추가한다.

[Med] services/gateway/src/team/analysis/event-time.ts:22 — 17시 정각은 올바르게 `야간`이지만, 10시 시작·다음 날 01시 종료는 종료 날짜를 버려 `주간`이 된다. — 한국 현지 날짜까지 포함해 18시 경계를 비교하고 자정 넘김 테스트를 추가한다.

[Med] services/gateway/src/team/analysis/draft-answer.ts:38 — 되묻기 답에 묻지 않은 `name`·`type` 등을 넣으면 기존 초안의 확정값을 그대로 덮는다. — 되묻기에서는 미확정 필드만 병합하고, 기존 값 수정은 명시적으로 처리한다.

완결된 스트림의 `sequenceProblems(..., {mode:"new"})` 위반과 마감·연결 종료 후 잠금 미해제 경로는 코드·테스트 검토에서 확인되지 않았다. 수정이 필요한 결함이 남아 있다.

VERDICT: FAIL