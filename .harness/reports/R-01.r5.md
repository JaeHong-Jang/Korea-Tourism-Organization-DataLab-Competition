**High 1건이 있어 T-001 재작업이 필요합니다.** 4차 지적 4건은 해결됐지만, 내용 revision 변경 후 재검증 경로에 모순이 남았습니다. 파일은 수정하지 않았습니다.

제공 로그는 검사 항목 118개·오류 0개입니다. 직접 실행한 Node·PHP 스키마 43개, Node 무결성 41개·적재 순서 11개·SSE 순서 15개도 기대 판정과 일치했습니다. Python·JSON-LD는 제공 로그로 확인했습니다. 아래 결함은 추가 Node 반례로 재현했습니다.

경로는 별도 표시가 없으면 `packages/contracts/` 기준입니다.

**1) 체크리스트**

| 항목 | 판정 | 근거 |
|---|---|---|
| ① 기능·세부 기능 포함 | FAIL | 새 사실 이후 재검사를 요구하지만 candidate 검사 갱신과 통과했던 게이트 B 재실행을 금지합니다(`rules/claim-lifecycle.mjs:16`, `rules/sse-sequence.mjs:31`). |
| ② 근거·발행 상태·단위 메타데이터 | PASS | 근거 참조, 문장 상태·검사 revision, 수치 단위·시간 단위·추정·발표일 필드가 있습니다(`schemas/claim.schema.json:49`, `schemas/common.schema.json:225`). |
| ③ 세 언어 판정 일치 | PASS | 제공 로그의 스키마 픽스처 43개에서 TS·Python·PHP 판정이 일치하며 Node·PHP 재실행도 같습니다. |
| ④ 도메인 이름 | PASS | 행사·예보·근거·관측·가정·문장·판정 중심의 이름을 사용합니다. |

**2) 4차 지적 4건**

| 지적 | 상태 | 근거 |
|---|---|---|
| High — 충돌 검사가 Claim 상태 전환 차단 | 해결 | Claim에 별도 전이 규칙을 적용합니다. `draft → candidate → published`와 새 ID 재작성 시퀀스가 통과했습니다(`rules/integrity.mjs:47`, `rules/claim-lifecycle.mjs:11`). |
| Med — Event·Forecast 내용 충돌 누락 | 해결 | 두 종류도 객체 내용을 보관·비교하며, 같은 ID의 변경 반례를 거부합니다(`rules/integrity.mjs:14`, `:48`). |
| Med — 예보서 내부 가정 등록 검사 누락 | 해결 | 중첩 위치와 관계없이 문서가 정의한 가정을 수집해 기준 그래프 등록 여부를 검사합니다(`rules/integrity.mjs:170`). |
| Med — 후속 요청 SSE 오판 | 해결 | `followup` 문맥에서 기존 예보 ID를 사용하고 `B → publish`를 허용합니다. 정상 후속 요청 픽스처가 통과했습니다(`rules/sse-sequence.mjs:8`, `:12`, `:68`). |

**3) 새 결함·회귀**

- **[High] [rules/claim-lifecycle.mjs:16](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/claim-lifecycle.mjs:16>) — 내용 revision 변경 후 필요한 재검증을 저장·진행할 수 없음 —** 두 문장 중 첫 문장은 revision 6에서 candidate로 통과시키고, 둘째 문장을 거부한 뒤 새 ID로 재작성하면 revision이 7이 됩니다. 첫 문장의 발행은 오래된 검사 때문에 거부됩니다. 검사 결과를 revision 7로 갱신한 문장은 스키마를 통과하지만 **`상태 candidate 그대로 내용을 바꿀 수 없다`**로 적재가 거부됩니다. 기존 `invalid-stale-checks.json`에 최신 검사 결과를 추가해도 같습니다. 또한 통과한 B 뒤 더 높은 revision의 B를 보내는 스트림은 **`게이트 B 통과 뒤에 다시 게이트 B`**로 거부됩니다(`rules/sse-sequence.mjs:31`). 이는 `sse-events.md:47`의 “게이트 B 뒤 새 사실이 들어오면 다시 검사”와 충돌합니다. T-601·T-602·T-303이 참조 판정을 그대로 구현하면 이 재검증 흐름이 막힙니다. **본문·근거를 유지하는 candidate의 검사 갱신과 revision 변경에 따른 B 재실행 조건을 계약화하고, 부분 재작성·오래된 검사 복구 시퀀스를 추가해야 합니다.**

- **[Med] [rules/integrity.mjs:65](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/integrity.mjs:65>) — ID가 없는 평시 자료의 새 내용은 revision 계산에서 빠짐 —** `changesContent`는 지정된 ID 객체만 비교합니다. 정상 평시 자료의 `weekdayMean[0].nonlocal`과 `nonlocalShare`를 바꾼 재적재가 **스키마 통과·무결성 오류 0개·`changesContent: false`**였습니다. 근거만 먼저 적재한 뒤 평시 자료를 최초로 추가해도 `false`입니다. 실제 그래프에 새 사실이 들어와도 내용 revision이 유지됩니다. **평시·유사 사례처럼 자체 ID가 없는 적재 객체도 전체 내용을 식별·비교하고, 동일 재적재와 새 내용 추가를 구분해야 합니다.**

- **[Med] [rules/sse-sequence.mjs:34](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/sse-sequence.mjs:34>) — 게이트 B와 발행 사이 기준 그래프 버전 변경을 놓침 —** 정상 스트림에서 발행 게이트의 `masterVersion`만 1에서 2로 바꿔도 **이벤트 스키마 통과·순서 위반 0개**였습니다. 현재 검사는 revision만 비교하므로 검증·발행의 고정 범위 요구를 충분히 검사하지 못합니다(`openapi/knowledge.yaml:112`). **B의 `masterVersion`도 저장해 발행과 비교하고 버전 변경 반례를 추가해야 합니다.**

VERDICT: FAIL -> T-001