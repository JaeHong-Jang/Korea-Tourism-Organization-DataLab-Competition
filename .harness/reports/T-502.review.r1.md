[High] [services/records/src/Snapshots/Service.php:29](/home/data/crowdcast-wt/L5/services/records/src/Snapshots/Service.php:29) — `forecastId`와 내부 `forecast.id`, `card.id`, `claims[].forecastId`의 일치를 검사하지 않습니다. 정상 스냅샷 저장 후 최상위 `forecastId`만 바꾸면 다시 200으로 저장되어 동일 예보가 중복됩니다. 다른 `card.eventId`도 허용됩니다. — 요청·응답 모두 예보 ID 간 일치와 `card.eventId`의 경로 일치를 검증하고 불일치 요청은 400으로 거부하세요.

[Med] [services/records/src/Snapshots/Service.php:60](/home/data/crowdcast-wt/L5/services/records/src/Snapshots/Service.php:60) — 행사 삭제 후 활성 행사만 찾는 `events->find()`가 실패하여 기존 스냅샷 GET도 404가 됩니다. DB 외래 키와 원문은 보존되지만 API에서 발행 이력을 조회할 수 없습니다. — 스냅샷 조회에는 tombstone을 포함하는 행사 존재 검사를 사용하고, 삭제 후 조회 회귀 테스트를 추가하세요.

[Med] [services/records/src/Routes/JsonResponse.php:22](/home/data/crowdcast-wt/L5/services/records/src/Routes/JsonResponse.php:22) — 행사 픽스처의 `expectedByHost.value`를 `1e400`으로 바꾸면 POST는 200이지만, 목록 재인코딩에서 PHP의 `INF` 때문에 GET `/v1/events` 전체가 500이 됩니다. — 저장 전에 비유한 수치와 재인코딩 가능성을 검사하여 해당 요청을 400으로 거부하고 두 저장 경로에 회귀 테스트를 추가하세요.

검증: PHPUnit 11개·80 assertions 통과. PHPStan은 읽기 전용 환경에서 캐시 생성 실패로 미완료. 파일 수정 없음.

VERDICT: FAIL