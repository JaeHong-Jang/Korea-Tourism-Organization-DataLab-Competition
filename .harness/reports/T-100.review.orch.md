[Med] services/forecast/src/crowdcast/data/mcst_values.py:25 — 새 `float` 보정이 유효숫자 15자리로 값을 바꿔 수치를 잘못 처리한다. 직접 실행하면 `integer(100000000000000.12, '원') → 100000000000000`이고, 정수인 `integer(1e15, '원') → None`이다. — 원래 값을 기준으로 정확한 정수 환산을 먼저 검사하고, 부동소수 오차 보정은 허용 범위를 검증한 경우에만 적용하라.

VERDICT: FAIL