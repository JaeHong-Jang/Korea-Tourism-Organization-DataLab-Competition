## T-601 결과: DONE

- 변경 파일: `services/knowledge/ontology/master/assumptions.ttl`, `services/knowledge/tests/{contract_cases,test_conversion,test_integrity,test_sequences,test_store_api}.py`
- 반영: 모든 동시체류율 값·범위를 계획 표대로 채우고 미정 문구 제거. 가정 8개마다 숫자 필드와 `low ≤ value ≤ high` 검사 추가.
- 수용 기준: `uv run --package crowdcast-knowledge pytest -q services/knowledge/tests` → **89 passed**. `node scripts/dev.mjs --check` → **✓ knowledge**. 실행 시 UV 캐시·저장소는 `/tmp` 사용.
- 경고: **37,268개 → 1개**. rdflib 내부 경고만 해당 테스트의 메시지·모듈 한정 필터로 제외. Starlette 경고는 유지.
- 의존성 변경: 없음
- CONTRACT-CHANGE: 없음
- BLOCKED: 없음
- 남은 일·주의: 웹·records 기동은 각각 `vite`, `vendor/autoload.php` 부재로 실패. 변경 Python 파일 Ruff 검사·포맷은 통과했으며, 기존 공용 `conftest.py` 포맷 불일치는 유지. 공유 산출물 변경 없음.