[High] services/forecast/src/crowdcast/pipeline/stages.py:204 — `fetch` 게이트가 `events --offline`이 만드는 `admin_dict.parquet`을 먼저 읽어 첫 실행을 막는다. 이 사전의 방문자 코드도 현재 수집 행에서 생성되므로 전 기간 누락된 지역은 결측률 분모에서 사라진다 — 수집 결과와 독립된 기준 코드 목록을 게이트 전에 확보·검증한다.

[High] services/forecast/src/crowdcast/pipeline/gates.py:140 — 이전 실행의 `features_availability.json`만 있어도 이번 피처 명령이 아무것도 쓰지 않은 경우 통과하며, `features.parquet` 존재도 확인하지 않는다 — 검사 파일 수정 시각이 단계 시작 이후인지 확인하고 피처 산출물도 필수로 검사한다.

[High] services/forecast/src/crowdcast/pipeline/stages.py:266 — `train`·`batch`도 명령이 0으로 끝나기만 하면 기존 산출물로 게이트를 통과할 수 있다 — 이번 단계에서 생성·갱신된 산출물인지 확인하고 무출력 성공 사례를 검증한다.

VERDICT: FAIL