[High] [T-203:19](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/.harness/tasks/T-203.md:19>) G0 분기 — 골드 <30에서 단순 모델을 주 모델로 정하고 두 모델을 모두 보고하는 방향은 타당하다. 그러나 `≥60 → LightGBM`은 06 §11의 **평가 연도마다 ≥15건·환산 기준선 양쪽 각 ≥10건**을 누락한다. 총 60건이어도 나머지 조건이 부족하면 ‘일부 충족’이다 — `<30 / 모든 조건 충족 / 그 외 일부 충족`으로 분기를 완결한다. 성적에 따른 선택은 현재 명세가 금지하지만, 이를 보장하려면 평가 전에 규칙·라벨 스냅샷·주 모델을 고정하고 H2 재판정은 별도 버전으로 남겨 기존 사전 등록 예보를 보존한다.

[High] T-203:13·19, 06 §11 — 단순 모델 선택만 추가했고, 기존 확률 기반 `distribution → judge` 흐름을 어떻게 바꾸는지는 없다. 모델 카드의 표본 한계 문장만으로는 §11의 **“확률 확정 판정 대신 구간 + 규칙 판정”**을 보장하지 못한다 — 골드 <30 및 기준선 한쪽 표본 부족 시 적용할 출력·판정 방식을 명시하고, 해당 분기의 수용 기준을 추가한다.

[Med] [06 §8 labels 게이트](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/docs/plan/06_데이터_모델_검증.md:145>) — 저신호 잡음에 의한 오탐은 줄이지만, **저신호 영역의 체계적 오류와 σ 과대 산출로 신호가 사라지는 오류의 탐지력은 약해진다.** 전체 음수 비율을 참고로만 남기면 이를 막을 검사가 없다 — 학습·명절 제외 **전** 후보에서 부호 있는 원래 순증으로 검사하고, 유의 신호 0건·σ=0·SNR 결측의 처리와 신호 표본 수·비율 급락 시 중단 기준을 정한다. 지역·연도별 순증 분포 검사와 날짜·지역 조인·단위·기준선 산식의 독립 검증도 유지한다.

[Med] [06 §8 뒤 9/25 메모](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/docs/plan/06_데이터_모델_검증.md:152>), labels_qc.md:53·78 — 현재 QC는 기존 게이트 실패만 기록하며, SNR 중앙값 0.78·유의 음수 9건·연휴 원인 판단을 재현할 집계를 제공하지 않는다. 중앙값만으로 잡음 지배나 음수의 원인을 확정할 수도 없다 — 변경 게이트의 분자·분모, 유의 음수 사례별 기준선·σ·일정, 명절 제외 전후 건수를 QC에 남긴다. 연휴·계절 효과는 검증 전까지 원인 가설로 표현한다.

[Med] 06 §8 메모·§9, T-203:19 `holiday_overlap` — 오염 가능성이 큰 실버를 제외하는 것은 합리적이나, **실버에 의존하는 명절 행사 표본을 체계적으로 줄인다.** 학습 제외만 명시되어 백테스트·사전 등록에서도 빠지는지 불명확하고, 벚꽃 주말의 계절 교란은 이 규칙으로 해결되지 않는다 — 명절 날짜 범위를 고정하고 양수·음수에 동일하게 적용한다. 연도·유형·명절별 제외 수와 남은 표본을 보고하며, 명절 행사는 유효한 골드로 별도 평가하거나 채점 불가 건수로 공개한다. 사전 등록의 선정·채점 제외 규칙을 결과 확인 전에 고정하고, 비명절 성적을 명절 행사까지 일반화하지 않는다.

VERDICT: FAIL