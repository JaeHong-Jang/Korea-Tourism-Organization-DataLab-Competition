[Med] services/gateway/package.json:8 — 단독 `npm -w services/gateway run dev`는 `.env`를 읽지 않습니다. WSL에서 `.env`에 Windows Ollama 주소를 설정해도 `config.ts`의 localhost 기본값을 사용해 Ollama를 꺼진 것으로 표시할 수 있습니다 — 단독 실행 경로에서도 `.env`를 로드하고 이미 설정된 환경 변수를 우선하세요.

VERDICT: PASS