① **부분 해결**: train·backtest가 실패한 뒤에는 시작 시점의 포인터를 복원합니다. 골든 0건의 `passed=None`을 복원 대상에서 제외한 것도 요청한 정책과 맞습니다. 다만 게이트가 끝나기 전 후보 모델이 노출됩니다. ② 실제 batch 실행 전 포인터·카드·버전 검사 **해결**. ③ 백테스트 세 파일의 존재 검사 **해결**.

- [High] [models/__main__.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/models/__main__.py:164), [pipeline/__main__.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/pipeline/__main__.py:151) — train이 후보 모델을 `latest.json`에 발행한 뒤 backtest 게이트가 끝날 때까지, 별도로 실행한 batch가 그 포인터를 사용할 수 있습니다. 나중에 성적 악화로 복원해도 이미 만든 예보는 되돌릴 수 없습니다 — 후보 포인터를 별도로 두고 backtest 게이트 통과 후에만 사용 포인터로 원자 승격하세요.
- [Med] [pipeline/__main__.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/pipeline/__main__.py:148) — 실패한 단계의 산출물 해시에 후보 `latest.json`을 기록한 다음 포인터를 복원하므로, 실행 기록의 해시가 실제 파일과 달라집니다. 앞서 통과한 train 기록도 같은 영향을 받습니다 — 복원 시 기록을 정정하거나, 변경 가능한 완료 포인터를 불변 산출물 목록에서 제외하세요.

파일 수정과 `.env` 열람 없이 정적 검토했습니다. `git diff --check b700664^ b700664`는 통과했고 테스트는 실행하지 않았습니다.

VERDICT: FAIL