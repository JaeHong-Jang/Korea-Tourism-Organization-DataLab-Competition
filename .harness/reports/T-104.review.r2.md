1. **해결:** `latest.json`의 `finishedAt`과 `runId`를 확인해 같은 `runId` 재실행을 허용합니다.
2. **부분 해결:** 경계와 부모 시 상수로 기준 코드를 만들고 수집 전에 읽습니다. 경계 목록의 완전성 검증은 부족합니다.
3. **미해결:** 필수 산출물의 수정 시각을 검사하지만 백테스트의 `latest.json`은 예외입니다.
4. **해결:** `train`·`batch`의 무출력 성공을 실패로 처리합니다.

[High] services/forecast/src/crowdcast/pipeline/stages.py:256 — 백테스트는 필수 산출물 표에 있는 `latest.json`의 수정 시각을 검사하지 않습니다. 기존 파일의 `finishedAt`이 단계 시작보다 미래이면 명령이 아무것도 쓰지 않고 0으로 끝나도 통과합니다 — `finishedAt` 검증과 함께 `latest.json`의 수정 시각도 단계 시작 이상인지 확인하세요.

[Med] services/forecast/src/crowdcast/pipeline/gates.py:43 — 형식이 맞는 2025 경계라도 시군구 일부가 빠지면 그대로 기준 목록으로 사용해 결측률 분모를 줄입니다 — 정본의 예상 시군구 252개와 코드 목록 완전성을 검증하세요.

VERDICT: FAIL