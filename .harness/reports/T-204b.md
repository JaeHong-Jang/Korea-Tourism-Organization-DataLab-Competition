## T-204b 결과: DONE

- 변경 파일(이번 보정): `configs/thresholds.yaml`, `services/forecast/src/crowdcast/analytics/upcoming.py`, `services/forecast/tests/analytics/test_upcoming_publish.py`, `services/forecast/tests/rules/test_interval_basis.py`.
- 수정 내용: 자체 판정 문구를 교체하고, runId에 실제 사용한 캐시 설정 객체의 정렬 JSON을 반영했습니다. 경계 표본·캐시 재로딩·설정 키 순서 테스트를 통과했습니다.
- 수용 기준:
  - 기본 `uv run --package crowdcast-forecast pytest -q services/forecast/tests` → 첫 API `TestClient` 진입에서 대기. 진단 실행도 40초 시간 초과(124).
  - `uv run --package crowdcast-forecast python /tmp/t204b_pytest_backend.py -q services/forecast/tests` → uvloop 적용, **1,199개 통과**.
  - `uv run --package crowdcast-forecast ruff check services/forecast`, `git diff --check` → 통과.
  - uv 실행에는 쓰기 가능한 `UV_CACHE_DIR=/tmp/crowdcast-t204b-uv-cache`, `UV_OFFLINE=true`를 적용했습니다.
- 실제 확인: 소래포구 10/2 `/v1/predict` → **200**. p10/p50/p90은 일평균 **5,857 / 11,162 / 32,149**, 순간 최대 **2,002 / 4,157 / 12,417**, `value`는 모두 `null`. 등급 **3**, 확률 **0.9975 / 0.4155**는 기존과 같습니다. `oodReasons`는 `["학습 범위 밖 피처: hazard_fireworks"]`입니다.
- 사유 문구: 구간 꼬리 없이 다음 자체 문구를 확인했습니다.  
  “자체 기준 — 예측으로는 순간 최대가 법정 인원 기준에 닿을 가능성이 높아 수립 대상으로 봅니다. 참고용 — 담당자 검토 필수”
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 없음.
- BLOCKED: 없음.
- 남은 일·주의: 기본 pytest는 오케스트레이터 환경에서 재확인이 필요합니다. 일괄 예보는 재실행하지 않았으며, 기존 산출물 세 파일의 해시는 이전 기록과 같습니다. 통합 후 재실행하면 됩니다.