이전 리뷰 **High 3건·Med 1건은 모두 오케스트레이터 판정에 맞게 해결**됐습니다.

- 취소·연기는 선정에서 필터링하지 않고, 채점 때 명시적 상태 파일로 처리합니다. 출처와 확인 시각도 검증합니다.
- 공개 메타는 `--send` 성공 후에만 생성하며, 채점 전에 준비본 전체 바이트의 해시를 대조합니다.
- 매 POST 직전에 KST 등록일을 재검사하고, 중단 시 전송 상태를 기록합니다.
- 채점 전에 원장과 준비본의 `forecastId` 전체 집합을 대조합니다.

[High] [select.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/scoring/select.py:162) — 행사 마스터와 T-205 예보의 비교가 요약 필드에 한정됩니다. T-205 이후 `hazards`나 `timeOfDay`가 바뀌어도 등록 준비본에 변경된 행사 스냅샷이 들어갈 수 있고, 채점의 등급 비교가 원래 예보와 다른 행사 조건을 사용하게 됩니다. 원래 [예보 ID 생성식](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/api/assemble/forecast.py:57)에 행사 전체 입력이 포함되므로, 등록 전에 현재 스냅샷으로 ID를 재계산해 JSONL의 ID와 대조하세요.

공개 경로 검사에서는 T-207 테스트의 쓰기가 임시 경로에 격리돼 있고, 선정 명령은 `RULES.md`만, `--send`는 승인된 메타 파일만 쓰는 것으로 확인했습니다. 기존 [ledger-export.php](/home/data/crowdcast-wt/L2/services/records/bin/ledger-export.php:25)는 별도 실행 시 `ledger.csv`를 쓰지만 이번 변경이나 테스트에서 호출되지 않습니다. 정적 검토만 수행했습니다.

VERDICT: FAIL