R-01은 **T-001 수정 후 재검토가 필요합니다.** 파일은 수정하지 않았습니다.

| 체크리스트 | 판정 | 근거 |
|---|---|---|
| ① 기능·세부 기능에 필요한 데이터 | **FAIL** | 예보서 재조회, 작업 기록, 근거 통계 등의 응답이 누락되거나 구체적인 필드 없이 선언되어 있습니다. |
| ② 근거·발행 상태·단위 메타데이터 | **FAIL** | 기본 필드는 있으나 JSON-LD 변환 시 관계가 소실되고, S07·S08·S10·S12에 필요한 정보와 단위 제약이 부족합니다. |
| ③ 세 언어 판정 일치 | **PASS** | 기존 픽스처 11개에서 TS·Python 3.12·PHP 모두 기대 판정과 일치했습니다. |
| ④ 도메인 용어 사용 | **PASS** | 행사·예보·판정·근거 중심의 이름을 사용합니다. 다만 `kind`의 JSON-LD 의미 충돌은 아래 수정 대상입니다. |

검증: `npm -w packages/contracts run check`는 `uv`의 캐시 잠금 생성이 읽기 전용 환경에서 거부되어 종료 코드 1이었습니다. TS·PHP 결과를 확인하고, 설치된 Python 3.12 환경에서 `check/python_check.py`를 직접 실행해 동일 판정을 확인했습니다. 이는 **기존 픽스처의 일치**이며 S01~S12 통과 증명은 아닙니다.

아래 경로는 `packages/contracts/` 기준입니다.

1. **[High] `jsonld/context.jsonld:11` — JSON에서 그래프로 옮길 때 타입과 관계가 소실됨 —** 정상 Claim·Evidence·Forecast 픽스처에 컨텍스트를 붙여 RDF로 변환하면 `rdf:type`이 모두 0개입니다. `source`, `observations`, `evidence`, `stepId`도 매핑되지 않아 데이터셋·관측값·작업 식별자가 보존되지 않습니다. Event의 지역도 필요한 `cc:inRegion` 대신 `cc:aboutRegion`으로 매핑됩니다. 클래스 부여와 중첩 객체 분해 규칙을 계약화하고, 변환 결과를 검사하는 픽스처를 추가해야 합니다(S03·S04·S09·S11).

2. **[High] `jsonld/context.jsonld:22`, `schemas/claim.schema.json:105`, `openapi/knowledge.yaml:58` — S12 검사 종류·세션 revision·검증 범위가 불완전함 —** Check의 `kind`가 `cc:checkKind`가 아닌 `cc:valueKind`로 변환되고, 모든 `revision`은 `cc:checksRevision`으로 매핑되어 Session의 `cc:revision`과 구분되지 않습니다. 검증·발행 계약에는 고정한 기준 그래프 버전을 확인할 필드도 없습니다. Check와 Session 매핑을 분리하고, 검증에 사용한 세션 revision·기준 그래프 버전을 발행까지 묶어 검증하도록 명시해야 합니다.

3. **[High] `schemas/common.schema.json:76`, `schemas/evidence.schema.json:99` — S07·S08·S10에 필요한 대상 노드 정보가 부족함 —** PredictionRun은 `modelVersion`만 있고 `prov:used`로 연결할 ModelRun 식별자가 없습니다. Assumption은 ID만 전달하며 적용 값·범위의 구조화된 계약이 없고, ModelEvidence·CheckEvidence에는 `aboutForecast` 참조가 없습니다. ModelRun·Assumption 계약과 명시적 참조를 추가해 모델 계보, 환산 가정, OOD 예보의 “참고용” 근거를 연결해야 합니다.

4. **[High] `schemas/judgment.schema.json:26`, `jsonld/context.jsonld:37` — 법정 판정의 규칙→조항 연결이 보존되지 않음 —** `ruleId`는 항상 `cc:aboutRule`로 매핑되어 Judgment의 `cc:judgedBy`와 구분되지 않고, Evidence의 `clauseId`는 Rule이 아닌 Evidence에 붙습니다. 법정 사유의 `clauseId: null`도 스키마가 허용합니다. Judgment→Rule→LegalClause를 별도 노드 관계로 정의하고 법정/자체 조건을 검증해야 합니다(S05·S06). 정상 예보 픽스처의 자체 판정·체크리스트도 ModelEvidence 대신 해당 RuleEvidence를 참조하도록 수정해야 합니다(`fixtures/forecast/valid-yeongjong.json:88`).

5. **[High] `schemas/common.schema.json:7`, `schemas/claim.schema.json:75` — ID 참조의 해석·존재·타입 규칙이 없음 —** 공통 ID는 문자열 형식만 제한하며 종류별 IRI 변환, 세션/기준 그래프 조회 범위, 참조 대상 타입을 정하지 않습니다. 실제로 없는 `observationIds`를 넣은 예보도 스키마 검사를 통과합니다. Quantity 자리표시자 역시 대상 필드의 존재·비-null 여부가 보장되지 않습니다. ID 정규화 규칙과 그래프 적재 시 참조 무결성 검사를 정의하고, 끊어진 참조·잘못된 타입·다른 세션 참조 픽스처를 추가해야 합니다.

6. **[High] `schemas/common.schema.json:31`, `schemas/forecast.schema.json:44`, `schemas/backtest-summary.schema.json:157` — 일평균 정답과 환산값의 단위 계약이 약함 —** `dailyMean`에 `unit: "원"`, `timeUnit: "기간누적"`을 넣거나 모든 수치 필드를 제거해도 통과합니다. 순간 최대도 가정 없이 `estimated: false`가 가능합니다. 공통 Quantity에는 §06-2의 `announcedAt`이 없고 골든 사례의 `model`에는 단위 메타데이터가 없습니다. 일평균·순간 최대 전용 제약, 필수 분위수, 두 환산 가정, 발표 시점과 비교 단위를 명시해야 합니다.

7. **[High] `schemas/region-baseline.schema.json:62`, `schemas/similar-event.schema.json:64`, `schemas/insight.schema.json:85` — 결과의 근거 ID만 있고 근거 조각을 받을 수 없음 —** 해당 응답은 `evidenceId(s)`만 제공하고 §06-12가 요구하는 `evidence[]`를 담지 않습니다. 이 ID로 근거를 직접 조회하는 API도 없습니다. 게이트웨이가 출처·기간·공개일을 그래프에 적재할 수 있도록 응답에 근거 조각을 포함해야 합니다. DataEvidence의 기간·수치 참조도 구조화하고 정상 픽스처로 검증해야 합니다.

8. **[High] `sse-events.md:12`, `schemas/sse-event.schema.json`의 `forecast` 분기 — 발행 전 근거·설명이 전송됨 —** 이벤트명 순서는 대체로 §10-3과 맞지만, 게이트 A 직후의 `forecast`가 전체 Forecast를 참조합니다. 정상 픽스처만으로 근거 카드 4개와 요인 설명 문장이 발행 전에 전송됩니다. 게이트 A용 숫자·판정 응답을 분리하고, 설명·근거는 발행 후 전송하도록 해야 합니다. 순서 예시에도 게이트 B 뒤 카드장인·브리핑 단계를 명시하고 발행 전 전송 금지 시퀀스를 검증해야 합니다.

9. **[High] `openapi/gateway.yaml:111`, `openapi/records.yaml:80` — 저장·재조회로 완전한 예보서를 복원할 수 없음 —** 예보서 조회와 스냅샷 모두 Forecast만 반환하며 발행 Claim, 세션 연결, 행사 상세, 유사 행사·평시 자료, 배치·요약을 포함하지 않습니다. 새로고침이나 공유 링크에서 M3 예보서를 재현할 계약이 부족합니다. 발행된 예보서 응답과 불변 스냅샷 계약을 정의해야 합니다. 초안 역시 `plan-section.schema.json:39`의 빈 `claimIds`와 독립적인 `body`만으로 내보내지 않도록 발행 Claim과 본문·잠금 수치의 연결 규칙을 명시해야 합니다.

10. **[Med] `openapi/knowledge.yaml:140`, `openapi/forecast.yaml:265`, `schemas/agent-status.schema.json:15` — 세부 기능의 데이터 계약이 미완성임 —** 근거 통계·사전 등록 채점·실행 기록은 구체적인 필드 없는 `object`이고, 팀원 작업의 입력 요약·출력 근거·소요시간 계약도 없습니다. M2-F3-c, M6-F4~F6, M7-F2, M8에 필요한 데이터가 합의되지 않은 상태입니다. 기능별 응답 스키마와 정상·빈 값·오류 픽스처를 추가해야 합니다.

VERDICT: FAIL -> T-001