## T-306c 결과: PARTIAL

- 변경 파일: [forecasts.ts](/home/data/crowdcast-wt/L3/services/gateway/src/routes/forecasts.ts), [followup-plan.ts](/home/data/crowdcast-wt/L3/services/gateway/src/team/lead/followup-plan.ts), [forecast-plan.test.ts](/home/data/crowdcast-wt/L3/services/gateway/tests/forecast-plan.test.ts), [team-fixture.ts](/home/data/crowdcast-wt/L3/services/gateway/tests/team-fixture.ts).
- 수용 기준:
  - `npm -w services/gateway test` → 911 통과·1 건너뜀·3 실패. 기존 HTTP 테스트가 `listen EPERM`으로 실패. 신규 16개는 모두 통과하며, 재요청·상담 경로와 계획 바이트 일치를 확인.
  - `npm -w services/gateway run build`·`npm -w services/gateway run lint` → 통과.
  - `node scripts/dev.mjs --check --only gateway` → 종료 코드 1. `tsx` IPC 소켓 생성이 `EPERM`으로 차단됨.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: `gateway.yaml`에 `POST /api/forecasts/{forecastId}/plan` 추가 필요. 본문 없음. 200 `{plan: <plan 계약>, docxHref: "/api/plans/{id}/export.docx"}`, 404 `{"error":"not_found"}`, 502 `{"error":"plan_rejected"}`.
- BLOCKED: 샌드박스 소켓 제한으로 HTTP 테스트·기동 검사 통과 확인 불가.
- 남은 일·주의: 구현 완료. 소켓 허용 환경에서 실패 명령 재실행 및 오케스트레이터의 계약 반영 필요.