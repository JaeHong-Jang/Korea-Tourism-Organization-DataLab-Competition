[Med] [pages.css:64](/home/data/crowdcast-wt/L4b/apps/web/src/styles/pages.css:64) — **새 결함: 태블릿에서 KPI 패널 폭이 무너집니다.** 좌우 여백 합이 764px이므로 768px 화면에서는 패딩을 제외한 콘텐츠 폭이 0이 됩니다. 기존 태블릿 규칙은 선택자 우선순위가 낮고, 세로 쌓기는 640px 이하에만 적용됩니다. — 좁은 화면에서 쌓기 또는 별도 폭 규칙을 적용하고 768·820px 배치를 검증하세요.

| 피드백 | 해결 여부 |
|---|---|
| 좌하단 비움·KPI 아래 가운데 | 기준 데스크톱 화면에서 해결. 태블릿은 위 결함 발생 |
| 모바일 쌓기·제주 마커 | 해결. 390px 스크린샷에서 순서·제주 선택 강조 확인, e2e 코드도 강제 클릭 제거 |
| 시작일 타임스탬프 정렬 | 해결. 날짜 순·위험 순 동률 회귀 테스트 통과 |
| 시도 17개 선택지 | 해결. 전국 포함 18개 선택지와 대구 `(0건)` 선택 유지 확인 |
| 목록 값 줄·값 표 | 해결. p10–p90 텍스트와 펼칠 수 있는 값 표 제공 |

검증: 스크린샷 4장·해시 일치, 타입 검사·lint 통과. 환경 파일 로딩을 끈 테스트에서 **81개 통과**, 관련 7개 모두 통과했습니다. 나머지 1개는 기존 투영 테스트의 `spawnSync git EPERM`으로 실패했습니다. build·e2e는 재실행하지 않았습니다. 파일 수정과 `.env` 접근 없음.

VERDICT: FAIL