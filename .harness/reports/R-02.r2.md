## 1) 1차 지적 4건

| 지적 | 재검토 결과 |
|---|---|
| `/validate`에 `{}` 전송 | 해결. 본문 없는 POST를 명시하며 실제 knowledge 라우트와 일치합니다. |
| 오류 본문 전달 | 해결. `/facts`의 422 본문을 `gate-report`로 검증해 상태와 함께 전달합니다. 계약에 본문 스키마가 없는 409는 상태만 전달합니다. |
| knowledge 응답의 런타임 검증 | 해결. 성공·오류 응답 모두 반환 직전에 검증합니다. |
| `dev.mjs` 포트 | 실행 명령과 health 주소의 포트는 일치합니다. 다만 `.env` 해석 차이로 유효한 포트 설정이 기동을 깨뜨리는 문제가 남았습니다. |

## 2) 체크리스트

| 항목 | 판정 | 근거 |
|---|---|---|
| §5 주석·파일 분리·이름 | PASS | 확인한 소스는 책임별로 분리됐고 300줄 이하이며 한국어 주석을 갖췄습니다. |
| 요청·응답 계약 검증 | PARTIAL | knowledge의 실제 요청·응답은 검증합니다. 게이트웨이의 발신 JSON은 런타임 요청 검증 없이 직렬화됩니다. |
| 비밀·로그 | PASS | 확인한 경로에서 API 키나 응답 원문을 로그·오류 메시지에 싣는 흐름은 없었습니다. |
| 서비스별 health | PASS | 세 서비스의 `/health`와 게이트웨이의 `/api/health`가 분리되고, 게이트웨이는 서비스별·Ollama 상태를 따로 확인합니다. |

라우트 대조에서는 knowledge `/facts`·`/validate`·`/publish`의 메서드·쿼리·본문이 일치했습니다. forecast 업무 라우트, records 행사 라우트, knowledge 근거 조회 라우트는 develop의 골격에 아직 등록되지 않아 현재 호출하면 404입니다. 각각 후속 T-204·T-502·T-603 범위이므로 R-02 결함으로 세지 않았습니다. 서비스 기동과 쓰기가 생기는 테스트는 실행하지 않았고, 코드 및 읽기 전용 명령으로 확인했습니다.

## 3) 지적 사항

- [High] [scripts/dev.mjs:49](/mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/scripts/dev.mjs:49) — 수동 `.env` 파서가 따옴표와 줄 끝 주석을 값에 남깁니다. `GATEWAY_PORT="8788" # 개발 포트`는 단독 게이트웨이의 `parseEnv`에서는 `8788`이지만 실행기에서는 숫자로 변환되지 않아 기동이 실패합니다. 읽기 전용 재현에서도 두 값이 달랐습니다. — 실행기도 `node:util`의 `parseEnv`로 읽고, 사용자 지정 포트와 데이터 루트 사례를 검증하십시오.
- [Med] [request-json.ts:53](/mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/services/gateway/src/clients/request-json.ts:53) — 발신 본문은 TypeScript 타입만 거쳐 `JSON.stringify`됩니다. 런타임 값이 계약을 어겨도 전송됩니다. — 본문이 있는 클라이언트 호출에서 해당 요청 스키마를 Ajv로 검증한 뒤 전송하십시오.
- [Low] [Controller.php:16](/mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/services/records/src/Health/Controller.php:16) — records health 응답은 테스트에서만 계약을 검사하며 실제 반환 경로는 `ContractValidator`를 사용하지 않습니다. — 반환 전 OpenAPI health 스키마를 검사하십시오.

VERDICT: FAIL -> T-001