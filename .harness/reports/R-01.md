**T-001 수정 후 재검토가 필요합니다.** 파일은 수정하지 않았습니다.

제공 로그의 41개 검사 항목은 모두 통과했습니다. 직접 재실행한 TS(Ajv)·PHP(opis)의 스키마 픽스처 32개도 기대 판정과 일치했습니다. 다만 추가 Node 반례에서 아래 계약 불일치를 확인했습니다. 아래 경로는 별도 표기가 없으면 `packages/contracts/` 기준입니다.

1) 체크리스트

| 항목 | 판정 | 근거 |
|---|---|---|
| ① 기능·세부 기능 포함 | **FAIL** | Event 적재 경로가 없어 S09의 행사 지역 대조를 구성할 수 없고, M7-F2·M8-F2/F3 데이터 계약도 남아 있습니다. |
| ② 근거·발행 상태·단위 메타데이터 | **FAIL** | 필드는 크게 보완됐지만 허용된 적재 종류의 변환 규칙과 참조 검사에 누락이 있습니다. |
| ③ 세 언어 판정 일치 | **PASS** | 제공 로그에서 TS·Python·PHP의 32개 스키마 픽스처 판정이 일치합니다. TS·PHP 재실행도 동일합니다. |
| ④ 도메인 이름 | **PASS** | 행사·예보·판정·근거·관측·가정·작업 기록에 맞는 이름을 사용합니다. |

2) 1차 지적 재검토

1. **부분 해결 — High 잔존.** Forecast·Claim의 타입, 출처·관측값·작업 관계는 추가됐습니다(`jsonld/types.json:3`, `jsonld/context.jsonld:76`). 그러나 `/facts`는 `event`와 `forecast-report`를 받지 않고(`openapi/knowledge.yaml:48`), Event 타입·지역 매핑은 `forecast-report.event`에만 있습니다(`jsonld/types.json:38`, `jsonld/context.jsonld:183`). 따라서 계약을 따르는 T-601에는 **예보 대상 Event와 지역을 적재할 경로가 없습니다**. 또한 허용된 `similar-event`·`region-baseline`에는 타입 규칙이 없고, 유사 행사의 `measured`·`announced`도 컨텍스트에 없습니다. Event 적재 경로와 허용 종류별 변환·기대 트리플을 완성해야 합니다.

2. **해결.** Check의 `checkKind`·`checksRevision`이 스코프로 분리됐고(`jsonld/context.jsonld:166`), 검증·발행 요청 모두 `revision`·`masterVersion`을 필수로 받습니다(`openapi/knowledge.yaml:85`, `openapi/knowledge.yaml:119`). 세션 revision 관리와 버전 불일치 409도 명시됐습니다(`docs/plan/09_온톨로지_근거그래프.md:91`, `.harness/tasks/T-602.md:7`).

3. **해결.** `modelRunId`, 구조화된 가정 값·범위, 모델 카드가 추가됐습니다(`schemas/common.schema.json:446`, `schemas/common.schema.json:482`, `schemas/model-card.schema.json:8`). ModelEvidence·CheckEvidence의 `forecastId`도 종류별 필수입니다(`schemas/evidence.schema.json:266`, `schemas/evidence.schema.json:311`).

4. **해결.** `judgment.ruleIds`가 `cc:judgedBy`로 연결되고(`jsonld/context.jsonld:118`), 법정 사유의 `clauseId`가 필수입니다(`schemas/judgment.schema.json:74`). Rule→LegalClause 연결은 기준 그래프 관계로 명시됐으며(`docs/plan/09_온톨로지_근거그래프.md:132`), 정상 픽스처의 자체 판정·체크리스트도 RuleEvidence를 참조합니다(`fixtures/forecast/valid-yeongjong.json:95`).

5. **부분 해결.** 종류별 ID, IRI 규칙, 일부 끊어진 참조와 비-null 자리표시자 검사가 추가됐습니다(`schemas/common.schema.json:12`, `jsonld/types.json:40`, `scripts/check.mjs:67`). 그러나 검사기는 `modelRunId`, Evidence의 `assumptionId`, Claim의 세션 일치를 확인하지 않습니다(`scripts/check.mjs:59`). 각각 존재하지 않는 ID·다른 세션으로 바꾼 반례가 **스키마 통과·무결성 오류 0개**였습니다. 대상 타입·세션 범위를 확인하는 검사와 반례가 더 필요합니다.

6. **해결.** 일평균의 단위·시간 단위·분위수, 순간 최대의 추정 표시·두 가정이 제약됐습니다(`schemas/common.schema.json:298`, `schemas/common.schema.json:332`). `announcedAt`과 골든 사례 모델의 비교 단위도 추가됐습니다(`schemas/common.schema.json:285`, `schemas/backtest-summary.schema.json:164`).

7. **해결.** 세 응답에 `evidence[]`가 필수로 추가됐고(`schemas/region-baseline.schema.json:66`, `schemas/similar-event.schema.json:68`, `schemas/insight.schema.json:93`), 개별 근거 조회 API도 있습니다(`openapi/knowledge.yaml:174`). DataEvidence의 기간·수치 참조 구조도 추가됐습니다(`schemas/evidence.schema.json:45`).

8. **부분 해결.** 발행 전 응답은 `forecast-card`로 분리됐고, 전체 Forecast 전송을 거부하는 픽스처도 통과합니다(`schemas/sse-event.schema.json:154`). 카드장인·브리핑·발행 순서도 수정됐습니다(`sse-events.md:25`). 다만 검사기는 이벤트명이 문서에 있는지만 확인하며, 요청했던 **발행 전 전송 금지 시퀀스 검사**는 없습니다(`scripts/check.mjs:115`). 기존 High 문제는 해소됐고 검증 보강이 남았습니다.

9. **해결.** 예보서 조회·스냅샷이 행사·세션·발행 Claim·근거·유사 행사·평시·배치·요약을 포함한 `forecast-report`로 변경됐습니다(`schemas/forecast-report.schema.json:8`, `openapi/records.yaml:87`). 초안 본문과 발행 문장의 연결 규칙, 작성된 섹션의 Claim 필수 조건도 명시됐습니다(`schemas/plan-section.schema.json:5`, `schemas/plan-section.schema.json:72`, `openapi/records.yaml:132`). 새 복합 응답의 일관성 문제는 아래에 별도로 적었습니다.

10. **부분 해결.** 근거 통계·채점판·모델 카드·실행 기록·팀원 작업 기록이 구체화됐습니다(`schemas/datalab-usage.schema.json:8`, `schemas/preregistration-scores.schema.json:8`, `schemas/pipeline-run.schema.json:8`, `schemas/agent-step.schema.json:8`). 그러나 활용 명세표에 필요한 **지표·기간·단위·용도**는 메뉴별 집계에 없고(`schemas/datalab-usage.schema.json:37`), 운영 API는 실행 목록만 제공해 평가 결과·최신성·그래프 크기 계약이 남아 있습니다(`openapi/gateway.yaml:273`). 요구사항은 `docs/plan/11_메뉴_기능_명세.md:108`, `:115`입니다.

3) 새 결함

- **[High] [.harness/tasks/T-601.md:11](/mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사%20데이터랩%20활용%20경진대회/.harness/tasks/T-601.md:11) — 요구된 무결성 오류 응답이 계약을 위반함 —** task는 `gate: "integrity"`인 `gate-report`를 요구하지만 `schemas/gate-report.schema.json:16`은 `A | B | publish`만 허용합니다. 해당 응답을 Ajv로 검증하면 `/gate`의 enum 오류로 거부됩니다. T-601과 T-301의 응답 검증을 함께 만족하도록 gate 값 또는 별도 오류 응답 계약을 통일하고 픽스처를 추가해야 합니다.

- **[Med] `scripts/check.mjs:77` — 문서에 존재하는 유사 행사 수치도 끊어진 참조로 오판함 —** 자리표시자 대상을 `forecast.dailyMean`·`forecast.peakConcurrent` 두 개에서만 찾습니다. `similar[].measured`에 Quantity를 넣고 그 `value`를 인용한 문서는 스키마를 통과하지만 무결성 검사에서 거부됐습니다. 문서 전체와 허용된 그래프 범위의 Quantity를 조회하고, 실제 대상 필드의 존재·타입을 검사해야 합니다.

- **[Med] `schemas/forecast-report.schema.json:45` — 복제된 수치와 배치·요약 참조의 일관성 규칙이 없음 —** `card.peakConcurrent.p50`만 바꾸거나 `layout[].claimIds`·`brief.claimIds`를 없는 ID로 바꿔도 스키마와 현재 무결성 검사를 모두 통과합니다(`scripts/check.mjs:73`). 스냅샷에서 숫자 카드와 본문이 달라지거나 배치 대상이 사라질 수 있습니다. `card`를 `forecast`의 결정적 투영으로 정의하고, 저장·응답 시 값 일치와 모든 배치·요약 참조를 검사해야 합니다.

VERDICT: FAIL -> T-001