[Med] services/forecast/src/crowdcast/rules/judge.py:106 — `basis="구간"`에서도 근거 문장은 확률의 `%` 표시를 포함한 채 생성됩니다. 반환용 표시만 143–146줄에서 바뀌어 구간 표시 문장에 `%`를 쓰지 않는다는 명세와 어긋납니다 — 근거 문장까지 구간 표시 규칙을 적용하고 해당 경로를 테스트하세요.

[Med] services/forecast/src/crowdcast/models/__main__.py:71 — G0 QC는 행사 일정·유형으로 계산한 순간 최대 구간에 의존하지만, 실행 시에는 `labels.parquet` 해시만 확인합니다. `events.parquet`만 바뀌면 이전 QC로 새 버전의 G0를 고정할 수 있습니다 — 현재 행사 입력으로 G0 QC를 재계산해 대조하거나 행사 스냅샷 해시를 검증하세요.

VERDICT: FAIL