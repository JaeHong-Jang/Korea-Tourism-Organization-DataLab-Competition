**3차 지적 5건은 해결됐지만, Claim 상태 전환을 막는 High 회귀 1건이 있어 T-001 재작업이 필요합니다.** 파일은 수정하지 않았습니다.

제공 로그는 101개 검사 항목에서 오류 0개입니다. 직접 실행한 Node·PHP 스키마 43개, Node 무결성 39개·SSE 순서 11개도 기대 판정과 일치했습니다. 아래 결함은 추가 Node 반례로 확인했습니다. 경로는 별도 표시가 없으면 `packages/contracts/` 기준입니다.

**1) 체크리스트**

| 항목 | 판정 | 근거 |
|---|---|---|
| ① 기능·세부 기능 포함 | FAIL | 09·10의 Claim 검증·상태 전환과 충돌 거부 규칙이 양립하지 않습니다(`rules/integrity.mjs:45`). |
| ② 근거·발행 상태·단위 메타데이터 | PASS | 근거 참조, 발행 상태·검사 결과, 단위·시간 단위·추정·발표일 필드가 있습니다(`schemas/claim.schema.json:49`, `schemas/common.schema.json:225`). |
| ③ 세 언어 판정 일치 | PASS | 스키마 픽스처 43개의 TS·Python·PHP 판정이 제공 로그에서 일치하며, Node·PHP 재실행도 같습니다. |
| ④ 도메인 이름 | PASS | 행사·예보·근거·관측·가정·문장·작업 단계 중심으로 이름을 사용합니다. |

**2) 3차 지적 5건**

| 지적 | 상태 | 근거 |
|---|---|---|
| High — 개별 Claim·Evidence 적재 참조 검사 없음 | 해결 | 세션 범위와 재귀 참조 검사가 추가됐습니다. 다른 세션·없는 예보·근거·수치·데이터셋 및 null 수치 필드를 거부합니다(`rules/integrity.mjs:108`, `:148`). |
| Med — 예보서 중복 근거 내용 불일치 | 해결 | 동일 ID 객체의 내용을 정규화해 비교하고, 모든 사본의 참조를 검사합니다(`rules/integrity.mjs:45`, `:152`). |
| Med — Quantity ID 충돌 | 해결 | 수치도 충돌 검사 대상이며, 값이 다른 동일 ID 반례를 거부합니다(`rules/integrity.mjs:13`, `:45`). |
| Med — SSE 게이트 역행 | 해결 | 발행 검사 뒤 게이트, 중복 A, 통과한 B 재실행을 거부합니다. 실패한 B의 재시도는 허용합니다(`rules/sse-sequence.mjs:20`, `:26`). |
| Med — composition 근거 참조 누락 | 해결 | 재귀 탐색이 `composition.evidenceId`도 검사하며, 없는 근거를 연결한 픽스처를 거부합니다(`rules/integrity.mjs:6`, `:65`, `:152`). |

**3) 새 결함·회귀**

- **[High] [rules/integrity.mjs:45](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/integrity.mjs:45>) — 충돌 검사가 Claim의 정상 상태 전환까지 차단함 —** 정상 Claim을 `draft`, `rendered: null`, `checks: []`로 적재하는 반례는 통과했습니다. 이어 같은 ID의 `candidate`와 검사 결과를 넣으면 스키마는 통과하지만 **`같은 id c-yeongjong-3에 다른 내용`**으로 거부됩니다. 실패한 draft의 재작성도 같습니다. 팀원 결과는 세션 그래프에 쓰고 문장은 상태를 전환해야 합니다(`docs/plan/05_기술_아키텍처.md:71`, `docs/plan/09_온톨로지_근거그래프.md:153`). 그러나 `/validate`는 검증만, `/publish`는 `candidate → published`만 정의해 이를 처리할 별도 경로도 없습니다(`openapi/knowledge.yaml:78`, `:112`). 그대로 구현하면 T-601의 적재 규칙이 이후 검증·발행 흐름을 막습니다. **Claim의 허용 상태 전환·검사 결과 갱신·재작성 방식을 revision 검사와 함께 계약화하고, 실제 적재 순서로 검증하는 픽스처를 추가해야 합니다.**

- **[Med] [rules/integrity.mjs:48](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/integrity.mjs:48>) — Event·Forecast는 같은 ID의 내용 충돌을 놓침 —** 두 종류는 객체 대신 ID 집합만 보관합니다. 기존 Event의 지역을 `11110`으로 바꾸거나 기존 Forecast의 `asOf`를 바꿔 재적재하는 반례가 각각 **스키마 통과·무결성 오류 0개**였습니다. 이는 09의 동일 ID 충돌 거부 규칙과 다릅니다. **두 종류도 내용을 비교하고, 수정이 필요한 객체는 허용 갱신 규칙을 명시해야 합니다.**

- **[Med] [rules/integrity.mjs:153](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/integrity.mjs:153>) — 예보서 내부 가정의 기준 그래프 등록 검사가 빠짐 —** 검사가 최상위 `doc.assumptions`에만 적용됩니다. 정상 예보서의 가정 ID와 관련 참조를 모두 `as-not-registered`로 바꾼 반례는 **스키마 통과·무결성 오류 0개**였습니다. 같은 문서의 `forecast`만 검사하면 **`assumption as-not-registered 기준 그래프에 없음`**으로 거부됩니다. 이전의 중첩 예보 검사에서 회귀한 부분입니다. **`forecast-report.forecast.assumptions`에도 동일한 등록 검사를 적용해야 합니다.**

- **[Med] [rules/sse-sequence.mjs:25](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/packages/contracts/rules/sse-sequence.mjs:25>) — 후속 질문 스트림을 새 예보 순서로 오판함 —** 10의 후속 설명·초안 플레이북은 기존 예보에서 `B → publish`로 진행합니다(`docs/plan/10_예보팀_에이전트_구조.md:63`). 이 순서로 구성한 스트림은 모든 이벤트 스키마를 통과하지만, 검사기는 A와 숫자 카드가 없다는 이유 등으로 위반 3개를 반환했습니다. **검사 적용 범위를 새 예보로 명시하거나, 후속 플레이북과 기존 예보 문맥을 받도록 확장해야 합니다.**

VERDICT: FAIL -> T-001