[Med] [doll-geometry.ts:51](/home/data/crowdcast-wt/L4c/apps/web/src/components/scene/crowd/doll-geometry.ts:51) — **측면 소실은 해결됐지만 위쪽 시선에서 소실이 남습니다.** 인형의 XZ 위치를 원점으로 두고 카메라 `(0,328.780,0)`, 표적 `(-120.014,0,0)`을 적용하면 허용 극각 `0.35`·거리 `350`을 만족합니다. 실제 지면 높이에서도 인형은 화면 안에 있지만 투영 면적은 약 `1.4×10⁻¹³px²`입니다. 두 수직 평면 모두 카메라에 모서리만 보이기 때문입니다 — 수평 면이나 카메라를 향하는 빌보드를 추가하고, 화면 중심 밖 인형의 **삼각형 투영 면적**을 검사하세요. 새 테스트의 수평 폭 검사만으로는 잡히지 않습니다.

삼각형은 인형당 **12→24개**, 2,000개에서 **24,000→48,000개**입니다. 기존 측정 조건의 전체 삼각형은 계산상 **121,574→145,574개(+19.7%)**이며 draw call은 유지됩니다. 수정 후 p95는 재측정이 필요합니다.

직접 확인: 폭 조건 4개, 타입 검사, 대상 lint 통과. 위 반례는 실제 형상으로 계산했습니다. 전체 테스트·빌드·성능은 재실행하지 않았으며, 파일 수정과 `.env` 접근은 없습니다.

VERDICT: FAIL