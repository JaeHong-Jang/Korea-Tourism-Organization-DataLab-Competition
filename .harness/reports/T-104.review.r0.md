[High] services/forecast/src/crowdcast/pipeline/run_record.py:104 — 선택한 단계가 모듈 부재로 `skipped`돼도 전체 기록이 `passed`가 되고 종료 코드도 0이다. `--from labels --to batch`에서 재현된다 — 선택 단계의 `skipped`를 전체 성공으로 집계하지 말고 미완료 상태와 종료 코드를 명확히 구분한다.

[High] services/forecast/src/crowdcast/pipeline/__main__.py:34 — `fetch`의 모든 실패를 재시도해 결측률·장부·행사 CLI 실패 같은 데이터 이상도 다시 실행한다 — 네트워크·일시 오류만 1회 재시도하고 나머지는 즉시 중단한다.

[High] services/forecast/src/crowdcast/pipeline/gates.py:43 — 결측률 분모를 파일에 나타난 지역과 날짜만으로 만든다. 날짜 하루나 지역 전체가 빠지면 결측이 0으로 계산될 수 있다 — 수집 대상 날짜 범위와 기준 시군구 목록으로 격자를 만든다.

[High] services/forecast/src/crowdcast/pipeline/gates.py:127 — `features`는 산출물이 없어도 종료 코드 0만으로 통과한다 — `features.parquet` 존재와 `available_at ≤ as_of` 검사 결과를 게이트에서 확인한다.

[High] services/forecast/src/crowdcast/pipeline/stages.py:104 — 백테스트 CLI가 새 결과를 쓰지 않고 0으로 끝나면 이전 `backtest.json`을 이번 산출물로 골라 자기 자신과 비교한 뒤 통과할 수 있다 — 이번 실행 결과를 식별해 이전 결과와 분리하고 그 파일만 해시·판정한다.

[Med] services/forecast/src/crowdcast/pipeline/gates.py:153 — 골든 사례가 0건이면 `all([])` 때문에 골든 재현 게이트가 통과한다 — 사례 수가 0이면 미검증으로 처리한다.

VERDICT: FAIL