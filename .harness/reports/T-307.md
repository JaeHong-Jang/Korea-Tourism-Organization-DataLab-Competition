## T-307 결과: DONE

- 변경 파일(2회차): `services/gateway/` 아래 `src/team/replay/validate-trace.ts`, `src/routes/team-replay.ts`, `tests/replay-roundtrip.test.ts`, `tests/replay-validation.test.ts`, `tests/t307-manual.md`.
- 수정 내용: `done.forecastId === null`을 `new`로 판정. 실제 `OUT_OF_SCOPE` 요청의 왕복 일치와 기존 후속 요청 재생 확인. 라우트 주석 보완.
- 수용 기준: `npm -w services/gateway test` → **744 통과·1 생략**. `npm -w services/gateway run build`·`lint`, `git diff --check` → 모두 통과.
- 의존성 변경: 없음
- CONTRACT-CHANGE: 없음
- BLOCKED: 없음
- 남은 일·주의: 간격 상한 **8000ms** 유지. 기존 curl 측정 기록 유지(원본·상한 적용 후 36ms, 수신 44.645ms). 이번 회차에는 재측정하지 않았으며, 원본·발표 trace 해시 불변 확인.