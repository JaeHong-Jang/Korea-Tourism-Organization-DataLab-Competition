[High] services/forecast/src/crowdcast/data/festival_dates.py:99 — `available_at`을 검사하지 않고 TourAPI 일정을 반영한다. 9월 25일 수집한 9월 29일 시작 행사는 D-14 기준을 넘는데도 채택된다. 공개 시점을 파싱해 행사별 `시작일−14일` 이내 자료만 사전 예보에 사용하도록 차단하라.

[High] services/forecast/src/crowdcast/data/festival_dates.py:147 — 이름 유사도와 지역만 맞으면 문체부의 기존 시작·종료일까지 덮어쓴다. 다른 회차가 합쳐져도 감지하지 못한다. 기존 일정과 충돌하면 보류하고 QC에 양쪽 근거를 남겨라.

[Med] services/forecast/src/crowdcast/data/events.py:39 — QC에 같은 ID로 2025년 5월·10월 일정이 충돌한 사례가 있다(`e-2025-11440-1742170c8f`, `e-2025-52110-175dbd639a`). 현재 병합은 두 일정을 모두 null로 만들어 별도 개최 회차를 잃는다. 동일 행사 중복과 별도 회차를 구분할 ID 규칙을 명세에서 결정하라.

[Med] services/forecast/src/crowdcast/data/festival_dates.py:71 — 문체부 행의 코드가 없으면 동일 이름의 TourAPI 행사도 매칭 대상에서 제외해 별도 ID로 추가한다. QC의 2026년 미매핑 행에 TourAPI가 붙으면 한 행사가 두 행으로 남을 수 있다. 미매핑 후보를 별도 검토 대상으로 기록하고 확정 후 ID 연결 규칙을 마련하라.

[Med] services/forecast/src/crowdcast/data/events.py:24 — `수상` 부분 문자열만으로 `수면` 위험을 켜므로 ‘수상작 전시’ 같은 수상(受賞) 행사도 오분류한다. 물 위 활동을 뜻하는 구체적 표현으로 제한하라.

VERDICT: FAIL