## T-306b 결과: PARTIAL

- 구현: 등급 2 이상에서 매뉴얼 규칙 6개를 발행합니다. 규칙 ID를 우선해 `staffing` 3개·`organization` 2개·`routes-evacuation` 1개를 배치하며, 요원 수는 계산하지 않습니다.
- 변경 파일: `configs/checklist.yaml`, forecast `rules/{checklist,evidence,judge}.py`·관련 테스트 3개, knowledge `ontology/master/rules.ttl`·`tests/test_manual_rules.py`, 계약 `jsonld/{master-ids,master-labels}.json`·`scripts/master_labels.py`, gateway `plan-templates.ts`·`tests/manual-plan{,-fixture}.ts`.
- 수용 기준:
  - forecast 전체: 기본 실행은 `TestClient` 대기로 시간 초과. uvloop 실행은 **1,437 통과·2 실패**. 본 레포 knowledge에 새 규칙이 없어 실패했으며, 이번 워크트리 knowledge로 연결한 재검증은 **2개 모두 통과**.
  - knowledge 전체: uvloop 실행으로 **298 통과**.
  - gateway 전체: **939 통과·1 건너뜀·3 실패**. 기존 HTTP 테스트에서 `listen EPERM`.
  - gateway build·lint, Python ruff, `master_labels.py --check` → **통과**.
  - 신규 문장의 실제 적재 요청 → **SHACL 게이트 B·발행 검증 통과**.
  - `npm --prefix packages/contracts run check` → uv 캐시 쓰기 제한·PHP `vendor/autoload.php` 누락으로 실패. Python 픽스처 44개·JSON-LD 검사는 별도 실행에서 통과.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 허용된 규칙 ID 6개 추가·labels 재생성·선택 `source` 전달만 반영.
- BLOCKED: 전체 수용 기준 통과에는 실행 환경 보완과 본 레포 knowledge 규칙 반영이 필요합니다.
- 남은 일·주의: 통합 후 실패 명령 재실행 및 오케스트레이터의 실제 상담→docx 확인. 변경은 작업 트리에 남겼습니다.