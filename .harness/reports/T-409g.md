## T-409g 결과: PARTIAL

- 구현: 재예보 API, 게이트·발행 흐름 재사용, 동시 요청 차단, 스냅샷 원값 비교, 날씨 근거 판별 완료.
- 변경 파일: `services/gateway/src/`의 `app.ts`, `contract/responses.ts`, `routes/events.ts`, `team/lead/{forecast-event,playbooks,publish}.ts`, `team/reforecast/{error,result,run,weather}.ts`; `services/gateway/tests/reforecast*.ts` 5개.
- 수용 기준: 재예보 테스트 **37개 통과**. `npm -w services/gateway run build`·`run lint` 통과.
- 전체 테스트: `npm -w services/gateway test` → **976 통과·1 건너뜀·3 실패**. 기존 HTTP 테스트에서 `listen EPERM: operation not permitted 127.0.0.1`.
- 의존성 변경: 없음
- CONTRACT-CHANGE: 없음
- BLOCKED: 샌드박스의 포트 리스닝 제한으로 전체 테스트 통과 확인 불가.
- 남은 일·주의: 오케스트레이터 환경에서 전체 테스트와 실제 서비스 연결 확인 필요. 변경은 작업 트리에 남겼습니다.