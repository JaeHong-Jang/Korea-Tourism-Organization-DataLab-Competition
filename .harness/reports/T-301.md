## T-301 결과: DONE

- 변경 파일: `services/gateway/` 안의 `src/clients/{forecast-client,health-client,knowledge-client,records-client,request-json}.ts`, `src/contract/session-facts.ts`, `src/{startup-config,index}.ts`, `tests/{client-requests,knowledge-errors,startup-config}.test.ts`
- 수정 내용: HTTP 메서드 명시·본문 없는 POST 처리, 422 오류 본문 계약 검증·409 상태 보존, 루트 `.env` 로딩·Ollama 후보 순서 적용.
- 수용 기준: `npm -w services/gateway test` → 79개 통과.
- 수용 기준: `npm -w services/gateway run build` 및 `run lint` → 통과.
- 수용 기준: `node scripts/dev.mjs --check --only gateway` → `✓ gateway`, 종료 코드 0.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 없음. `@crowdcast/contracts/rules/sse-sequence.mjs` import와 `sequenceProblems` export 확인.
- BLOCKED: 없음.
- 남은 일·주의: 오케스트레이터 재검토·통합 대기. 변경은 작업 트리에 남겼습니다.