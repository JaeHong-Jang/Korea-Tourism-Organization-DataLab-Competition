## T-443 결과: DONE

- 변경 파일: forecast `data/{events,festival_dates,tourapi,image_cache,image_download,images}.py`, `api/{app.py,routes/images.py,routes/upcoming.py}`, 관련 테스트 4개.
- 구현: 이미지 URL·저작권 보존, 다운로드 CLI·5MB 제한·별도 요청 장부, 캐시 이미지 라우트와 summary `image` 연결.
- 수용 기준:
  - 신규 테스트 → **42 passed**.
  - 전체 테스트(기존 uvloop 러너) → **1,716 passed**, PyTensor 캐시 권한 오류 5건은 `/tmp` 캐시로 재실행해 **5 passed**.
  - `.venv/bin/ruff check services/forecast`, `git diff --check` → 통과.
- 의존성 변경: 없음
- CONTRACT-CHANGE: 없음
- BLOCKED: 없음
- 남은 일·주의: 기본 pytest는 `TestClient` 진입에서 대기하여 uvloop를 사용했습니다. 실제 외부 호출·공유 데이터 변경은 없으며, 9/26 보강 후 오케스트레이터가 `python -m crowdcast.data.images fetch --upcoming`을 실행하면 됩니다.