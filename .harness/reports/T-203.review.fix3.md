### 직전 리뷰 재판정

| 지적 | 판정 |
|---|---|
| High: 동시 실행이 임시 폴더를 충돌시킴 | **해결.** 실행 전체에 비차단 배타 잠금이 걸립니다. |
| High: 모델 카드와 완료 포인터의 이중 발행 | **부분 해결.** 발행은 단일 포인터로 바뀌었지만 T-104 독자는 아직 옛 카드 경로를 읽습니다. |
| High: G0 부분 파일이 고정됨 | **해결.** 잠금 안에서 임시 파일을 완성해 원자 교체합니다. |
| Med: 비교 실행 ID를 재시도 때 잃음 | **해결.** 먼저 공개한 모델 폴더의 `run.json`에서 복원합니다. |
| Med: 최종 경로에 부분 폴더가 노출됨 | **해결.** 완성된 폴더를 한 번에 rename합니다. |
| Med: `publish.py`가 허용 경로 밖임 | **해결.** T-203 허용 경로에 추가됐습니다. |

### 남은 결함

- [High] [T-104 stages.py](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/services/forecast/src/crowdcast/pipeline/stages.py:46>), [run_record.py](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/services/forecast/src/crowdcast/pipeline/run_record.py:18>), [gates.py](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/services/forecast/src/crowdcast/pipeline/gates.py:199>) — 파이프라인은 삭제된 `models/model_card.json`을 계속 요구하므로 train·batch 게이트가 실패합니다. 같은 버전 재실행 때 불변 모델 파일의 수정 시각도 새로워야 한다고 검사합니다 — T-104의 카드 조회와 산출물 게이트를 `latest.json`이 가리키는 버전으로 바꾸고, 재실행 완료 여부는 포인터의 `finishedAt`으로 판정하세요.
- [High] [g0.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/models/g0.py:95) — `labels_g0.json`의 **바이트만** 달라지면 원본 변경을 허용합니다. 실제 테스트도 QC에 줄바꿈만 추가해 “재생성”을 흉내 냅니다. 이 상태에서 `events.parquet`가 바뀌어도 오래된 G0 집계를 새 입력의 사전 판정으로 고정할 수 있습니다 — 현재 labels·events에서 G0 집계를 다시 계산해 QC와 대조하거나, T-103이 기록한 원본 해시를 검증하세요.
- [Med] [T-203.md](</mnt/c/Users/User/Desktop/대학교/3학년/한국관광공사 데이터랩 활용 경진대회/.harness/tasks/T-203.md:7>) — 허용 경로는 고쳤지만 실행 산출물에는 삭제된 공유 모델 카드가 남아 있어 산출물 게이트의 기준이 모호합니다 — 모델 카드를 `models/<modelVersion>/model_card.json`으로, 완료 기준을 `reports/backtest/latest.json`으로 갱신하세요.

읽기 전용 `git diff --check`는 통과했습니다. 파일 수정 금지 지시에 따라 쓰기 작업을 동반하는 테스트는 실행하지 않았습니다.

VERDICT: FAIL