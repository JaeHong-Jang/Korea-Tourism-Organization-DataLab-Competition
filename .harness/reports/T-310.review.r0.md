[Med] services/gateway/src/clients/request-json.ts:70 — 새 조회 경로가 공용 클라이언트의 `response.ok`에 의존해, 계약상 200만 허용된 상류의 201·206 응답도 유효한 JSON이면 200으로 발행한다. 리다이렉트도 자동 추적한다 — 상태를 200으로 한정하고 리다이렉트를 거부한다.

[Med] services/gateway/src/clients/records-relay-client.ts:70 — JSON 응답의 Content-Type을 확인하지 않아 `text/html`에 담긴 유효한 JSON도 통과한다 — records.yaml의 `application/json`을 검증하고 위반 시 503을 반환한다.

[Med] services/gateway/src/routes/records-relay.ts:160 — `text/plain` 요청 본문도 JSON으로 파싱해 변경 요청으로 중계한다 — records.yaml이 요구하는 `application/json`을 본문 처리 전에 확인한다.

[Med] services/gateway/src/clients/records-relay-client.ts:62 — docx를 크기 제한 없이 `arrayBuffer()`로 읽고 JSON도 제한 없이 파싱해 큰 응답이 게이트웨이 메모리를 소진할 수 있다 — 본문 크기 상한을 적용하고 docx는 제한을 지키며 스트리밍한다.

[Med] services/gateway/src/routes/proxy-response.ts:45 — 상위 요청의 중단 신호를 클라이언트 옵션에 전달하지 않아 클라이언트가 연결을 끊어도 상류 요청이 최대 5초간 계속된다 — 요청 신호를 전달하고 병렬 조회 실패 시 남은 호출도 중단한다.

[Low] services/gateway/src/routes/records-relay.ts:145 — Hono가 점 세그먼트를 먼저 정규화하므로 `/api/records/tmp/../ledger`가 목록 밖 404 대신 `ledger`로 중계된다. 금지된 records API가 열리지는 않지만 원시 경로 검사는 우회된다 — 원시 요청 경로에서 점 세그먼트를 거부한다.

VERDICT: FAIL