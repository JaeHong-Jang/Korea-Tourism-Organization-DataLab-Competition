- [Med] [prediction-scatter.tsx:45](/home/data/crowdcast-wt/L4b/apps/web/src/features/validation/prediction-scatter.tsx:45) — `Math.max(value, low)`가 계약상 허용되는 `p10=0`을 양수인 축 최솟값으로 그려 구간을 왜곡합니다. 전부 0이면 표 보기까지 사라집니다 — 비양수 값의 로그 축 제외·절단을 명시하고, 원본 값의 표 보기는 항상 제공하세요.

- [Med] [api.ts:31](/home/data/crowdcast-wt/L4b/apps/web/src/lib/validation/api.ts:31) — HTTP 200의 손상된 JSON은 `SyntaxError`가 발생해 `useContract`에서 오류가 아닌 미공개 빈 상태로 처리됩니다 — JSON 파싱 실패도 계약 오류로 분류하고 오류 안내·수치 미표시를 검증하세요.

- [Med] [preregistration-board.tsx:71](/home/data/crowdcast-wt/L4b/apps/web/src/features/validation/preregistration-board.tsx:71) — 실측의 `timeUnit`을 생략해 같은 `명` 단위인 순간 인원과 기간 누적 인원이 구분되지 않습니다 — 시간 단위를 함께 표시하세요. 기존 `formatQuantity`를 재사용할 수 있습니다.

- [Med] [prediction-scatter.tsx:157](/home/data/crowdcast-wt/L4b/apps/web/src/features/validation/prediction-scatter.tsx:157) 및 다른 표 래퍼 — 가로 스크롤 영역과 내부 셀에 명시적인 포커스 대상이 없습니다. 스크롤 영역을 자동으로 탭 순서에 넣지 않는 브라우저에서는 키보드로 오른쪽 열에 접근하기 어렵습니다 — 래퍼에 `tabIndex={0}`과 접근 가능한 영역 이름을 추가하세요.

High 없음. 정적 검토만 수행했으며 파일 수정·서비스 모듈 import·`.env` 열람은 하지 않았습니다.

VERDICT: PASS