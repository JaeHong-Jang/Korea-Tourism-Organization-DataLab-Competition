2회차 피드백 판정:

1. **해결** — 비양수 p50·실측은 로그 축에서 제외하고, p10≤0은 화살표로 표시합니다. 전부 0이어도 표 전환이 유지됩니다.
2. **해결** — JSON 파싱 실패를 계약 오류로 변환하며, `useContract`가 오류 상태로 분류합니다.
3. **해결** — 사전 등록 실측에 `formatQuantity`를 적용해 시간 단위·공간 범위도 표시합니다.
4. **부분 해결** — S6 산점도·골든·사전 등록 표는 수정됐지만, S7 활용 명세표가 빠졌습니다.
5. **해결** — `.validation-cases`의 `align-self: start`로 골든 카드 높이가 내용에 맞춰집니다.

- [Med] [datalab-spec-table.tsx:26](/home/data/crowdcast-wt/L4b/apps/web/src/features/insights/datalab-spec-table.tsx:26) — **4번 잔존:** 가로 스크롤 래퍼와 내부에 포커스 대상이 없어, 스크롤 영역을 자동으로 탭 순서에 넣지 않는 브라우저에서는 오른쪽 열에 키보드로 접근하기 어렵습니다 — `tabIndex={0}`, `role="region"`, 구별되는 `aria-label`을 추가하세요.

- [Med] [golden-cases.tsx:55](/home/data/crowdcast-wt/L4b/apps/web/src/features/validation/golden-cases.tsx:55) — **신규 발견:** 골든 실측의 `estimated=true`를 표시하지 않아 추정값이 확정 실측처럼 보입니다. 주최 측 예상 셀도 같은 메타데이터를 누락합니다 — 두 Quantity 셀에 `formatQuantity`를 적용해 추정 여부를 보존하세요.

- [Med] [golden-cases.tsx:47](/home/data/crowdcast-wt/L4b/apps/web/src/features/validation/golden-cases.tsx:47) — **신규 발견:** 계약이 허용하는 `value=null, p50=5000` 응답을 `—`로 표시합니다. 실측 셀도 동일해 대표값이 있어도 사라집니다 — `value ?? p50`을 사용하는 공용 포매터로 표시하고 해당 응답을 검증하세요.

High 없음. 정적 검토만 수행했습니다. test 135·build·lint·색 검사 및 스크린샷 확인은 제공된 오케스트레이터 결과이며 재실행하지 않았습니다. 파일 수정·서비스 모듈 import·`.env` 열람은 하지 않았습니다.

VERDICT: PASS