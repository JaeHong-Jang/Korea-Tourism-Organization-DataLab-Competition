## T-441 결과: PARTIAL

- 변경 파일: `services/gateway/{src,tests,evals}/`, forecast의 `analytics/upcoming.py`, `api/routes/upcoming.py`, `tests/api/test_upcoming{_event,_predict}.py`.
- 구현: 방문객 추천, 목적 되묻기, eventId 바로 예보, 이미지·그래프 중계, recommend 시나리오 3개 완료.
- 수용 기준:
  - gateway build·lint 통과.
  - gateway 테스트: 포트 제한 테스트 3개 제외 **1,076 통과, 1 skip**. 전체 실행은 `listen EPERM`으로 실패.
  - Python: 임시 이벤트 루프 보완으로 **280 통과**. 별도 knowledge 프로세스 검사 3개 제외. 원래 명령은 TestClient 대기로 완료하지 못함.
  - ruff·`git diff --check` 통과.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 없음.
- BLOCKED: 샌드박스 실행 제약으로 전체 수용 기준 재검증 필요.
- 남은 일·주의: 기존 `upcoming.parquet`에 입력 스냅샷을 넣도록 일괄 예보 재생성 필요. 실제 시나리오 실행은 오케스트레이터 담당.