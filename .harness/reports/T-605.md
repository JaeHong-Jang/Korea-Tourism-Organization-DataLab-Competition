## T-605 결과: DONE

- 변경 파일: `services/knowledge/` 아래 `src/knowledge/lineage/*`, `api/app.py`, `store/master.py`, `ontology/crowdcast.ttl`, 게이트 SHACL, 계보 테스트·픽스처.
- 구현: PROV 적재, 해시 기반 멱등성·버전 증가, 실패 이력 보존, 모델→데이터셋 단일 SPARQL 질의, 기동·CLI 연결.
- 수용 기준: knowledge 전체 **324 passed**, ruff·`git diff --check` 통과. CLI·재시작·통계 계약도 검증.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 없음.
- BLOCKED: 없음.
- 남은 일·주의: 기본 pytest는 샌드박스의 `socketpair.send()` 차단으로 멈춰, 설치된 uvloop를 적용해 전체 테스트를 통과했습니다. 실제 서비스 기동 확인은 오케스트레이터 담당입니다.