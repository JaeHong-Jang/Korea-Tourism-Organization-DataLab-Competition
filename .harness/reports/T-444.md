## T-444 결과: DONE

- 구현: `GET /v1/master/graph` 추가. 한국어 라벨·출처 URL·계보 방향·계약 검증·결정적 정렬·생성 시각 보존·파일 묶음 지원, 세션 그래프 제외.
- 변경 파일: knowledge의 `api/{app,master_graph}.py`, `query/graph_{view,labels,nodes,files}.py`, `store/{master,graph_view}.py`, `tests/test_master_graph_{api,snapshot,size}.py`.
- 수용 기준: knowledge 전체 pytest **340개 통과**(기존 uvloop 적용), `.venv/bin/ruff check services/knowledge` 및 `git diff --check` 통과.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 없음.
- BLOCKED: 없음.
- 남은 일·주의: 기본 asyncio 실행은 TestClient 시작에서 시간 초과하여 uvloop로 검증했습니다. 파일 외 노드만으로 제한을 초과하면 500을 반환합니다. 실제 서비스 확인은 오케스트레이터 담당입니다.