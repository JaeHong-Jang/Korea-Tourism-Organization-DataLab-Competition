## T-208 결과: DONE

- 변경 파일: `configs/weather_adjustment.yaml`, forecast의 `api/assemble/{forecast,http,weather,weather_lookup}.py`, `features/weather_history.py`, `models/weather_adjust.py`, 관련 테스트.
- 구현: 요청 예보에 날씨 근거·우천 점검 연결. 계수 없으면 수치 유지, 충분한 표본의 계수만 적용. 일괄 예보는 날씨 호출 없음.
- 수용 기준:
  - `pytest -q services/forecast/tests` → **1,602개 통과**. 샌드박스 TestClient 대기 문제는 `/tmp/t208-test-support` 실행 보완 적용.
  - `.venv/bin/ruff check services/forecast/src services/forecast/tests` → 통과.
  - `python -m crowdcast.models.weather_adjust fit` → `자료 없음 — weather_daily.parquet`, 정상 종료.
- 의존성 변경: 없음
- CONTRACT-CHANGE: 없음
- BLOCKED: 없음
- 남은 일·주의: 실제 기상청 호출·knowledge 연결 확인은 지시대로 오케스트레이터 담당. 변경은 작업 트리에 남겼습니다.