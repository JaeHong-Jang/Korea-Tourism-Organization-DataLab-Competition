[High] [fake-forecast.ts:35](/home/data/crowdcast-wt/L3/services/gateway/src/team/runtime/fake-forecast.ts:35) — 가짜 예보가 2025년 픽스처의 수치를 임의의 입력 행사에 붙이고 `asOf`만 바꾼다. 기본 모드도 `fake`여서 수동 기록의 2026년 행사에 이 수치가 게이트 A 통과 후 표시됐다. — 가짜 응답은 명시적인 데모 행사로 제한하고, 실제 상담의 숫자 카드는 결정적 forecast 서비스 응답에서만 만들도록 한다.

[High] [gates.ts:4](/home/data/crowdcast-wt/L3/services/gateway/src/team/lead/gates.ts:4) — 게이트 A에서 S04를 빼고 있어 데이터셋 제목·발행기관 검사를 건너뛴 채 숫자 카드를 보낼 수 있다. T-303의 6개 규칙 지시와 `docs/plan/10` §3 및 knowledge 구현의 7개 규칙이 충돌한다. — 오케스트레이터가 task의 검사 범위를 바로잡고 S04를 포함한 실패 사례를 검증한다.

[Med] [draft-answer.ts:53](/home/data/crowdcast-wt/L3/services/gateway/src/team/analysis/draft-answer.ts:53) — 누락 필드를 거의 모두 필수 질문으로 바꿔 수동 확인에서 7개 `ask`를 한꺼번에 보냈다. — 날짜·장소·유형을 우선 확인하고, 요금은 계약값 `미상`, 예산은 `null`로 둘 수 있게 한다. 주최 유형과 정확한 시작·종료 시각 및 시간대는 현재 Event 계약에 미상값이 없어 임의 기본값을 넣으면 안 된다. 이를 생략하려면 계약과 예보 입력을 먼저 바꾸고, 그 전에는 시간 정보를 한 질문으로 묶는다.

[Med] [dictation-step.ts:23](/home/data/crowdcast-wt/L3/services/gateway/src/team/analysis/dictation-step.ts:23) — 실제 문장에 ‘불꽃축제’가 있는데 추출된 `typeText`가 null이면 그대로 유형을 되묻는다. — 원문에 명시된 유형 표현을 제한된 규칙으로 재확인해 `불꽃`으로 정규화하고 해당 문장을 회귀 사례로 추가한다.

[Med] [playbooks.ts:58](/home/data/crowdcast-wt/L3/services/gateway/src/team/lead/playbooks.ts:58) — 행사 `/facts` 적재 전에 `session.analyzed`를 설정한다. 적재가 실패해도 같은 세션은 이후 `OUT_OF_SCOPE`가 되어 답변이나 재시도를 이어갈 수 없다. — 실제 적재 성공 시점에 상태를 전환하고, 실패한 부분 적재의 재시도 규칙을 명시한다.

[Med] [events.ts:61](/home/data/crowdcast-wt/L3/services/gateway/src/team/runtime/events.ts:61) — trace 쓰기와 SSE 전송은 전체 마감 신호로 제한되지 않는다. 파일 또는 전송이 멈추면 20초 타이머가 지나도 `done`과 세션 잠금 해제가 지연된다. — 이벤트 쓰기도 마감·연결 종료에 묶고 기록 실패 시 종료 경로를 보장한다.

[Med] [events.ts:65](/home/data/crowdcast-wt/L3/services/gateway/src/team/runtime/events.ts:65) — JSONL에 `event_card` 전체를 저장해 행사명·장소·홍보 문구 등 사용자가 입력한 원문 조각이 계속 남는다. — trace에는 작업 상태와 식별자 위주로 남기고 입력 조각은 제거하거나 필요한 범위로 축약한다.

[Low] [request-json.ts:44](/home/data/crowdcast-wt/L3/services/gateway/src/clients/request-json.ts:44) — 필수 POST 본문이 런타임에서 `undefined`가 되면 Ajv 검증을 건너뛰고 본문 없는 요청을 전송한다. — `bodySchema`가 있으면 값이 `undefined`여도 검증해 거부한다.

VERDICT: FAIL