[Med] [kit-page.tsx:106](/home/data/crowdcast-wt/L4a/apps/web/src/pages/dev/kit-page.tsx:106) — 카드로 포커스를 옮기면서 출발 칩을 저장하지 않고, `preventDefault()`로 앵커 이동 이력도 남기지 않습니다. 복귀 UI도 없습니다. 모델 `[2]`는 세 위치에 있어 근거 ID만으로 원래 위치를 구분할 수 없습니다 — 실제 활성화한 칩 요소를 보관하고, 키보드로 실행 가능한 “읽던 곳으로” 동작에서 스크롤·포커스를 복원하세요.

[Low] [evidence-chip.tsx:65](/home/data/crowdcast-wt/L4a/apps/web/src/components/common/evidence-chip.tsx:65) — 확장된 다중 행 `onClick` 콜백 바로 위에 한국어 설명 주석이 없어 AGENTS §5-1을 충족하지 않습니다 — 사용자 지정 이동과 기본 앵커 동작의 분기 이유를 한 줄로 적으세요.

요청한 네 항목은 다음과 같이 확인했습니다.

- **번호 기본값:** 생략 시 `[1]·[2]` 테스트 통과.
- **배치·출처 통합:** 가로 배치와 `title`·`aria-description` 미리보기 확인.
- **테스트 주석:** 첫 `it` 위 주석 추가 확인.
- **카드 이동:** DOM 재현에서 칩 7개 모두 열기·강조·포커스 이동·스크롤 호출 확인. Enter는 네이티브 링크 활성화 경로이며 실제 브라우저 키 입력은 재검증하지 않았습니다.

같은 근거의 여러 칩은 동일 카드로 연결되고 DOM ID 중복은 없습니다. 강조는 추가 외곽선으로 구분되어 색에만 의존하지 않습니다. 변경 파일 색 리터럴 0건, 스크린샷 3장과 해시 일치를 확인했습니다.

`.env` 로딩·파일 출력을 끈 테스트 73개, 메모리 빌드, 타입 검사, lint·diff 검사 통과. 파일 수정과 `.env` 접근은 없었습니다.

VERDICT: FAIL