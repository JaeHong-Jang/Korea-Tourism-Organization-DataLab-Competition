직전 Med 지적인 **버전 2 해시 미대조는 해결됐습니다**. `read_previous`가 기존 산출물을 덮어쓰기 전에 parquet 해시와 연도별 집계를 확인합니다. 두 파일이 없는 첫 실행은 진행하고, 이전 기록은 있으나 parquet가 없으면 중단합니다.

- **[Med] 같은 스냅샷 재실행이 정상 산출물을 거부할 수 있습니다.** [merge.py](/home/data/crowdcast-wt/L1/services/forecast/src/crowdcast/labels/merge.py:25)는 SNR을 소수 여섯 자리로 반올림하지만, 최초 유의 집계는 반올림 전 SNR을 씁니다. [silver_history.py](/home/data/crowdcast-wt/L1/services/forecast/src/crowdcast/labels/silver_history.py:75)는 parquet의 반올림된 SNR로 집계를 복원합니다. 예를 들어 SNR `3.0000004`는 최초에는 유의 신호이고 parquet에서는 `3.0`이므로, 다음 실행에서 집계 불일치로 중단됩니다. 판정과 재검증의 정밀도를 맞추고 경계값 재실행을 검증해야 합니다.
- **[Low] 집계 변조 회귀 테스트가 새 검증을 확인하지 못합니다.** [test_cli.py](/home/data/crowdcast-wt/L1/services/forecast/tests/labels/test_cli.py:216)는 유의 수만 늘려 기존 비율의 분모와 불일치하게 만듭니다. 따라서 parquet 집계 대조에 도달하기 전에 실패할 수 있습니다. 해시 교체 사례는 해당 검증을 확인합니다.

VERDICT: FAIL