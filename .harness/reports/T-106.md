## T-106 결과: DONE

- 변경 파일: `services/forecast/src/crowdcast/pipeline/{__main__,run_record,record_privacy,freshness,freshness_cache,summary,summary_facts}.py`, `api/{app.py,routes/ops.py}`, `services/forecast/tests/{api/test_ops.py,pipeline/test_freshness.py,pipeline/test_summary.py}`.
- 구현: 실행 목록·최신성 API, Ollama 숫자 가드·템플릿 대체, 기존 실행 요약 CLI. 요약 실패 시 단계 결과·종료 코드를 보존합니다.
- 수용 기준: 기본 `pytest -q services/forecast/tests`는 샌드박스 `socketpair.send()` 권한 제한으로 시간 초과(124). 이벤트 루프 타이머를 적용한 동일 전체 테스트는 **1,451개 통과**. 최종 최적화 후 관련 테스트 **167개 통과**.
- 수용 기준: `ruff check services/forecast/src services/forecast/tests`·`git diff --check` → 통과.
- 실제 자료 확인: 최신성 6행 계약 검증 통과. 최초 **5.28초**, 반복 **1.32초**.
- 의존성 변경: 없음.
- CONTRACT-CHANGE: 없음. 전용 필드가 없는 운영 정보는 `title`에 표시하고, 일괄 예보 시각은 파일 갱신 시각 기반 **추정**으로 표시했습니다.
- BLOCKED: 없음.
- 남은 일·주의: 오케스트레이터 환경에서 기본 pytest 재확인·서비스 기동·기존 기록 요약 보강 필요. 공유 산출물과 기존 `reports/backtest` 변경은 건드리지 않았습니다.