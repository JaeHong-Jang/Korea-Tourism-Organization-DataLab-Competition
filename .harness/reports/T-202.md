## T-202 결과: DONE

- 변경 파일: `configs/{peak_profiles,thresholds,checklist}.yaml`, `services/forecast/src/crowdcast/rules/{__init__,peak,judge,checklist,evidence}.py`, `services/forecast/tests/rules/`의 6개 파일. 모두 허용 경로.
- 수용 기준: `uv run --package crowdcast-forecast pytest -q services/forecast/tests/rules` → **125 passed**. `/tmp` uv 캐시·오프라인 환경 사용.
- 검증: 4등급 경계, 인원 무관 법정 조건, 우천 대비, 재현성·확률 일관성, 근거 연결·등록 ID·계약 스키마 통과. Ruff·포맷 검사 통과.
- 의존성 변경: 없음
- CONTRACT-CHANGE: 없음
- BLOCKED: 없음
- 남은 일·주의: T-204에서 `judge()` 반환값의 `judgment`, `probabilities`, `evidence`를 API 응답에 조립.