[High] [configs/thresholds.yaml:34](/home/data/crowdcast-wt/L2/configs/thresholds.yaml:34) — 도달 확률이 정확히 50%여도 중앙값은 기준 미만일 수 있습니다. 기존 테스트의 `[0] × 2000 + [1000] × 2000` 표본은 3등급이지만 중앙값은 500명이라 새 사유 문장이 사실과 다릅니다. 등급과 확률은 유지하고, 이 경계에서는 중앙값을 단정하지 않는 별도 문구를 쓰도록 수정하세요.

[Med] [upcoming.py:50](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/analytics/upcoming.py:50) — runId는 현재 설정 파일을 해시하지만, 예보 계산의 `rule_settings()`와 `_profiles()`는 설정을 캐시합니다. 같은 프로세스에서 파일이 바뀌면 새 runId에 이전 설정으로 계산한 예보가 붙을 수 있습니다. 실행마다 해시한 내용과 실제 사용한 설정을 일치시키고, 캐시가 채워진 뒤 설정을 바꾸는 경우를 검증하세요.

정적 Ruff 검사는 통과했습니다. 서비스 모듈과 테스트는 실행하지 않았습니다.

VERDICT: FAIL