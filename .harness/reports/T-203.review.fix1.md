직전 지적 중 **요청 달력 문제는 해결**됐습니다. `calendar_features.py`가 요청의 `holiday_calendar`를 읽지 않고 규칙 달력만 사용합니다. **재실행 중 기존 발행물 보호 문제는 부분 해결**입니다. 검증 전 실패는 보호하지만, 폴더 교체를 시작한 뒤의 실패와 중단은 여전히 위험합니다.

G0 규칙 변경 자체는 “조용한 재사용 금지”를 깨지 않습니다. QC 해시가 바뀌면 새 모델 버전에서 다시 판정하고 `supersedes`를 기록하며, 같은 버전의 다른 입력·결정은 `freeze_g0`가 거부합니다. 다만 아래 폴더 교체가 고정된 `g0.json`의 **경로와 파일 자체**를 잠시 없애거나 복사본으로 대체합니다.

- [High] [publish.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/models/publish.py:30) — 기존 `latest.json`이 같은 runId를 가리키는 동안 모델·보고서 폴더를 차례로 이동합니다. 그 사이 독자는 없는 모델 폴더나 새 모델·옛 보고서를 볼 수 있고, 프로세스가 중단되면 G0가 `.previous-*`에만 남을 수 있습니다 — 재실행 산출물을 고유한 불변 경로에 발행하고, 모두 준비된 뒤 포인터 하나를 교체하며 중단 후 복구 경로를 마련하세요.
- [High] [__main__.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/models/__main__.py:149) — 폴더 교체 후 백업을 삭제한 다음 공유 카드와 `latest.json`을 씁니다. 이 단계의 쓰기·교체가 실패하면 옛 완료 표시가 바뀐 폴더를 가리키고, 공유 카드만 앞서갈 수도 있습니다 — 완료 표시 교체까지 이전 상태를 복구할 수 있게 유지하거나 위의 불변 경로 발행 방식으로 묶으세요.
- [Med] [publish.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/models/publish.py:47) — 공유 파일 쓰기·교체 실패 시 `.tmp` 정리가 없고, `.previous-*` 삭제 오류도 무시합니다 — 실패 시 임시 파일을 정리하고 백업 삭제 실패를 확인·보고하세요.
- [Med] [__main__.py](/home/data/crowdcast-wt/L2/services/forecast/src/crowdcast/models/__main__.py:229) — 평가 가능 연도가 없으면 임시 폴더를 우회해 최종 runId 폴더에 `backtest.md`를 쓰고 실패합니다 — 실패 진단을 완료된 실행 폴더와 분리하세요.

AGENTS.md §5의 파일 길이·주석·파일 분리에서 추가 위반은 찾지 못했습니다. `git diff --check`는 통과했습니다. 파일은 수정하지 않았고 테스트는 실행하지 않았습니다.

VERDICT: FAIL